import typing

from PyQt6 import QtCore, QtGui, QtWidgets
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


class NumpadButton(QtWidgets.QPushButton):
    def __init__(self, parent=None):
        args = [parent]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNumpadButtonǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁNumpadButtonǁ__init____mutmut_mutants'), args, kwargs, self)
    def xǁNumpadButtonǁ__init____mutmut_orig(self, parent=None):
        super().__init__(parent)
        self._position: str = ""
    def xǁNumpadButtonǁ__init____mutmut_1(self, parent=None):
        super().__init__(None)
        self._position: str = ""
    def xǁNumpadButtonǁ__init____mutmut_2(self, parent=None):
        super().__init__(parent)
        self._position: str = None
    def xǁNumpadButtonǁ__init____mutmut_3(self, parent=None):
        super().__init__(parent)
        self._position: str = "XXXX"
    
    xǁNumpadButtonǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNumpadButtonǁ__init____mutmut_1': xǁNumpadButtonǁ__init____mutmut_1, 
        'xǁNumpadButtonǁ__init____mutmut_2': xǁNumpadButtonǁ__init____mutmut_2, 
        'xǁNumpadButtonǁ__init____mutmut_3': xǁNumpadButtonǁ__init____mutmut_3
    }
    xǁNumpadButtonǁ__init____mutmut_orig.__name__ = 'xǁNumpadButtonǁ__init__'

    def get_position(self):
        """Get numpad button position"""
        return self._position

    def set_position(self, value):
        args = [value]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNumpadButtonǁset_position__mutmut_orig'), object.__getattribute__(self, 'xǁNumpadButtonǁset_position__mutmut_mutants'), args, kwargs, self)

    def xǁNumpadButtonǁset_position__mutmut_orig(self, value):
        """Set position"""
        self._position = str(value).lower()

    def xǁNumpadButtonǁset_position__mutmut_1(self, value):
        """Set position"""
        self._position = None

    def xǁNumpadButtonǁset_position__mutmut_2(self, value):
        """Set position"""
        self._position = str(value).upper()

    def xǁNumpadButtonǁset_position__mutmut_3(self, value):
        """Set position"""
        self._position = str(None).lower()
    
    xǁNumpadButtonǁset_position__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNumpadButtonǁset_position__mutmut_1': xǁNumpadButtonǁset_position__mutmut_1, 
        'xǁNumpadButtonǁset_position__mutmut_2': xǁNumpadButtonǁset_position__mutmut_2, 
        'xǁNumpadButtonǁset_position__mutmut_3': xǁNumpadButtonǁset_position__mutmut_3
    }
    xǁNumpadButtonǁset_position__mutmut_orig.__name__ = 'xǁNumpadButtonǁset_position'

    def paintEvent(self, e: QtGui.QPaintEvent | None):
        args = [e]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNumpadButtonǁpaintEvent__mutmut_orig'), object.__getattribute__(self, 'xǁNumpadButtonǁpaintEvent__mutmut_mutants'), args, kwargs, self)

    def xǁNumpadButtonǁpaintEvent__mutmut_orig(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_1(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = None
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_2(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(None)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_3(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = None
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_4(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(None)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_5(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(None, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_6(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, None)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_7(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_8(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, )
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_9(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, False)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_10(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(None, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_11(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, None)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_12(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_13(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, )
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_14(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, False)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_15(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(None, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_16(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, None)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_17(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_18(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, )

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_19(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, False)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_20(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = None
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_21(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = None

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_22(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None and _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_23(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is not None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_24(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is not None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_25(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = None
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_26(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(None, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_27(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, None, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_28(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, None)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_29(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_30(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_31(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, )
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_32(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = None

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_33(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(None, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_34(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, None, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_35(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, None)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_36(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_37(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_38(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, )
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_39(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(165, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_40(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 165, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_41(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 165)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_42(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(None, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_43(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, None, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_44(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, None)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_45(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_46(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_47(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, )
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_48(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(224, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_49(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 224, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_50(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 224)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_51(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = None
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_52(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = None
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_53(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() * 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_54(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 3.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_55(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = None
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_56(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() * 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_57(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 3.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_58(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(None)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_59(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position != "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_60(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "XXleftXX":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_61(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "LEFT":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_62(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                None,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_63(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                None,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_64(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                None,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_65(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                None,
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_66(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_67(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_68(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_69(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_70(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 - self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_71(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                1 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_72(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                1,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_73(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(None, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_74(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, None)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_75(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_76(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, )

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_77(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                None,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_78(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                None,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_79(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                None,
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_80(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                None,
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_81(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                None,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_82(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                None,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_83(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                None,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_84(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_85(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_86(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_87(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_88(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_89(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_90(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_91(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                1,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_92(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                1,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_93(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(None, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_94(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, None)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_95(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_96(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, )
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_97(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position != "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_98(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "XXrightXX":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_99(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "RIGHT":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_100(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                None,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_101(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                None,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_102(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                None,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_103(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                None,
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_104(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_105(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_106(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_107(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_108(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                1,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_109(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                1,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_110(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() * 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_111(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 3,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_112(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(None, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_113(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, None)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_114(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_115(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, )

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_116(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                None,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_117(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                None,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_118(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                None,
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_119(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                None,
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_120(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                None,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_121(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                None,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_122(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                None,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_123(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_124(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_125(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_126(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_127(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_128(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_129(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_130(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                1,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_131(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                1,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_132(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(None, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_133(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, None)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_134(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_135(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, )

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_136(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position != "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_137(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "XXdownXX":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_138(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "DOWN":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_139(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                None,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_140(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                None,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_141(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                None,
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_142(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                None,
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_143(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                None,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_144(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                None,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_145(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                None,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_146(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_147(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_148(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_149(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_150(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_151(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_152(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_153(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                1,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_154(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                1,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_155(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(None, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_156(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, None)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_157(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_158(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, )

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_159(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                None,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_160(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                None,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_161(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                None,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_162(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                None,
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_163(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_164(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_165(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_166(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_167(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                1,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_168(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                1,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_169(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(None, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_170(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, None)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_171(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_172(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, )
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_173(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = None

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_174(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = None
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_175(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            None,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_176(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            None,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_177(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            None,
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_178(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            None,
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_179(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_180(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_181(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_182(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_183(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left() - self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_184(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() / 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_185(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 1.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_186(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top() - self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_187(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() / 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_188(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 1.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_189(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() / 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_190(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 1.9),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_191(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() / 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_192(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 1.9),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_193(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(None)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_194(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(None)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_195(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(None)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_196(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = None
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_197(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = None

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_198(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            None,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_199(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            None,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_200(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            None,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_201(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            None,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_202(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_203(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_204(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_205(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_206(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() / 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_207(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 3.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_208(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() / 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_209(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 3.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_210(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() / 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_211(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 1.8,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_212(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() / 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_213(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 1.8,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_214(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = None
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_215(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(None)
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_216(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(None)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_217(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position - margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_218(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = None
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_219(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(None)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_220(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(None)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_221(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(2)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_222(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(None)
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_223(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(None, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_224(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, None, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_225(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, None))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_226(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_227(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_228(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, ))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_229(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(1, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_230(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 1, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_231(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 1))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_232(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(None)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_233(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                None,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_234(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                None,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_235(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                None,
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_236(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_237(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_238(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_239(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft & QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_240(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic & QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_241(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(None),
            )
            painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁNumpadButtonǁpaintEvent__mutmut_242(self, e: QtGui.QPaintEvent | None):
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionButton()
        self.initStyleOption(opt)

        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)

        _rect = self.rect()
        _style = self.style()

        if _style is None or _rect is None:
            return
        margin = _style.pixelMetric(_style.PixelMetric.PM_ButtonMargin, opt, self)
        bg_color = (
            QtGui.QColor(164, 164, 164)
            if self.isDown()
            else QtGui.QColor(223, 223, 223)
        )

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        if self._position == "left":
            path.addRect(
                0 + self.rect().toRectF().normalized().height(),
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)
        elif self._position == "right":
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width() / 2,
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)

            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        elif self._position == "down":
            path.addRoundedRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
                xRadius,
                yRadius,
                QtCore.Qt.SizeMode.AbsoluteSize,
            )
            painter.fillPath(path, bg_color)

        else:
            path.addRect(
                0,
                0,
                self.rect().toRectF().normalized().width(),
                self.rect().toRectF().normalized().height(),
            )
            painter.fillPath(path, bg_color)
        icon_path = QtGui.QPainterPath()

        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)

        # Draw Icon
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )

        if self.text():
            _start_text_position = int(self.button_ellipse.right())
            _rect.setLeft(_start_text_position + margin)
            _pen = painter.pen()
            _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
            _pen.setWidth(1)
            _pen.setColor(QtGui.QColor(0, 0, 0))
            painter.setPen(_pen)

            painter.drawText(
                _rect,
                QtCore.Qt.TextFlag.TextShowMnemonic
                | QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter,
                str(self.text()),
            )
            painter.setPen(None)
    
    xǁNumpadButtonǁpaintEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNumpadButtonǁpaintEvent__mutmut_1': xǁNumpadButtonǁpaintEvent__mutmut_1, 
        'xǁNumpadButtonǁpaintEvent__mutmut_2': xǁNumpadButtonǁpaintEvent__mutmut_2, 
        'xǁNumpadButtonǁpaintEvent__mutmut_3': xǁNumpadButtonǁpaintEvent__mutmut_3, 
        'xǁNumpadButtonǁpaintEvent__mutmut_4': xǁNumpadButtonǁpaintEvent__mutmut_4, 
        'xǁNumpadButtonǁpaintEvent__mutmut_5': xǁNumpadButtonǁpaintEvent__mutmut_5, 
        'xǁNumpadButtonǁpaintEvent__mutmut_6': xǁNumpadButtonǁpaintEvent__mutmut_6, 
        'xǁNumpadButtonǁpaintEvent__mutmut_7': xǁNumpadButtonǁpaintEvent__mutmut_7, 
        'xǁNumpadButtonǁpaintEvent__mutmut_8': xǁNumpadButtonǁpaintEvent__mutmut_8, 
        'xǁNumpadButtonǁpaintEvent__mutmut_9': xǁNumpadButtonǁpaintEvent__mutmut_9, 
        'xǁNumpadButtonǁpaintEvent__mutmut_10': xǁNumpadButtonǁpaintEvent__mutmut_10, 
        'xǁNumpadButtonǁpaintEvent__mutmut_11': xǁNumpadButtonǁpaintEvent__mutmut_11, 
        'xǁNumpadButtonǁpaintEvent__mutmut_12': xǁNumpadButtonǁpaintEvent__mutmut_12, 
        'xǁNumpadButtonǁpaintEvent__mutmut_13': xǁNumpadButtonǁpaintEvent__mutmut_13, 
        'xǁNumpadButtonǁpaintEvent__mutmut_14': xǁNumpadButtonǁpaintEvent__mutmut_14, 
        'xǁNumpadButtonǁpaintEvent__mutmut_15': xǁNumpadButtonǁpaintEvent__mutmut_15, 
        'xǁNumpadButtonǁpaintEvent__mutmut_16': xǁNumpadButtonǁpaintEvent__mutmut_16, 
        'xǁNumpadButtonǁpaintEvent__mutmut_17': xǁNumpadButtonǁpaintEvent__mutmut_17, 
        'xǁNumpadButtonǁpaintEvent__mutmut_18': xǁNumpadButtonǁpaintEvent__mutmut_18, 
        'xǁNumpadButtonǁpaintEvent__mutmut_19': xǁNumpadButtonǁpaintEvent__mutmut_19, 
        'xǁNumpadButtonǁpaintEvent__mutmut_20': xǁNumpadButtonǁpaintEvent__mutmut_20, 
        'xǁNumpadButtonǁpaintEvent__mutmut_21': xǁNumpadButtonǁpaintEvent__mutmut_21, 
        'xǁNumpadButtonǁpaintEvent__mutmut_22': xǁNumpadButtonǁpaintEvent__mutmut_22, 
        'xǁNumpadButtonǁpaintEvent__mutmut_23': xǁNumpadButtonǁpaintEvent__mutmut_23, 
        'xǁNumpadButtonǁpaintEvent__mutmut_24': xǁNumpadButtonǁpaintEvent__mutmut_24, 
# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNumpadButtonǁsetProperty__mutmut_orig'), object.__getattribute__(self, 'xǁNumpadButtonǁsetProperty__mutmut_mutants'), args, kwargs, self)

    def xǁNumpadButtonǁsetProperty__mutmut_orig(self, name: str, value: typing.Any):
        """Re-implemented method, set widget properties"""
        if name == "position":
            self.set_position(value)

        return super().setProperty(name, value)

    def xǁNumpadButtonǁsetProperty__mutmut_1(self, name: str, value: typing.Any):
        """Re-implemented method, set widget properties"""
        if name != "position":
            self.set_position(value)

        return super().setProperty(name, value)

    def xǁNumpadButtonǁsetProperty__mutmut_2(self, name: str, value: typing.Any):
        """Re-implemented method, set widget properties"""
        if name == "XXpositionXX":
            self.set_position(value)

        return super().setProperty(name, value)

    def xǁNumpadButtonǁsetProperty__mutmut_3(self, name: str, value: typing.Any):
        """Re-implemented method, set widget properties"""
        if name == "POSITION":
            self.set_position(value)

        return super().setProperty(name, value)

    def xǁNumpadButtonǁsetProperty__mutmut_4(self, name: str, value: typing.Any):
        """Re-implemented method, set widget properties"""
        if name == "position":
            self.set_position(None)

        return super().setProperty(name, value)

    def xǁNumpadButtonǁsetProperty__mutmut_5(self, name: str, value: typing.Any):
        """Re-implemented method, set widget properties"""
        if name == "position":
            self.set_position(value)

        return super().setProperty(None, value)

    def xǁNumpadButtonǁsetProperty__mutmut_6(self, name: str, value: typing.Any):
        """Re-implemented method, set widget properties"""
        if name == "position":
            self.set_position(value)

        return super().setProperty(name, None)

    def xǁNumpadButtonǁsetProperty__mutmut_7(self, name: str, value: typing.Any):
        """Re-implemented method, set widget properties"""
        if name == "position":
            self.set_position(value)

        return super().setProperty(value)

    def xǁNumpadButtonǁsetProperty__mutmut_8(self, name: str, value: typing.Any):
        """Re-implemented method, set widget properties"""
        if name == "position":
            self.set_position(value)

        return super().setProperty(name, )
    
    xǁNumpadButtonǁsetProperty__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNumpadButtonǁsetProperty__mutmut_1': xǁNumpadButtonǁsetProperty__mutmut_1, 
        'xǁNumpadButtonǁsetProperty__mutmut_2': xǁNumpadButtonǁsetProperty__mutmut_2, 
        'xǁNumpadButtonǁsetProperty__mutmut_3': xǁNumpadButtonǁsetProperty__mutmut_3, 
        'xǁNumpadButtonǁsetProperty__mutmut_4': xǁNumpadButtonǁsetProperty__mutmut_4, 
        'xǁNumpadButtonǁsetProperty__mutmut_5': xǁNumpadButtonǁsetProperty__mutmut_5, 
        'xǁNumpadButtonǁsetProperty__mutmut_6': xǁNumpadButtonǁsetProperty__mutmut_6, 
        'xǁNumpadButtonǁsetProperty__mutmut_7': xǁNumpadButtonǁsetProperty__mutmut_7, 
        'xǁNumpadButtonǁsetProperty__mutmut_8': xǁNumpadButtonǁsetProperty__mutmut_8
    }
    xǁNumpadButtonǁsetProperty__mutmut_orig.__name__ = 'xǁNumpadButtonǁsetProperty'
