from PyQt6 import QtCore, QtGui, QtWidgets
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


class ListCustomButton(QtWidgets.QPushButton):
    def __init__(self, parent=None) -> None:
        args = [parent]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁListCustomButtonǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁListCustomButtonǁ__init____mutmut_mutants'), args, kwargs, self)
    def xǁListCustomButtonǁ__init____mutmut_orig(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_1(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(None)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_2(self, parent=None) -> None:
        if parent:
            super(None, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_3(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, None).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_4(self, parent=None) -> None:
        if parent:
            super(self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_5(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, ).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_6(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(None, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_7(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, None).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_8(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_9(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, ).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_10(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = None
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_11(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = None
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_12(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = None
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_13(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(None, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_14(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, None, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_15(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, None)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_16(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_17(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_18(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, )
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_19(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(256, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_20(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 256, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_21(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 256)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_22(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = None
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_23(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "XX#1A8FBFXX"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_24(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1a8fbf"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_25(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = None
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_26(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = "XXXX"
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_27(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = None

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_28(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = "XXXX"

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_29(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = None
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_30(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 16
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_31(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = None

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_32(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 12

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_33(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = None
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_34(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = True
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_35(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = None

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_36(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = True

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_37(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(None, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_38(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, None)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_39(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_40(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, )
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_41(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, False)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_42(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            None,
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_43(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            None,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_44(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Fixed,
        )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_45(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.text_color: QtGui.QColor = QtGui.QColor(255, 255, 255)
        self.pressed_color = "#1A8FBF"
        self._text = ""
        self._right_text = ""

        self._rfontsize = 15
        self._lfontsize = 11

        self._is_pressed = False
        self._is_hovered = False

        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
        self.setSizePolicy(
            QtWidgets.QSizePolicy.Policy.Expanding,
            )

        self.setFocusPolicy(QtCore.Qt.FocusPolicy.NoFocus)
    def xǁListCustomButtonǁ__init____mutmut_46(self, parent=None) -> None:
        if parent:
            super(ListCustomButton, self).__init__(parent)
        else:
            super(ListCustomButton, self).__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self.second_icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        
        'xǁListCustomButtonǁ__init____mutmut_29': xǁListCustomButtonǁ__init____mutmut_29, 
        'xǁListCustomButtonǁ__init____mutmut_30': xǁListCustomButtonǁ__init____mutmut_30, 
        'xǁListCustomButtonǁ__init____mutmut_31': xǁListCustomButtonǁ__init____mutmut_31, 
        'xǁListCustomButtonǁ__init____mutmut_32': xǁListCustomButtonǁ__init____mutmut_32, 
        'xǁListCustomButtonǁ__init____mutmut_33': xǁListCustomButtonǁ__init____mutmut_33, 
        'xǁListCustomButtonǁ__init____mutmut_34': xǁListCustomButtonǁ__init____mutmut_34, 
        'xǁListCustomButtonǁ__init____mutmut_35': xǁListCustomButtonǁ__init____mutmut_35, 
        'xǁListCustomButtonǁ__init____mutmut_36': xǁListCustomButtonǁ__init____mutmut_36, 
        'xǁListCustomButtonǁ__init____mutmut_37': xǁListCustomButtonǁ__init____mutmut_37, 
        'xǁListCustomButtonǁ__init____mutmut_38': xǁListCustomButtonǁ__init____mutmut_38, 
        'xǁListCustomButtonǁ__init____mutmut_39': xǁListCustomButtonǁ__init____mutmut_39, 
        'xǁListCustomButtonǁ__init____mutmut_40': xǁListCustomButtonǁ__init____mutmut_40, 
        'xǁListCustomButtonǁ__init____mutmut_41': xǁListCustomButtonǁ__init____mutmut_41, 
        'xǁListCustomButtonǁ__init____mutmut_42': xǁListCustomButtonǁ__init____mutmut_42, 
        'xǁListCustomButtonǁ__init____mutmut_43': xǁListCustomButtonǁ__init____mutmut_43, 
        'xǁListCustomButtonǁ__init____mutmut_44': xǁListCustomButtonǁ__init____mutmut_44, 
        'xǁListCustomButtonǁ__init____mutmut_45': xǁListCustomButtonǁ__init____mutmut_45, 
        'xǁListCustomButtonǁ__init____mutmut_46': xǁListCustomButtonǁ__init____mutmut_46
    }
    xǁListCustomButtonǁ__init____mutmut_orig.__name__ = 'xǁListCustomButtonǁ__init__'

    def setText(self, text: str) -> None:
        args = [text]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁListCustomButtonǁsetText__mutmut_orig'), object.__getattribute__(self, 'xǁListCustomButtonǁsetText__mutmut_mutants'), args, kwargs, self)

    def xǁListCustomButtonǁsetText__mutmut_orig(self, text: str) -> None:
        """Set widget text"""
        self._text = text
        self.update()

    def xǁListCustomButtonǁsetText__mutmut_1(self, text: str) -> None:
        """Set widget text"""
        self._text = None
        self.update()
    
    xǁListCustomButtonǁsetText__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁListCustomButtonǁsetText__mutmut_1': xǁListCustomButtonǁsetText__mutmut_1
    }
    xǁListCustomButtonǁsetText__mutmut_orig.__name__ = 'xǁListCustomButtonǁsetText'

    def text(self) -> str:
        """Widget text"""
        return self._text

    def setRightText(self, text: str) -> None:
        args = [text]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁListCustomButtonǁsetRightText__mutmut_orig'), object.__getattribute__(self, 'xǁListCustomButtonǁsetRightText__mutmut_mutants'), args, kwargs, self)

    def xǁListCustomButtonǁsetRightText__mutmut_orig(self, text: str) -> None:
        """Set widget right text"""
        self._right_text = text
        self.update()

    def xǁListCustomButtonǁsetRightText__mutmut_1(self, text: str) -> None:
        """Set widget right text"""
        self._right_text = None
        self.update()
    
    xǁListCustomButtonǁsetRightText__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁListCustomButtonǁsetRightText__mutmut_1': xǁListCustomButtonǁsetRightText__mutmut_1
    }
    xǁListCustomButtonǁsetRightText__mutmut_orig.__name__ = 'xǁListCustomButtonǁsetRightText'

    def rightText(self) -> str:
        """Widget right text"""
        return self._right_text

    def setLeftFontSize(self, size: int) -> None:
        args = [size]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁListCustomButtonǁsetLeftFontSize__mutmut_orig'), object.__getattribute__(self, 'xǁListCustomButtonǁsetLeftFontSize__mutmut_mutants'), args, kwargs, self)

    def xǁListCustomButtonǁsetLeftFontSize__mutmut_orig(self, size: int) -> None:
        """Set widget left text font size"""
        self._lfontsize = size
        self.update()

    def xǁListCustomButtonǁsetLeftFontSize__mutmut_1(self, size: int) -> None:
        """Set widget left text font size"""
        self._lfontsize = None
        self.update()
    
    xǁListCustomButtonǁsetLeftFontSize__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁListCustomButtonǁsetLeftFontSize__mutmut_1': xǁListCustomButtonǁsetLeftFontSize__mutmut_1
    }
    xǁListCustomButtonǁsetLeftFontSize__mutmut_orig.__name__ = 'xǁListCustomButtonǁsetLeftFontSize'

    def setRightFontSize(self, size: int) -> None:
        args = [size]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁListCustomButtonǁsetRightFontSize__mutmut_orig'), object.__getattribute__(self, 'xǁListCustomButtonǁsetRightFontSize__mutmut_mutants'), args, kwargs, self)

    def xǁListCustomButtonǁsetRightFontSize__mutmut_orig(self, size: int) -> None:
        """Set widget right text font size"""
        self._rfontsize = size
        self.update()

    def xǁListCustomButtonǁsetRightFontSize__mutmut_1(self, size: int) -> None:
        """Set widget right text font size"""
        self._rfontsize = None
        self.update()
    
    xǁListCustomButtonǁsetRightFontSize__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁListCustomButtonǁsetRightFontSize__mutmut_1': xǁListCustomButtonǁsetRightFontSize__mutmut_1
    }
    xǁListCustomButtonǁsetRightFontSize__mutmut_orig.__name__ = 'xǁListCustomButtonǁsetRightFontSize'

    def setPixmap(self, pixmap: QtGui.QPixmap) -> None:
        args = [pixmap]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁListCustomButtonǁsetPixmap__mutmut_orig'), object.__getattribute__(self, 'xǁListCustomButtonǁsetPixmap__mutmut_mutants'), args, kwargs, self)

    def xǁListCustomButtonǁsetPixmap__mutmut_orig(self, pixmap: QtGui.QPixmap) -> None:
        """Set widget pixmap"""
        self.icon_pixmap = pixmap
        self.update()

    def xǁListCustomButtonǁsetPixmap__mutmut_1(self, pixmap: QtGui.QPixmap) -> None:
        """Set widget pixmap"""
        self.icon_pixmap = None
        self.update()
    
    xǁListCustomButtonǁsetPixmap__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁListCustomButtonǁsetPixmap__mutmut_1': xǁListCustomButtonǁsetPixmap__mutmut_1
    }
    xǁListCustomButtonǁsetPixmap__mutmut_orig.__name__ = 'xǁListCustomButtonǁsetPixmap'

    def setSecondPixmap(self, pixmap: QtGui.QPixmap) -> None:
        args = [pixmap]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁListCustomButtonǁsetSecondPixmap__mutmut_orig'), object.__getattribute__(self, 'xǁListCustomButtonǁsetSecondPixmap__mutmut_mutants'), args, kwargs, self)

    def xǁListCustomButtonǁsetSecondPixmap__mutmut_orig(self, pixmap: QtGui.QPixmap) -> None:
        """Set widget secondary pixmap"""
        self.second_icon_pixmap = pixmap
        self.update()

    def xǁListCustomButtonǁsetSecondPixmap__mutmut_1(self, pixmap: QtGui.QPixmap) -> None:
        """Set widget secondary pixmap"""
        self.second_icon_pixmap = None
        self.update()
    
    xǁListCustomButtonǁsetSecondPixmap__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁListCustomButtonǁsetSecondPixmap__mutmut_1': xǁListCustomButtonǁsetSecondPixmap__mutmut_1
    }
    xǁListCustomButtonǁsetSecondPixmap__mutmut_orig.__name__ = 'xǁListCustomButtonǁsetSecondPixmap'

    def mousePressEvent(self, event: QtGui.QMouseEvent) -> None:
        args = [event]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁListCustomButtonǁmousePressEvent__mutmut_orig'), object.__getattribute__(self, 'xǁListCustomButtonǁmousePressEvent__mutmut_mutants'), args, kwargs, self)

    def xǁListCustomButtonǁmousePressEvent__mutmut_orig(self, event: QtGui.QMouseEvent) -> None:
        """Re-implemented method, handle mouse press event"""
        self._is_pressed = True
        self.update()
        super().mousePressEvent(event)

    def xǁListCustomButtonǁmousePressEvent__mutmut_1(self, event: QtGui.QMouseEvent) -> None:
        """Re-implemented method, handle mouse press event"""
        self._is_pressed = None
        self.update()
        super().mousePressEvent(event)

    def xǁListCustomButtonǁmousePressEvent__mutmut_2(self, event: QtGui.QMouseEvent) -> None:
        """Re-implemented method, handle mouse press event"""
        self._is_pressed = False
        self.update()
        super().mousePressEvent(event)

    def xǁListCustomButtonǁmousePressEvent__mutmut_3(self, event: QtGui.QMouseEvent) -> None:
        """Re-implemented method, handle mouse press event"""
        self._is_pressed = True
        self.update()
        super().mousePressEvent(None)
    
    xǁListCustomButtonǁmousePressEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁListCustomButtonǁmousePressEvent__mutmut_1': xǁListCustomButtonǁmousePressEvent__mutmut_1, 
        'xǁListCustomButtonǁmousePressEvent__mutmut_2': xǁListCustomButtonǁmousePressEvent__mutmut_2, 
        'xǁListCustomButtonǁmousePressEvent__mutmut_3': xǁListCustomButtonǁmousePressEvent__mutmut_3
    }
    xǁListCustomButtonǁmousePressEvent__mutmut_orig.__name__ = 'xǁListCustomButtonǁmousePressEvent'

    def mouseReleaseEvent(self, event: QtGui.QMouseEvent) -> None:
        args = [event]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁListCustomButtonǁmouseReleaseEvent__mutmut_orig'), object.__getattribute__(self, 'xǁListCustomButtonǁmouseReleaseEvent__mutmut_mutants'), args, kwargs, self)

    def xǁListCustomButtonǁmouseReleaseEvent__mutmut_orig(self, event: QtGui.QMouseEvent) -> None:
        """Re-implemented method, handle mouse release event"""
        self._is_pressed = False
        self.update()
        super().mouseReleaseEvent(event)

    def xǁListCustomButtonǁmouseReleaseEvent__mutmut_1(self, event: QtGui.QMouseEvent) -> None:
        """Re-implemented method, handle mouse release event"""
        self._is_pressed = None
        self.update()
        super().mouseReleaseEvent(event)

    def xǁListCustomButtonǁmouseReleaseEvent__mutmut_2(self, event: QtGui.QMouseEvent) -> None:
        """Re-implemented method, handle mouse release event"""
        self._is_pressed = True
        self.update()
        super().mouseReleaseEvent(event)

    def xǁListCustomButtonǁmouseReleaseEvent__mutmut_3(self, event: QtGui.QMouseEvent) -> None:
        """Re-implemented method, handle mouse release event"""
        self._is_pressed = False
        self.update()
        super().mouseReleaseEvent(None)
    
    xǁListCustomButtonǁmouseReleaseEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁListCustomButtonǁmouseReleaseEvent__mutmut_1': xǁListCustomButtonǁmouseReleaseEvent__mutmut_1, 
        'xǁListCustomButtonǁmouseReleaseEvent__mutmut_2': xǁListCustomButtonǁmouseReleaseEvent__mutmut_2, 
        'xǁListCustomButtonǁmouseReleaseEvent__mutmut_3': xǁListCustomButtonǁmouseReleaseEvent__mutmut_3
    }
    xǁListCustomButtonǁmouseReleaseEvent__mutmut_orig.__name__ = 'xǁListCustomButtonǁmouseReleaseEvent'

    def leaveEvent(self, event: QtCore.QEvent) -> None:
        args = [event]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁListCustomButtonǁleaveEvent__mutmut_orig'), object.__getattribute__(self, 'xǁListCustomButtonǁleaveEvent__mutmut_mutants'), args, kwargs, self)

    def xǁListCustomButtonǁleaveEvent__mutmut_orig(self, event: QtCore.QEvent) -> None:
        """Re-implemented method, handle leave event"""
        self._is_hovered = False
        self.update()
        super().leaveEvent(event)

    def xǁListCustomButtonǁleaveEvent__mutmut_1(self, event: QtCore.QEvent) -> None:
        """Re-implemented method, handle leave event"""
        self._is_hovered = None
        self.update()
        super().leaveEvent(event)

    def xǁListCustomButtonǁleaveEvent__mutmut_2(self, event: QtCore.QEvent) -> None:
        """Re-implemented method, handle leave event"""
        self._is_hovered = True
        self.update()
        super().leaveEvent(event)

    def xǁListCustomButtonǁleaveEvent__mutmut_3(self, event: QtCore.QEvent) -> None:
        """Re-implemented method, handle leave event"""
        self._is_hovered = False
        self.update()
        super().leaveEvent(None)
    
    xǁListCustomButtonǁleaveEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁListCustomButtonǁleaveEvent__mutmut_1': xǁListCustomButtonǁleaveEvent__mutmut_1, 
        'xǁListCustomButtonǁleaveEvent__mutmut_2': xǁListCustomButtonǁleaveEvent__mutmut_2, 
        'xǁListCustomButtonǁleaveEvent__mutmut_3': xǁListCustomButtonǁleaveEvent__mutmut_3
    }
    xǁListCustomButtonǁleaveEvent__mutmut_orig.__name__ = 'xǁListCustomButtonǁleaveEvent'

    def paintEvent(self, e: QtGui.QPaintEvent | None) -> None:
        args = [e]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁListCustomButtonǁpaintEvent__mutmut_orig'), object.__getattribute__(self, 'xǁListCustomButtonǁpaintEvent__mutmut_mutants'), args, kwargs, self)

    def xǁListCustomButtonǁpaintEvent__mutmut_orig(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_1(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = None
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_2(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(None)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_3(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(None, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_4(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, None)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_5(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_6(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, )
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_7(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, False)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_8(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(None, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_9(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, None)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_10(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_11(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, )

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_12(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, False)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_13(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = None
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_14(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = None

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_15(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() * 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_16(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 6.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_17(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = None
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_18(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(None, radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_19(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), None, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_20(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, None)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_21(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_22(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_23(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, )

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_24(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(None), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_25(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = None
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_26(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() / 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_27(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 1.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_28(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = None
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_29(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() / 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_30(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 1.9
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_31(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = None
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_32(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            None,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_33(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            None,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_34(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            None,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_35(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            None,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_36(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_37(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_38(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_39(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_40(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin + ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_41(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() + ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_42(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() - ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_43(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = None
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_44(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(None)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_45(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = None

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_46(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = None
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_47(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() / 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_48(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 1.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_49(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = None
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_50(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() / 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_51(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 1.5
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_52(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = None
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_53(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            None,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_54(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            None,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_55(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            None,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_56(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            None,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_57(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_58(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_59(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_60(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_61(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() - left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_62(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() - left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_63(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = None  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_64(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 11  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_65(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_66(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = None
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_67(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(None)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_68(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(None)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_69(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(21)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_70(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(None)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_71(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(None)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_72(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(None, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_73(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, None)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_74(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_75(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, )
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_76(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = None
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_77(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(None)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_78(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(None)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_79(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(91)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_80(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(None)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_81(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(None)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_82(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(None, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_83(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, None)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_84(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_85(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, )

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_86(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_87(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = None
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_88(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size / 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_89(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 1.1
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_90(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = None
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_91(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                None,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_92(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                None,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_93(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                None,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_94(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                None,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_95(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_96(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_97(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_98(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_99(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() - icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_100(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin * 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_101(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 3,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_102(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() - icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_103(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin * 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_104(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 3,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_105(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() + icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_106(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() + icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_107(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = None
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_108(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                None,
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_109(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                None,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_110(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                None,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_111(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_112(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_113(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_114(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = None
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_115(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() - (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_116(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) * 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_117(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() + icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_118(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 3.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_119(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = None
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_120(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() - (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_121(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) * 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_122(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() + icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_123(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 3.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_124(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = None
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_125(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                None,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_126(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                None,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_127(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                None,
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_128(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                None,
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_129(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_130(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_131(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_132(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_133(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                None, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_134(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, None, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_135(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, None
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_136(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_137(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_138(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_139(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_140(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = None
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_141(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                None,
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_142(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                None,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_143(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                None,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_144(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_145(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_146(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_147(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = None
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_148(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x() - (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_149(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) / 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_150(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() + left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_151(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 3.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_152(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = None
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_153(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) / 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_154(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                -(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_155(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() + left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_156(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 3.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_157(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = None
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_158(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                None,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_159(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                None,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_160(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                None,
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_161(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                None,
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_162(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_163(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_164(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_165(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_166(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                None,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_167(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                None,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_168(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                None,
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_169(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_170(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_171(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_172(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = None  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_173(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size - 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_174(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin - left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_175(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 9  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_176(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = None
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_177(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            None
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_178(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin + rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_179(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size + ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_180(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() + ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_181(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() / 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_182(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 1.1
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_183(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = None

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_184(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            None,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_185(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            None,
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_186(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            None,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_187(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            None,
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_188(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_189(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_190(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_191(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_192(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() - left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_193(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() + left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_194(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin + rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_195(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(None)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_196(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = None
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_197(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(None)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_198(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(None)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_199(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = None
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_200(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(None)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_201(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = None

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_202(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = None

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_203(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 + metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_204(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() - (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_205(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) * 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_206(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() - main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_207(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 3 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_208(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = None  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_209(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 11  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_210(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = None
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_211(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(None)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_212(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(None)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_213(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = None
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_214(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(None)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_215(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = None

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_216(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(None)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_217(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = None

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_218(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap + right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_219(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() + gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_220(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = None  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_221(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() + 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_222(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x + text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_223(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 11
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_224(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = None

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_225(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            None,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_226(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            None,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_227(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            None,
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_228(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_229(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_230(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_231(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(None),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_232(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(None)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_233(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            None,
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_234(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            None,
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_235(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            None,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_236(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_237(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_238(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_239(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(None),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_240(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(None),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_241(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(None)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_242(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(None)  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_243(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(None, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_244(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, None, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_245(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, None))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_246(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_247(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_248(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, ))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_249(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(161, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_250(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 161, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_251(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 161))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_252(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = None
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_253(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = None
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_254(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2 + right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_255(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top() - (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_256(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) * 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_257(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() - right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_258(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 3
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_259(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                None,
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_260(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                None,
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_261(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                None,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_262(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_263(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_264(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(right_text_y),
                )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_265(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(None),
                int(right_text_y),
                self._right_text,
            )

        painter.end()

    def xǁListCustomButtonǁpaintEvent__mutmut_266(self, e: QtGui.QPaintEvent | None) -> None:
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        rect = self.rect()
        radius = rect.height() / 5.0

        # Main rounded rectangle path (using the adjusted rect)
        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), radius, radius)

        # Ellipse ("hole") for the icon on the right
        ellipse_margin = rect.height() * 0.05
        ellipse_size = rect.height() * 0.90
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )
        ellipse_path = QtGui.QPainterPath()
        ellipse_path.addEllipse(ellipse_rect)
        self.button_ellipse = ellipse_rect

        # Ellipse ("hole") for the icon on the left (only if present)
        left_icon_margin = rect.height() * 0.05
        left_icon_size = rect.height() * 0.50
        left_icon_rect = QtCore.QRectF(
            rect.left() + left_icon_margin,
            rect.top() + left_icon_margin,
            left_icon_size,
            left_icon_size,
        )
        left_margin = 10  # default left margin

        # Gradient background (left to right)
        if not self._is_pressed:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(20)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)
        else:
            pressed_color = QtGui.QColor(self.pressed_color)
            pressed_color.setAlpha(90)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            painter.setBrush(pressed_color)
            painter.fillPath(path, pressed_color)

        # Draw icon inside the ellipse "hole" (on the right)
        if not self.icon_pixmap.isNull():
            icon_margin = ellipse_size * 0.10
            icon_rect = QtCore.QRectF(
                ellipse_rect.left() + icon_margin / 2,
                ellipse_rect.top() + icon_margin / 2,
                ellipse_rect.width() - icon_margin,
                ellipse_rect.height() - icon_margin,
            )
            icon_scaled = self.icon_pixmap.scaled(
                icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the ellipse
            adjusted_x = icon_rect.x() + (icon_rect.width() - icon_scaled.width()) / 2.0
            adjusted_y = (
                icon_rect.y() + (icon_rect.height() - icon_scaled.height()) / 2.0
            )
            adjusted_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                icon_scaled.width(),
                icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_icon_rect, icon_scaled, icon_scaled.rect().toRectF()
            )

        # Draw second icon (on the left, if present)
        if not self.second_icon_pixmap.isNull():
            left_icon_scaled = self.second_icon_pixmap.scaled(
                left_icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            # Center the icon in the rect
            adjusted_x = (
                left_icon_rect.x()
                + (left_icon_rect.width() - left_icon_scaled.width()) // 2.0
            )
            adjusted_y = (
                # left_icon_rect.y()
                # + (left_icon_rect.height() - left_icon_scaled.height()) // 2.0
                +(self.height() - left_icon_rect.height()) // 2.0
            )
            adjusted_left_icon_rect = QtCore.QRectF(
                adjusted_x,
                adjusted_y,
                left_icon_scaled.width(),
                left_icon_scaled.height(),
            )
            painter.drawPixmap(
                adjusted_left_icon_rect,
                left_icon_scaled,
                left_icon_scaled.rect().toRectF(),
            )
            left_margin = left_icon_margin + left_icon_size + 8  # 8px gap after icon

        # Draw text, area before the ellipse (adjusted for left icon)
        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )
        text_rect = QtCore.QRectF(
            rect.left() + left_margin,
            rect.top(),
            text_margin - rect.left() - left_margin,
            rect.height(),
        )

        # Draw main text (left-aligned)
        painter.setPen(self.text_color)
        _font = painter.font()
        _font.setPointSize(self._lfontsize)
        painter.setFont(_font)
        metrics = QtGui.QFontMetrics(_font)
        main_text_height = metrics.height()

        # Vertically center text
        text_y = rect.top() + (rect.height() + main_text_height) / 2 - metrics.descent()

        # Calculate where to start the right text: just left of the right icon ellipse
        gap = 10  # gap between right text and icon ellipse
        right_font = QtGui.QFont(_font)
        right_font.setPointSize(self._rfontsize)
        right_metrics = QtGui.QFontMetrics(right_font)
        right_text_width = right_metrics.horizontalAdvance(self._right_text)

        # The right text should end at ellipse_rect.left() - gap
        right_text_x = ellipse_rect.left() - gap - right_text_width

        # Draw main text (left-aligned, but don't overlap right text)
        max_main_text_width = (
            right_text_x - text_rect.left() - 10
        )  # 10px gap between main and right text
        elided_main_text = metrics.elidedText(
            self._text,
            QtCore.Qt.TextElideMode.ElideRight,
            int(max_main_text_width),
        )

        painter.setFont(_font)
        painter.drawText(
            int(text_rect.left()),
            int(text_y),
            elided_main_text,
        )

        # Draw right text (smaller, grey, just left of the icon)
        if self._right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))  # grey color
            right_text_height = right_metrics.height()
            right_text_y = (
                rect.top()
                + (rect.height() + right_text_height) / 2
                - right_metrics.descent()
            )
            painter.drawText(
                int(right_text_x),
                int(None),
                self._right_text,
            )

        painter.end()
    
    xǁListCustomButtonǁpaintEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁListCustomButtonǁpaintEvent__mutmut_1': xǁListCustomButtonǁpaintEvent__mutmut_1, 
        'xǁListCustomButtonǁpaintEvent__mutmut_2': xǁListCustomButtonǁpaintEvent__mutmut_2, 
        'xǁListCustomButtonǁpaintEvent__mutmut_3': xǁListCustomButtonǁpaintEvent__mutmut_3, 
        'xǁListCustomButtonǁpaintEvent__mutmut_4': xǁListCustomButtonǁpaintEvent__mutmut_4, 
        'xǁListCustomButtonǁpaintEvent__mutmut_5': xǁListCustomButtonǁpaintEvent__mutmut_5, 
        'xǁListCustomButtonǁpaintEvent__mutmut_6': xǁListCustomButtonǁpaintEvent__mutmut_6, 
        'xǁListCustomButtonǁpaintEvent__mutmut_7': xǁListCustomButtonǁpaintEvent__mutmut_7, 
        'xǁListCustomButtonǁpaintEvent__mutmut_8': xǁListCustomButtonǁpaintEvent__mutmut_8, 
        'xǁListCustomButtonǁpaintEvent__mutmut_9': xǁListCustomButtonǁpaintEvent__mutmut_9, 
        'xǁListCustomButtonǁpaintEvent__mutmut_10': xǁListCustomButtonǁpaintEvent__mutmut_10, 
        'xǁListCustomButtonǁpaintEvent__mutmut_11': xǁListCustomButtonǁpaintEvent__mutmut_11, 
        'xǁListCustomButtonǁpaintEvent__mutmut_12': xǁListCustomButtonǁpaintEvent__mutmut_12, 
        'xǁListCustomButtonǁpaintEvent__mutmut_13': xǁListCustomButtonǁpaintEvent__mutmut_13, 
        'xǁListCustomButtonǁpaintEvent__mutmut_14': xǁListCustomButtonǁpaintEvent__mutmut_14, 
        'xǁListCustomButtonǁpaintEvent__mutmut_15': xǁListCustomButtonǁpaintEvent__mutmut_15, 
