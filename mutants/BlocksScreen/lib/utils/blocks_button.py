import typing
import enum
from PyQt6 import QtCore, QtGui, QtWidgets
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


class ButtonColors(enum.Enum):
    """Standard button colors"""

    NORMAL_BG = (223, 223, 223)
    PRESSED_BG = (169, 169, 169)
    DISABLED_BG = (169, 169, 169)
    TEXT_COLOR = (0, 0, 0)
    DISABLED_TEXT_COLOR = (102, 102, 102)
    NOTIFICATION_DOT = (226, 31, 31)


class BlocksCustomButton(QtWidgets.QAbstractButton):
    def __init__(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        args = [parent]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksCustomButtonǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁBlocksCustomButtonǁ__init____mutmut_mutants'), args, kwargs, self)
    def xǁBlocksCustomButtonǁ__init____mutmut_orig(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_1(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(None)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_2(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = None
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_3(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = None
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_4(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = None
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_5(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = True
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_6(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = ""
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_7(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = ""
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_8(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = None
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_9(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = "XXXX"
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_10(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = None
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_11(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = "XXXX"
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_12(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = None
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_13(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = None
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_14(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = True
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_15(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(None, True)
    def xǁBlocksCustomButtonǁ__init____mutmut_16(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, None)
    def xǁBlocksCustomButtonǁ__init____mutmut_17(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(True)
    def xǁBlocksCustomButtonǁ__init____mutmut_18(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, )
    def xǁBlocksCustomButtonǁ__init____mutmut_19(
        self,
        parent: QtWidgets.QWidget | None = None,
    ) -> None:
        if parent:
            super().__init__(parent)
        else:
            super().__init__()
        self.icon_pixmap: QtGui.QPixmap = QtGui.QPixmap()
        self._icon_rect: QtCore.QRectF = QtCore.QRectF()
        self._is_flat: bool = False
        self.button_background = None
        self.button_ellipse = None
        self._text: str = ""
        self._name: str = ""
        self.text_color: QtGui.QColor = QtGui.QColor(*ButtonColors.TEXT_COLOR.value)
        self._show_notification: bool = False
        self.setAttribute(QtCore.Qt.WidgetAttribute.WA_AcceptTouchEvents, False)
    
    xǁBlocksCustomButtonǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksCustomButtonǁ__init____mutmut_1': xǁBlocksCustomButtonǁ__init____mutmut_1, 
        'xǁBlocksCustomButtonǁ__init____mutmut_2': xǁBlocksCustomButtonǁ__init____mutmut_2, 
        'xǁBlocksCustomButtonǁ__init____mutmut_3': xǁBlocksCustomButtonǁ__init____mutmut_3, 
        'xǁBlocksCustomButtonǁ__init____mutmut_4': xǁBlocksCustomButtonǁ__init____mutmut_4, 
        'xǁBlocksCustomButtonǁ__init____mutmut_5': xǁBlocksCustomButtonǁ__init____mutmut_5, 
        'xǁBlocksCustomButtonǁ__init____mutmut_6': xǁBlocksCustomButtonǁ__init____mutmut_6, 
        'xǁBlocksCustomButtonǁ__init____mutmut_7': xǁBlocksCustomButtonǁ__init____mutmut_7, 
        'xǁBlocksCustomButtonǁ__init____mutmut_8': xǁBlocksCustomButtonǁ__init____mutmut_8, 
        'xǁBlocksCustomButtonǁ__init____mutmut_9': xǁBlocksCustomButtonǁ__init____mutmut_9, 
        'xǁBlocksCustomButtonǁ__init____mutmut_10': xǁBlocksCustomButtonǁ__init____mutmut_10, 
        'xǁBlocksCustomButtonǁ__init____mutmut_11': xǁBlocksCustomButtonǁ__init____mutmut_11, 
        'xǁBlocksCustomButtonǁ__init____mutmut_12': xǁBlocksCustomButtonǁ__init____mutmut_12, 
        'xǁBlocksCustomButtonǁ__init____mutmut_13': xǁBlocksCustomButtonǁ__init____mutmut_13, 
        'xǁBlocksCustomButtonǁ__init____mutmut_14': xǁBlocksCustomButtonǁ__init____mutmut_14, 
        'xǁBlocksCustomButtonǁ__init____mutmut_15': xǁBlocksCustomButtonǁ__init____mutmut_15, 
        'xǁBlocksCustomButtonǁ__init____mutmut_16': xǁBlocksCustomButtonǁ__init____mutmut_16, 
        'xǁBlocksCustomButtonǁ__init____mutmut_17': xǁBlocksCustomButtonǁ__init____mutmut_17, 
        'xǁBlocksCustomButtonǁ__init____mutmut_18': xǁBlocksCustomButtonǁ__init____mutmut_18, 
        'xǁBlocksCustomButtonǁ__init____mutmut_19': xǁBlocksCustomButtonǁ__init____mutmut_19
    }
    xǁBlocksCustomButtonǁ__init____mutmut_orig.__name__ = 'xǁBlocksCustomButtonǁ__init__'

    def setShowNotification(self, show: bool) -> None:
        args = [show]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksCustomButtonǁsetShowNotification__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksCustomButtonǁsetShowNotification__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksCustomButtonǁsetShowNotification__mutmut_orig(self, show: bool) -> None:
        """Set notification on button"""
        if self._show_notification != show:
            self._show_notification = show
            self.update()

    def xǁBlocksCustomButtonǁsetShowNotification__mutmut_1(self, show: bool) -> None:
        """Set notification on button"""
        if self._show_notification == show:
            self._show_notification = show
            self.update()

    def xǁBlocksCustomButtonǁsetShowNotification__mutmut_2(self, show: bool) -> None:
        """Set notification on button"""
        if self._show_notification != show:
            self._show_notification = None
            self.update()
    
    xǁBlocksCustomButtonǁsetShowNotification__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksCustomButtonǁsetShowNotification__mutmut_1': xǁBlocksCustomButtonǁsetShowNotification__mutmut_1, 
        'xǁBlocksCustomButtonǁsetShowNotification__mutmut_2': xǁBlocksCustomButtonǁsetShowNotification__mutmut_2
    }
    xǁBlocksCustomButtonǁsetShowNotification__mutmut_orig.__name__ = 'xǁBlocksCustomButtonǁsetShowNotification'

    @property
    def name(self):
        """Widget name"""
        return self._name

    @name.setter
    def name(self, new_name) -> None:
        self._name = new_name
        self.setObjectName(new_name)

    def text(self) -> str | None:
        """Button text"""
        return self._text

    def setText(self, text: str) -> None:
        args = [text]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksCustomButtonǁsetText__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksCustomButtonǁsetText__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksCustomButtonǁsetText__mutmut_orig(self, text: str) -> None:
        """Set button text"""
        self._text = text
        self.update()

    def xǁBlocksCustomButtonǁsetText__mutmut_1(self, text: str) -> None:
        """Set button text"""
        self._text = None
        self.update()
    
    xǁBlocksCustomButtonǁsetText__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksCustomButtonǁsetText__mutmut_1': xǁBlocksCustomButtonǁsetText__mutmut_1
    }
    xǁBlocksCustomButtonǁsetText__mutmut_orig.__name__ = 'xǁBlocksCustomButtonǁsetText'

    def setPixmap(self, pixmap: QtGui.QPixmap) -> None:
        args = [pixmap]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksCustomButtonǁsetPixmap__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksCustomButtonǁsetPixmap__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksCustomButtonǁsetPixmap__mutmut_orig(self, pixmap: QtGui.QPixmap) -> None:
        """Set button pixmap"""
        self.icon_pixmap = pixmap
        self.update()

    def xǁBlocksCustomButtonǁsetPixmap__mutmut_1(self, pixmap: QtGui.QPixmap) -> None:
        """Set button pixmap"""
        self.icon_pixmap = None
        self.update()
    
    xǁBlocksCustomButtonǁsetPixmap__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksCustomButtonǁsetPixmap__mutmut_1': xǁBlocksCustomButtonǁsetPixmap__mutmut_1
    }
    xǁBlocksCustomButtonǁsetPixmap__mutmut_orig.__name__ = 'xǁBlocksCustomButtonǁsetPixmap'

    def mousePressEvent(self, e: QtGui.QMouseEvent) -> None:
        args = [e]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksCustomButtonǁmousePressEvent__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksCustomButtonǁmousePressEvent__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksCustomButtonǁmousePressEvent__mutmut_orig(self, e: QtGui.QMouseEvent) -> None:
        """Handle mouse press events"""
        if not self.isEnabled():
            e.ignore()
            return
        if self.button_background:
            pos_f = QtCore.QPointF(e.pos())
            if self.button_background.contains(pos_f):
                super().mousePressEvent(e)
                return
            e.ignore()
            return
        super().mousePressEvent(e)

    def xǁBlocksCustomButtonǁmousePressEvent__mutmut_1(self, e: QtGui.QMouseEvent) -> None:
        """Handle mouse press events"""
        if self.isEnabled():
            e.ignore()
            return
        if self.button_background:
            pos_f = QtCore.QPointF(e.pos())
            if self.button_background.contains(pos_f):
                super().mousePressEvent(e)
                return
            e.ignore()
            return
        super().mousePressEvent(e)

    def xǁBlocksCustomButtonǁmousePressEvent__mutmut_2(self, e: QtGui.QMouseEvent) -> None:
        """Handle mouse press events"""
        if not self.isEnabled():
            e.ignore()
            return
        if self.button_background:
            pos_f = None
            if self.button_background.contains(pos_f):
                super().mousePressEvent(e)
                return
            e.ignore()
            return
        super().mousePressEvent(e)

    def xǁBlocksCustomButtonǁmousePressEvent__mutmut_3(self, e: QtGui.QMouseEvent) -> None:
        """Handle mouse press events"""
        if not self.isEnabled():
            e.ignore()
            return
        if self.button_background:
            pos_f = QtCore.QPointF(None)
            if self.button_background.contains(pos_f):
                super().mousePressEvent(e)
                return
            e.ignore()
            return
        super().mousePressEvent(e)

    def xǁBlocksCustomButtonǁmousePressEvent__mutmut_4(self, e: QtGui.QMouseEvent) -> None:
        """Handle mouse press events"""
        if not self.isEnabled():
            e.ignore()
            return
        if self.button_background:
            pos_f = QtCore.QPointF(e.pos())
            if self.button_background.contains(None):
                super().mousePressEvent(e)
                return
            e.ignore()
            return
        super().mousePressEvent(e)

    def xǁBlocksCustomButtonǁmousePressEvent__mutmut_5(self, e: QtGui.QMouseEvent) -> None:
        """Handle mouse press events"""
        if not self.isEnabled():
            e.ignore()
            return
        if self.button_background:
            pos_f = QtCore.QPointF(e.pos())
            if self.button_background.contains(pos_f):
                super().mousePressEvent(None)
                return
            e.ignore()
            return
        super().mousePressEvent(e)

    def xǁBlocksCustomButtonǁmousePressEvent__mutmut_6(self, e: QtGui.QMouseEvent) -> None:
        """Handle mouse press events"""
        if not self.isEnabled():
            e.ignore()
            return
        if self.button_background:
            pos_f = QtCore.QPointF(e.pos())
            if self.button_background.contains(pos_f):
                super().mousePressEvent(e)
                return
            e.ignore()
            return
        super().mousePressEvent(None)
    
    xǁBlocksCustomButtonǁmousePressEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksCustomButtonǁmousePressEvent__mutmut_1': xǁBlocksCustomButtonǁmousePressEvent__mutmut_1, 
        'xǁBlocksCustomButtonǁmousePressEvent__mutmut_2': xǁBlocksCustomButtonǁmousePressEvent__mutmut_2, 
        'xǁBlocksCustomButtonǁmousePressEvent__mutmut_3': xǁBlocksCustomButtonǁmousePressEvent__mutmut_3, 
        'xǁBlocksCustomButtonǁmousePressEvent__mutmut_4': xǁBlocksCustomButtonǁmousePressEvent__mutmut_4, 
        'xǁBlocksCustomButtonǁmousePressEvent__mutmut_5': xǁBlocksCustomButtonǁmousePressEvent__mutmut_5, 
        'xǁBlocksCustomButtonǁmousePressEvent__mutmut_6': xǁBlocksCustomButtonǁmousePressEvent__mutmut_6
    }
    xǁBlocksCustomButtonǁmousePressEvent__mutmut_orig.__name__ = 'xǁBlocksCustomButtonǁmousePressEvent'

    def setFlat(self, flat) -> None:
        args = [flat]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksCustomButtonǁsetFlat__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksCustomButtonǁsetFlat__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksCustomButtonǁsetFlat__mutmut_orig(self, flat) -> None:
        """Enable 'flat' appearance to the button"""
        if self._is_flat != flat:
            self._is_flat = flat
            self.update()  # Schedule repaint

    def xǁBlocksCustomButtonǁsetFlat__mutmut_1(self, flat) -> None:
        """Enable 'flat' appearance to the button"""
        if self._is_flat == flat:
            self._is_flat = flat
            self.update()  # Schedule repaint

    def xǁBlocksCustomButtonǁsetFlat__mutmut_2(self, flat) -> None:
        """Enable 'flat' appearance to the button"""
        if self._is_flat != flat:
            self._is_flat = None
            self.update()  # Schedule repaint
    
    xǁBlocksCustomButtonǁsetFlat__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksCustomButtonǁsetFlat__mutmut_1': xǁBlocksCustomButtonǁsetFlat__mutmut_1, 
        'xǁBlocksCustomButtonǁsetFlat__mutmut_2': xǁBlocksCustomButtonǁsetFlat__mutmut_2
    }
    xǁBlocksCustomButtonǁsetFlat__mutmut_orig.__name__ = 'xǁBlocksCustomButtonǁsetFlat'

    def isFlat(self) -> bool:
        """Get flat property

        Returns:
            bool: Button has 'flat' appearance enabled
        """
        return self._is_flat

    def setAutoDefault(self, _):
        """Disable auto default behavior"""
        return

    def setProperty(self, name: str, value: typing.Any):
        args = [name, value]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksCustomButtonǁsetProperty__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksCustomButtonǁsetProperty__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksCustomButtonǁsetProperty__mutmut_orig(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name == "icon_pixmap":
            self.icon_pixmap = value
        if name == "name":
            self._name = name
        if name == "text_color":
            self.text_color = QtGui.QColor(value)
        self.update()

    def xǁBlocksCustomButtonǁsetProperty__mutmut_1(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name != "icon_pixmap":
            self.icon_pixmap = value
        if name == "name":
            self._name = name
        if name == "text_color":
            self.text_color = QtGui.QColor(value)
        self.update()

    def xǁBlocksCustomButtonǁsetProperty__mutmut_2(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name == "XXicon_pixmapXX":
            self.icon_pixmap = value
        if name == "name":
            self._name = name
        if name == "text_color":
            self.text_color = QtGui.QColor(value)
        self.update()

    def xǁBlocksCustomButtonǁsetProperty__mutmut_3(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name == "ICON_PIXMAP":
            self.icon_pixmap = value
        if name == "name":
            self._name = name
        if name == "text_color":
            self.text_color = QtGui.QColor(value)
        self.update()

    def xǁBlocksCustomButtonǁsetProperty__mutmut_4(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name == "icon_pixmap":
            self.icon_pixmap = None
        if name == "name":
            self._name = name
        if name == "text_color":
            self.text_color = QtGui.QColor(value)
        self.update()

    def xǁBlocksCustomButtonǁsetProperty__mutmut_5(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name == "icon_pixmap":
            self.icon_pixmap = value
        if name != "name":
            self._name = name
        if name == "text_color":
            self.text_color = QtGui.QColor(value)
        self.update()

    def xǁBlocksCustomButtonǁsetProperty__mutmut_6(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name == "icon_pixmap":
            self.icon_pixmap = value
        if name == "XXnameXX":
            self._name = name
        if name == "text_color":
            self.text_color = QtGui.QColor(value)
        self.update()

    def xǁBlocksCustomButtonǁsetProperty__mutmut_7(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name == "icon_pixmap":
            self.icon_pixmap = value
        if name == "NAME":
            self._name = name
        if name == "text_color":
            self.text_color = QtGui.QColor(value)
        self.update()

    def xǁBlocksCustomButtonǁsetProperty__mutmut_8(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name == "icon_pixmap":
            self.icon_pixmap = value
        if name == "name":
            self._name = None
        if name == "text_color":
            self.text_color = QtGui.QColor(value)
        self.update()

    def xǁBlocksCustomButtonǁsetProperty__mutmut_9(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name == "icon_pixmap":
            self.icon_pixmap = value
        if name == "name":
            self._name = name
        if name != "text_color":
            self.text_color = QtGui.QColor(value)
        self.update()

    def xǁBlocksCustomButtonǁsetProperty__mutmut_10(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name == "icon_pixmap":
            self.icon_pixmap = value
        if name == "name":
            self._name = name
        if name == "XXtext_colorXX":
            self.text_color = QtGui.QColor(value)
        self.update()

    def xǁBlocksCustomButtonǁsetProperty__mutmut_11(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name == "icon_pixmap":
            self.icon_pixmap = value
        if name == "name":
            self._name = name
        if name == "TEXT_COLOR":
            self.text_color = QtGui.QColor(value)
        self.update()

    def xǁBlocksCustomButtonǁsetProperty__mutmut_12(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name == "icon_pixmap":
            self.icon_pixmap = value
        if name == "name":
            self._name = name
        if name == "text_color":
            self.text_color = None
        self.update()

    def xǁBlocksCustomButtonǁsetProperty__mutmut_13(self, name: str, value: typing.Any):
        """Set widget properties"""
        if name == "icon_pixmap":
            self.icon_pixmap = value
        if name == "name":
            self._name = name
        if name == "text_color":
            self.text_color = QtGui.QColor(None)
        self.update()
    
    xǁBlocksCustomButtonǁsetProperty__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksCustomButtonǁsetProperty__mutmut_1': xǁBlocksCustomButtonǁsetProperty__mutmut_1, 
        'xǁBlocksCustomButtonǁsetProperty__mutmut_2': xǁBlocksCustomButtonǁsetProperty__mutmut_2, 
        'xǁBlocksCustomButtonǁsetProperty__mutmut_3': xǁBlocksCustomButtonǁsetProperty__mutmut_3, 
        'xǁBlocksCustomButtonǁsetProperty__mutmut_4': xǁBlocksCustomButtonǁsetProperty__mutmut_4, 
        'xǁBlocksCustomButtonǁsetProperty__mutmut_5': xǁBlocksCustomButtonǁsetProperty__mutmut_5, 
        'xǁBlocksCustomButtonǁsetProperty__mutmut_6': xǁBlocksCustomButtonǁsetProperty__mutmut_6, 
        'xǁBlocksCustomButtonǁsetProperty__mutmut_7': xǁBlocksCustomButtonǁsetProperty__mutmut_7, 
        'xǁBlocksCustomButtonǁsetProperty__mutmut_8': xǁBlocksCustomButtonǁsetProperty__mutmut_8, 
        'xǁBlocksCustomButtonǁsetProperty__mutmut_9': xǁBlocksCustomButtonǁsetProperty__mutmut_9, 
        'xǁBlocksCustomButtonǁsetProperty__mutmut_10': xǁBlocksCustomButtonǁsetProperty__mutmut_10, 
        'xǁBlocksCustomButtonǁsetProperty__mutmut_11': xǁBlocksCustomButtonǁsetProperty__mutmut_11, 
        'xǁBlocksCustomButtonǁsetProperty__mutmut_12': xǁBlocksCustomButtonǁsetProperty__mutmut_12, 
        'xǁBlocksCustomButtonǁsetProperty__mutmut_13': xǁBlocksCustomButtonǁsetProperty__mutmut_13
    }
    xǁBlocksCustomButtonǁsetProperty__mutmut_orig.__name__ = 'xǁBlocksCustomButtonǁsetProperty'

    def paintEvent(self, e: typing.Optional[QtGui.QPaintEvent]):
        args = [e]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksCustomButtonǁpaintEvent__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksCustomButtonǁpaintEvent__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_orig(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_1(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = None
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_2(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(None)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_3(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(None, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_4(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, None)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_5(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_6(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, )
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_7(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, False)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_8(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(None, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_9(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, None)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_10(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_11(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, )
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_12(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, False)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_13(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(None, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_14(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, None)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_15(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_16(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, )
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_17(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, False)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_18(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = None
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_19(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = None
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_20(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style and not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_21(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_22(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_23(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = None
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_24(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = None
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_25(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse() and opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_26(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat and self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_27(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_28(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state | QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_29(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                None, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_30(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, None, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_31(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, None, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_32(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, None
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_33(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_34(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_35(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_36(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_37(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            None, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_38(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, None, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_39(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, None, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_40(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, None
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_41(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_42(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_43(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_44(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_45(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(None)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_46(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_47(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = None
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_48(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = None
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_49(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = None
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_50(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = None
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_51(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = None
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_52(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = None

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_53(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = None

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_54(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = None
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_55(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = None
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_56(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() * 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_57(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 3.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_58(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = None
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_59(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() * 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_60(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 3.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_61(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(None)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_62(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            None,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_63(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            None,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_64(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            None,
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_65(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            None,
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_66(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            None,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_67(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            None,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_68(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            None,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_69(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_70(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_71(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_72(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_73(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_74(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_75(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_76(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            1,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_77(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            1,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_78(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = None
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_79(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = None
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_80(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            None,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_81(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            None,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_82(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            None,
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_83(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            None,
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_84(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_85(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_86(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_87(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_88(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left() - self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_89(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() / 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_90(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 1.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_91(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top() - self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_92(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() / 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_93(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 1.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_94(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() / 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_95(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 1.9),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_96(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() / 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_97(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 1.9),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_98(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(None)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_99(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = None
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_100(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(None)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_101(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(None)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_102(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(None)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_103(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(None, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_104(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, None)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_105(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_106(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, )
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_107(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = None
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_108(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = None
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_109(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            None,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_110(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            None,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_111(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            None,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_112(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            None,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_113(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_114(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_115(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_116(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_117(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() / 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_118(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 3.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_119(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() / 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_120(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 3.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_121(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() / 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_122(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 1.8,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_123(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() / 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_124(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 1.8,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_125(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_126(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = None
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_127(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                None,
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_128(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                None,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_129(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                None,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_130(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_131(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_132(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_133(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = None
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_134(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = None
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_135(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = None
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_136(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) * 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_137(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() + scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_138(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 3.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_139(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = None
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_140(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) * 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_141(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() + scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_142(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 3.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_143(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = None
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_144(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                None,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_145(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                None,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_146(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                None,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_147(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                None,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_148(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_149(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_150(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_151(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_152(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() - adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_153(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() - adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_154(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = None
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_155(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(None)
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_156(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(None)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_157(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_158(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = None
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_159(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(None)
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_160(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(None)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_161(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = None
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_162(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(None)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_163(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(None)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_164(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    None
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_165(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(None, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_166(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, None, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_167(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, None)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_168(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_169(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_170(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, )
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_171(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(1, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_172(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 1, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_173(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    None
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_174(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = None
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_175(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    None, bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_176(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), None, bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_177(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), None, 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_178(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), None
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_179(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_180(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_181(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_182(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_183(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 121
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_184(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(None, tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_185(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), None)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_186(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_187(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), )
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_188(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = None
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_189(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = None
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_190(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = None
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_191(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(None, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_192(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, None)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_193(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_194(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, )
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_195(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(None, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_196(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, None, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_197(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, None)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_198(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(_rect, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_199(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, current_text_color)
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_200(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, )
        if self._show_notification:
            self._paint_notification(painter)

        painter.end()

    def xǁBlocksCustomButtonǁpaintEvent__mutmut_201(self, e: typing.Optional[QtGui.QPaintEvent]):
        """Re-implemented method, paint widget"""
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        _rect = self.rect()
        _style = self.style()
        if not _style or not _rect:
            return
        # Flat button control
        opt = QtWidgets.QStyleOptionButton()
        draw_frame = (
            not self._is_flat
            or self.underMouse()
            or opt.state & QtWidgets.QStyle.StateFlag.State_Sunken
        )
        if draw_frame:
            _style.drawControl(
                QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
            )
        _style.drawControl(
            QtWidgets.QStyle.ControlElement.CE_PushButtonLabel, opt, painter, self
        )
        self.setStyle(_style)
        # Determine background and text colors based on state
        if not self.isEnabled():
            bg_color_tuple = ButtonColors.DISABLED_BG.value
            current_text_color = QtGui.QColor(*ButtonColors.DISABLED_TEXT_COLOR.value)
        elif self.isDown():
            bg_color_tuple = ButtonColors.PRESSED_BG.value
            current_text_color = self.text_color
        else:
            bg_color_tuple = ButtonColors.NORMAL_BG.value
            current_text_color = self.text_color

        bg_color = QtGui.QColor(*bg_color_tuple)

        path = QtGui.QPainterPath()
        xRadius = self.rect().toRectF().normalized().height() / 2.0
        yRadius = self.rect().toRectF().normalized().height() / 2.0
        painter.setBackgroundMode(QtCore.Qt.BGMode.TransparentMode)
        path.addRoundedRect(
            0,
            0,
            self.rect().toRectF().normalized().width(),
            self.rect().toRectF().normalized().height(),
            xRadius,
            yRadius,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        icon_path = QtGui.QPainterPath()
        self.button_ellipse = QtCore.QRectF(
            self.rect().toRectF().normalized().left()
            + self.rect().toRectF().normalized().height() * 0.05,
            self.rect().toRectF().normalized().top()
            + self.rect().toRectF().normalized().height() * 0.05,
            (self.rect().toRectF().normalized().height() * 0.90),
            (self.rect().toRectF().normalized().height() * 0.90),
        )
        icon_path.addEllipse(self.button_ellipse)
        self.button_background = path.subtracted(icon_path)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(bg_color)
        painter.fillPath(self.button_background, bg_color)
        _parent_rect = self.button_ellipse.toRect()
        _icon_rect = QtCore.QRectF(
            _parent_rect.left() * 2.8,
            _parent_rect.top() * 2.8,
            _parent_rect.width() * 0.80,
            _parent_rect.height() * 0.80,
        )
        if not self.icon_pixmap.isNull():
            _icon_scaled = self.icon_pixmap.scaled(
                _icon_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            scaled_width = _icon_scaled.width()
            scaled_height = _icon_scaled.height()
            adjusted_x = (_icon_rect.width() - scaled_width) / 2.0
            adjusted_y = (_icon_rect.height() - scaled_height) / 2.0
            adjusted_icon_rect = QtCore.QRectF(
                _icon_rect.x() + adjusted_x,
                _icon_rect.y() + adjusted_y,
                scaled_width,
                scaled_height,
            )
            tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
            tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
            if not self.isEnabled():
                tinted_icon_pixmap = QtGui.QPixmap(_icon_scaled.size())
                tinted_icon_pixmap.fill(QtCore.Qt.GlobalColor.transparent)
                icon_painter = QtGui.QPainter(tinted_icon_pixmap)
                icon_painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)
                icon_painter.setRenderHint(
                    QtGui.QPainter.RenderHint.SmoothPixmapTransform
                )
                icon_painter.drawPixmap(0, 0, _icon_scaled)
                icon_painter.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceAtop
                )
                tint = QtGui.QColor(
                    bg_color.red(), bg_color.green(), bg_color.blue(), 120
                )
                icon_painter.fillRect(tinted_icon_pixmap.rect(), tint)
                icon_painter.end()
                final_pixmap = tinted_icon_pixmap
            else:
                final_pixmap = _icon_scaled
            destination_point = adjusted_icon_rect.toRect().topLeft()
            painter.drawPixmap(destination_point, final_pixmap)
        if self.text():
            self._paint_text(painter, _rect, current_text_color)
        if self._show_notification:
            self._paint_notification(None)

        painter.end()
    
    xǁBlocksCustomButtonǁpaintEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksCustomButtonǁpaintEvent__mutmut_1': xǁBlocksCustomButtonǁpaintEvent__mutmut_1, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_2': xǁBlocksCustomButtonǁpaintEvent__mutmut_2, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_3': xǁBlocksCustomButtonǁpaintEvent__mutmut_3, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_4': xǁBlocksCustomButtonǁpaintEvent__mutmut_4, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_5': xǁBlocksCustomButtonǁpaintEvent__mutmut_5, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_6': xǁBlocksCustomButtonǁpaintEvent__mutmut_6, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_7': xǁBlocksCustomButtonǁpaintEvent__mutmut_7, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_8': xǁBlocksCustomButtonǁpaintEvent__mutmut_8, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_9': xǁBlocksCustomButtonǁpaintEvent__mutmut_9, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_10': xǁBlocksCustomButtonǁpaintEvent__mutmut_10, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_11': xǁBlocksCustomButtonǁpaintEvent__mutmut_11, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_12': xǁBlocksCustomButtonǁpaintEvent__mutmut_12, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_13': xǁBlocksCustomButtonǁpaintEvent__mutmut_13, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_14': xǁBlocksCustomButtonǁpaintEvent__mutmut_14, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_15': xǁBlocksCustomButtonǁpaintEvent__mutmut_15, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_16': xǁBlocksCustomButtonǁpaintEvent__mutmut_16, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_17': xǁBlocksCustomButtonǁpaintEvent__mutmut_17, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_18': xǁBlocksCustomButtonǁpaintEvent__mutmut_18, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_19': xǁBlocksCustomButtonǁpaintEvent__mutmut_19, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_20': xǁBlocksCustomButtonǁpaintEvent__mutmut_20, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_21': xǁBlocksCustomButtonǁpaintEvent__mutmut_21, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_22': xǁBlocksCustomButtonǁpaintEvent__mutmut_22, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_23': xǁBlocksCustomButtonǁpaintEvent__mutmut_23, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_24': xǁBlocksCustomButtonǁpaintEvent__mutmut_24, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_25': xǁBlocksCustomButtonǁpaintEvent__mutmut_25, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_26': xǁBlocksCustomButtonǁpaintEvent__mutmut_26, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_27': xǁBlocksCustomButtonǁpaintEvent__mutmut_27, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_28': xǁBlocksCustomButtonǁpaintEvent__mutmut_28, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_29': xǁBlocksCustomButtonǁpaintEvent__mutmut_29, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_30': xǁBlocksCustomButtonǁpaintEvent__mutmut_30, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_31': xǁBlocksCustomButtonǁpaintEvent__mutmut_31, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_32': xǁBlocksCustomButtonǁpaintEvent__mutmut_32, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_33': xǁBlocksCustomButtonǁpaintEvent__mutmut_33, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_34': xǁBlocksCustomButtonǁpaintEvent__mutmut_34, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_35': xǁBlocksCustomButtonǁpaintEvent__mutmut_35, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_36': xǁBlocksCustomButtonǁpaintEvent__mutmut_36, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_37': xǁBlocksCustomButtonǁpaintEvent__mutmut_37, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_38': xǁBlocksCustomButtonǁpaintEvent__mutmut_38, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_39': xǁBlocksCustomButtonǁpaintEvent__mutmut_39, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_40': xǁBlocksCustomButtonǁpaintEvent__mutmut_40, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_41': xǁBlocksCustomButtonǁpaintEvent__mutmut_41, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_42': xǁBlocksCustomButtonǁpaintEvent__mutmut_42, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_43': xǁBlocksCustomButtonǁpaintEvent__mutmut_43, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_44': xǁBlocksCustomButtonǁpaintEvent__mutmut_44, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_45': xǁBlocksCustomButtonǁpaintEvent__mutmut_45, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_46': xǁBlocksCustomButtonǁpaintEvent__mutmut_46, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_47': xǁBlocksCustomButtonǁpaintEvent__mutmut_47, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_48': xǁBlocksCustomButtonǁpaintEvent__mutmut_48, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_49': xǁBlocksCustomButtonǁpaintEvent__mutmut_49, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_50': xǁBlocksCustomButtonǁpaintEvent__mutmut_50, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_51': xǁBlocksCustomButtonǁpaintEvent__mutmut_51, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_52': xǁBlocksCustomButtonǁpaintEvent__mutmut_52, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_53': xǁBlocksCustomButtonǁpaintEvent__mutmut_53, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_54': xǁBlocksCustomButtonǁpaintEvent__mutmut_54, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_55': xǁBlocksCustomButtonǁpaintEvent__mutmut_55, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_56': xǁBlocksCustomButtonǁpaintEvent__mutmut_56, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_57': xǁBlocksCustomButtonǁpaintEvent__mutmut_57, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_58': xǁBlocksCustomButtonǁpaintEvent__mutmut_58, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_59': xǁBlocksCustomButtonǁpaintEvent__mutmut_59, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_60': xǁBlocksCustomButtonǁpaintEvent__mutmut_60, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_61': xǁBlocksCustomButtonǁpaintEvent__mutmut_61, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_62': xǁBlocksCustomButtonǁpaintEvent__mutmut_62, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_63': xǁBlocksCustomButtonǁpaintEvent__mutmut_63, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_64': xǁBlocksCustomButtonǁpaintEvent__mutmut_64, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_65': xǁBlocksCustomButtonǁpaintEvent__mutmut_65, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_66': xǁBlocksCustomButtonǁpaintEvent__mutmut_66, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_67': xǁBlocksCustomButtonǁpaintEvent__mutmut_67, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_68': xǁBlocksCustomButtonǁpaintEvent__mutmut_68, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_69': xǁBlocksCustomButtonǁpaintEvent__mutmut_69, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_70': xǁBlocksCustomButtonǁpaintEvent__mutmut_70, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_71': xǁBlocksCustomButtonǁpaintEvent__mutmut_71, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_72': xǁBlocksCustomButtonǁpaintEvent__mutmut_72, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_73': xǁBlocksCustomButtonǁpaintEvent__mutmut_73, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_74': xǁBlocksCustomButtonǁpaintEvent__mutmut_74, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_75': xǁBlocksCustomButtonǁpaintEvent__mutmut_75, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_76': xǁBlocksCustomButtonǁpaintEvent__mutmut_76, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_77': xǁBlocksCustomButtonǁpaintEvent__mutmut_77, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_78': xǁBlocksCustomButtonǁpaintEvent__mutmut_78, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_79': xǁBlocksCustomButtonǁpaintEvent__mutmut_79, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_80': xǁBlocksCustomButtonǁpaintEvent__mutmut_80, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_81': xǁBlocksCustomButtonǁpaintEvent__mutmut_81, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_82': xǁBlocksCustomButtonǁpaintEvent__mutmut_82, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_83': xǁBlocksCustomButtonǁpaintEvent__mutmut_83, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_84': xǁBlocksCustomButtonǁpaintEvent__mutmut_84, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_85': xǁBlocksCustomButtonǁpaintEvent__mutmut_85, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_86': xǁBlocksCustomButtonǁpaintEvent__mutmut_86, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_87': xǁBlocksCustomButtonǁpaintEvent__mutmut_87, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_88': xǁBlocksCustomButtonǁpaintEvent__mutmut_88, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_89': xǁBlocksCustomButtonǁpaintEvent__mutmut_89, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_90': xǁBlocksCustomButtonǁpaintEvent__mutmut_90, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_91': xǁBlocksCustomButtonǁpaintEvent__mutmut_91, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_92': xǁBlocksCustomButtonǁpaintEvent__mutmut_92, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_93': xǁBlocksCustomButtonǁpaintEvent__mutmut_93, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_94': xǁBlocksCustomButtonǁpaintEvent__mutmut_94, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_95': xǁBlocksCustomButtonǁpaintEvent__mutmut_95, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_96': xǁBlocksCustomButtonǁpaintEvent__mutmut_96, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_97': xǁBlocksCustomButtonǁpaintEvent__mutmut_97, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_98': xǁBlocksCustomButtonǁpaintEvent__mutmut_98, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_99': xǁBlocksCustomButtonǁpaintEvent__mutmut_99, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_100': xǁBlocksCustomButtonǁpaintEvent__mutmut_100, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_101': xǁBlocksCustomButtonǁpaintEvent__mutmut_101, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_102': xǁBlocksCustomButtonǁpaintEvent__mutmut_102, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_103': xǁBlocksCustomButtonǁpaintEvent__mutmut_103, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_104': xǁBlocksCustomButtonǁpaintEvent__mutmut_104, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_105': xǁBlocksCustomButtonǁpaintEvent__mutmut_105, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_106': xǁBlocksCustomButtonǁpaintEvent__mutmut_106, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_107': xǁBlocksCustomButtonǁpaintEvent__mutmut_107, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_108': xǁBlocksCustomButtonǁpaintEvent__mutmut_108, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_109': xǁBlocksCustomButtonǁpaintEvent__mutmut_109, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_110': xǁBlocksCustomButtonǁpaintEvent__mutmut_110, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_111': xǁBlocksCustomButtonǁpaintEvent__mutmut_111, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_112': xǁBlocksCustomButtonǁpaintEvent__mutmut_112, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_113': xǁBlocksCustomButtonǁpaintEvent__mutmut_113, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_114': xǁBlocksCustomButtonǁpaintEvent__mutmut_114, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_115': xǁBlocksCustomButtonǁpaintEvent__mutmut_115, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_116': xǁBlocksCustomButtonǁpaintEvent__mutmut_116, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_117': xǁBlocksCustomButtonǁpaintEvent__mutmut_117, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_118': xǁBlocksCustomButtonǁpaintEvent__mutmut_118, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_119': xǁBlocksCustomButtonǁpaintEvent__mutmut_119, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_120': xǁBlocksCustomButtonǁpaintEvent__mutmut_120, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_121': xǁBlocksCustomButtonǁpaintEvent__mutmut_121, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_122': xǁBlocksCustomButtonǁpaintEvent__mutmut_122, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_123': xǁBlocksCustomButtonǁpaintEvent__mutmut_123, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_124': xǁBlocksCustomButtonǁpaintEvent__mutmut_124, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_125': xǁBlocksCustomButtonǁpaintEvent__mutmut_125, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_126': xǁBlocksCustomButtonǁpaintEvent__mutmut_126, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_127': xǁBlocksCustomButtonǁpaintEvent__mutmut_127, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_128': xǁBlocksCustomButtonǁpaintEvent__mutmut_128, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_129': xǁBlocksCustomButtonǁpaintEvent__mutmut_129, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_130': xǁBlocksCustomButtonǁpaintEvent__mutmut_130, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_131': xǁBlocksCustomButtonǁpaintEvent__mutmut_131, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_132': xǁBlocksCustomButtonǁpaintEvent__mutmut_132, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_133': xǁBlocksCustomButtonǁpaintEvent__mutmut_133, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_134': xǁBlocksCustomButtonǁpaintEvent__mutmut_134, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_135': xǁBlocksCustomButtonǁpaintEvent__mutmut_135, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_136': xǁBlocksCustomButtonǁpaintEvent__mutmut_136, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_137': xǁBlocksCustomButtonǁpaintEvent__mutmut_137, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_138': xǁBlocksCustomButtonǁpaintEvent__mutmut_138, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_139': xǁBlocksCustomButtonǁpaintEvent__mutmut_139, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_140': xǁBlocksCustomButtonǁpaintEvent__mutmut_140, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_141': xǁBlocksCustomButtonǁpaintEvent__mutmut_141, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_142': xǁBlocksCustomButtonǁpaintEvent__mutmut_142, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_143': xǁBlocksCustomButtonǁpaintEvent__mutmut_143, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_144': xǁBlocksCustomButtonǁpaintEvent__mutmut_144, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_145': xǁBlocksCustomButtonǁpaintEvent__mutmut_145, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_146': xǁBlocksCustomButtonǁpaintEvent__mutmut_146, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_147': xǁBlocksCustomButtonǁpaintEvent__mutmut_147, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_148': xǁBlocksCustomButtonǁpaintEvent__mutmut_148, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_149': xǁBlocksCustomButtonǁpaintEvent__mutmut_149, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_150': xǁBlocksCustomButtonǁpaintEvent__mutmut_150, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_151': xǁBlocksCustomButtonǁpaintEvent__mutmut_151, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_152': xǁBlocksCustomButtonǁpaintEvent__mutmut_152, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_153': xǁBlocksCustomButtonǁpaintEvent__mutmut_153, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_154': xǁBlocksCustomButtonǁpaintEvent__mutmut_154, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_155': xǁBlocksCustomButtonǁpaintEvent__mutmut_155, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_156': xǁBlocksCustomButtonǁpaintEvent__mutmut_156, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_157': xǁBlocksCustomButtonǁpaintEvent__mutmut_157, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_158': xǁBlocksCustomButtonǁpaintEvent__mutmut_158, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_159': xǁBlocksCustomButtonǁpaintEvent__mutmut_159, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_160': xǁBlocksCustomButtonǁpaintEvent__mutmut_160, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_161': xǁBlocksCustomButtonǁpaintEvent__mutmut_161, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_162': xǁBlocksCustomButtonǁpaintEvent__mutmut_162, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_163': xǁBlocksCustomButtonǁpaintEvent__mutmut_163, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_164': xǁBlocksCustomButtonǁpaintEvent__mutmut_164, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_165': xǁBlocksCustomButtonǁpaintEvent__mutmut_165, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_166': xǁBlocksCustomButtonǁpaintEvent__mutmut_166, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_167': xǁBlocksCustomButtonǁpaintEvent__mutmut_167, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_168': xǁBlocksCustomButtonǁpaintEvent__mutmut_168, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_169': xǁBlocksCustomButtonǁpaintEvent__mutmut_169, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_170': xǁBlocksCustomButtonǁpaintEvent__mutmut_170, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_171': xǁBlocksCustomButtonǁpaintEvent__mutmut_171, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_172': xǁBlocksCustomButtonǁpaintEvent__mutmut_172, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_173': xǁBlocksCustomButtonǁpaintEvent__mutmut_173, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_174': xǁBlocksCustomButtonǁpaintEvent__mutmut_174, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_175': xǁBlocksCustomButtonǁpaintEvent__mutmut_175, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_176': xǁBlocksCustomButtonǁpaintEvent__mutmut_176, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_177': xǁBlocksCustomButtonǁpaintEvent__mutmut_177, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_178': xǁBlocksCustomButtonǁpaintEvent__mutmut_178, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_179': xǁBlocksCustomButtonǁpaintEvent__mutmut_179, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_180': xǁBlocksCustomButtonǁpaintEvent__mutmut_180, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_181': xǁBlocksCustomButtonǁpaintEvent__mutmut_181, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_182': xǁBlocksCustomButtonǁpaintEvent__mutmut_182, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_183': xǁBlocksCustomButtonǁpaintEvent__mutmut_183, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_184': xǁBlocksCustomButtonǁpaintEvent__mutmut_184, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_185': xǁBlocksCustomButtonǁpaintEvent__mutmut_185, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_186': xǁBlocksCustomButtonǁpaintEvent__mutmut_186, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_187': xǁBlocksCustomButtonǁpaintEvent__mutmut_187, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_188': xǁBlocksCustomButtonǁpaintEvent__mutmut_188, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_189': xǁBlocksCustomButtonǁpaintEvent__mutmut_189, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_190': xǁBlocksCustomButtonǁpaintEvent__mutmut_190, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_191': xǁBlocksCustomButtonǁpaintEvent__mutmut_191, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_192': xǁBlocksCustomButtonǁpaintEvent__mutmut_192, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_193': xǁBlocksCustomButtonǁpaintEvent__mutmut_193, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_194': xǁBlocksCustomButtonǁpaintEvent__mutmut_194, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_195': xǁBlocksCustomButtonǁpaintEvent__mutmut_195, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_196': xǁBlocksCustomButtonǁpaintEvent__mutmut_196, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_197': xǁBlocksCustomButtonǁpaintEvent__mutmut_197, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_198': xǁBlocksCustomButtonǁpaintEvent__mutmut_198, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_199': xǁBlocksCustomButtonǁpaintEvent__mutmut_199, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_200': xǁBlocksCustomButtonǁpaintEvent__mutmut_200, 
        'xǁBlocksCustomButtonǁpaintEvent__mutmut_201': xǁBlocksCustomButtonǁpaintEvent__mutmut_201
    }
    xǁBlocksCustomButtonǁpaintEvent__mutmut_orig.__name__ = 'xǁBlocksCustomButtonǁpaintEvent'

    def _paint_text(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        args = [painter, rect, text_color]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksCustomButtonǁ_paint_text__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksCustomButtonǁ_paint_text__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_orig(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_1(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = None
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_2(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = None
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_3(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(None)
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_4(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() + int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_5(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(None))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_6(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(None)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_7(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(None))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_8(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(None)
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_9(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() + int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_10(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(None))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_11(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(None)
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_12(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(None))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_13(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = None
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_14(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(None)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_15(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(None)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_16(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(2)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_17(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(None)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_18(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(None)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_19(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(None)
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_20(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() + int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_21(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(None))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_22(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() / 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_23(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 2.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_24(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(None)
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_25(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(None))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_26(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            None,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_27(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            None,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_28(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            None,
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_29(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_30(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_31(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_32(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic & QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_33(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(None),
        )
        painter.setPen(QtCore.Qt.PenStyle.NoPen)

    def xǁBlocksCustomButtonǁ_paint_text__mutmut_34(
        self, painter: QtGui.QPainter, rect: QtCore.QRect, text_color: QtGui.QColor
    ) -> None:
        _text_rect = rect
        _text_rect2 = rect
        _text_rect2.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect2.setLeft(int(self.button_ellipse.width()))
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width()))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        _pen = painter.pen()
        _pen.setStyle(QtCore.Qt.PenStyle.SolidLine)
        _pen.setWidth(1)
        _pen.setColor(text_color)
        painter.setPen(_pen)
        _text_rect.setWidth(self.width() - int(self.button_ellipse.width() * 1.4))
        _text_rect.setLeft(int(self.button_ellipse.width()))
        painter.drawText(
            _text_rect,
            QtCore.Qt.TextFlag.TextShowMnemonic | QtCore.Qt.AlignmentFlag.AlignCenter,
            str(self.text()),
        )
        painter.setPen(None)
    
    xǁBlocksCustomButtonǁ_paint_text__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksCustomButtonǁ_paint_text__mutmut_1': xǁBlocksCustomButtonǁ_paint_text__mutmut_1, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_2': xǁBlocksCustomButtonǁ_paint_text__mutmut_2, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_3': xǁBlocksCustomButtonǁ_paint_text__mutmut_3, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_4': xǁBlocksCustomButtonǁ_paint_text__mutmut_4, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_5': xǁBlocksCustomButtonǁ_paint_text__mutmut_5, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_6': xǁBlocksCustomButtonǁ_paint_text__mutmut_6, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_7': xǁBlocksCustomButtonǁ_paint_text__mutmut_7, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_8': xǁBlocksCustomButtonǁ_paint_text__mutmut_8, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_9': xǁBlocksCustomButtonǁ_paint_text__mutmut_9, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_10': xǁBlocksCustomButtonǁ_paint_text__mutmut_10, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_11': xǁBlocksCustomButtonǁ_paint_text__mutmut_11, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_12': xǁBlocksCustomButtonǁ_paint_text__mutmut_12, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_13': xǁBlocksCustomButtonǁ_paint_text__mutmut_13, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_14': xǁBlocksCustomButtonǁ_paint_text__mutmut_14, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_15': xǁBlocksCustomButtonǁ_paint_text__mutmut_15, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_16': xǁBlocksCustomButtonǁ_paint_text__mutmut_16, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_17': xǁBlocksCustomButtonǁ_paint_text__mutmut_17, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_18': xǁBlocksCustomButtonǁ_paint_text__mutmut_18, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_19': xǁBlocksCustomButtonǁ_paint_text__mutmut_19, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_20': xǁBlocksCustomButtonǁ_paint_text__mutmut_20, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_21': xǁBlocksCustomButtonǁ_paint_text__mutmut_21, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_22': xǁBlocksCustomButtonǁ_paint_text__mutmut_22, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_23': xǁBlocksCustomButtonǁ_paint_text__mutmut_23, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_24': xǁBlocksCustomButtonǁ_paint_text__mutmut_24, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_25': xǁBlocksCustomButtonǁ_paint_text__mutmut_25, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_26': xǁBlocksCustomButtonǁ_paint_text__mutmut_26, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_27': xǁBlocksCustomButtonǁ_paint_text__mutmut_27, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_28': xǁBlocksCustomButtonǁ_paint_text__mutmut_28, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_29': xǁBlocksCustomButtonǁ_paint_text__mutmut_29, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_30': xǁBlocksCustomButtonǁ_paint_text__mutmut_30, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_31': xǁBlocksCustomButtonǁ_paint_text__mutmut_31, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_32': xǁBlocksCustomButtonǁ_paint_text__mutmut_32, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_33': xǁBlocksCustomButtonǁ_paint_text__mutmut_33, 
        'xǁBlocksCustomButtonǁ_paint_text__mutmut_34': xǁBlocksCustomButtonǁ_paint_text__mutmut_34
    }
    xǁBlocksCustomButtonǁ_paint_text__mutmut_orig.__name__ = 'xǁBlocksCustomButtonǁ_paint_text'

    def _paint_notification(self, painter: QtGui.QPainter) -> None:
        args = [painter]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksCustomButtonǁ_paint_notification__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksCustomButtonǁ_paint_notification__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_orig(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 0, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_1(self, painter: QtGui.QPainter) -> None:
        dot_diameter = None
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 0, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_2(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() / 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 0, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_3(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 1.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 0, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_4(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = None
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 0, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_5(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() + dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 0, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_6(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = None
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 0, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_7(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(None)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 0, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_8(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(None)
        dot_rect = QtCore.QRectF(dot_x, 0, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_9(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = None
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_10(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(None, 0, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_11(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, None, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_12(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 0, None, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_13(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 0, dot_diameter, None)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_14(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(0, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_15(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_16(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 0, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_17(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 0, dot_diameter, )
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_18(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 1, dot_diameter, dot_diameter)
        painter.drawEllipse(dot_rect)

    def xǁBlocksCustomButtonǁ_paint_notification__mutmut_19(self, painter: QtGui.QPainter) -> None:
        dot_diameter = self.height() * 0.4
        dot_x = self.width() - dot_diameter
        notification_color = QtGui.QColor(*ButtonColors.NOTIFICATION_DOT.value)
        painter.setBrush(notification_color)
        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        dot_rect = QtCore.QRectF(dot_x, 0, dot_diameter, dot_diameter)
        painter.drawEllipse(None)
    
    xǁBlocksCustomButtonǁ_paint_notification__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksCustomButtonǁ_paint_notification__mutmut_1': xǁBlocksCustomButtonǁ_paint_notification__mutmut_1, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_2': xǁBlocksCustomButtonǁ_paint_notification__mutmut_2, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_3': xǁBlocksCustomButtonǁ_paint_notification__mutmut_3, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_4': xǁBlocksCustomButtonǁ_paint_notification__mutmut_4, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_5': xǁBlocksCustomButtonǁ_paint_notification__mutmut_5, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_6': xǁBlocksCustomButtonǁ_paint_notification__mutmut_6, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_7': xǁBlocksCustomButtonǁ_paint_notification__mutmut_7, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_8': xǁBlocksCustomButtonǁ_paint_notification__mutmut_8, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_9': xǁBlocksCustomButtonǁ_paint_notification__mutmut_9, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_10': xǁBlocksCustomButtonǁ_paint_notification__mutmut_10, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_11': xǁBlocksCustomButtonǁ_paint_notification__mutmut_11, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_12': xǁBlocksCustomButtonǁ_paint_notification__mutmut_12, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_13': xǁBlocksCustomButtonǁ_paint_notification__mutmut_13, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_14': xǁBlocksCustomButtonǁ_paint_notification__mutmut_14, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_15': xǁBlocksCustomButtonǁ_paint_notification__mutmut_15, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_16': xǁBlocksCustomButtonǁ_paint_notification__mutmut_16, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_17': xǁBlocksCustomButtonǁ_paint_notification__mutmut_17, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_18': xǁBlocksCustomButtonǁ_paint_notification__mutmut_18, 
        'xǁBlocksCustomButtonǁ_paint_notification__mutmut_19': xǁBlocksCustomButtonǁ_paint_notification__mutmut_19
    }
    xǁBlocksCustomButtonǁ_paint_notification__mutmut_orig.__name__ = 'xǁBlocksCustomButtonǁ_paint_notification'
