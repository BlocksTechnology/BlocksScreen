import typing
from dataclasses import dataclass, field

from PyQt6 import QtCore, QtGui, QtWidgets  # pylint: disable=import-error
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


@dataclass(slots=True)
class ListItem:
    """List item data"""

    text: str
    right_text: str = ""
    _rfontsize: int = 0
    _lfontsize: int = 0

    callback: typing.Optional[typing.Callable] = None

    color: str = "#dfdfdf"
    color_left_icon: bool = False
    right_icon: typing.Optional[QtGui.QPixmap] = None
    left_icon: typing.Optional[QtGui.QPixmap] = None

    selected: bool = False
    allow_check: bool = True

    not_clickable: bool = False

    allow_expand: bool = False
    needs_expansion: bool = False
    is_expanded: bool = False

    height: int = 60
    notificate: bool = False

    # stores width and heitgh of the button so we dont need to recalculate it every time
    _cache: typing.Dict[int, int] = field(default_factory=dict)

    def clear_cache(self):
        """Call this if text or font size changes dynamically"""
        self._cache.clear()


class EntryListModel(QtCore.QAbstractListModel):
    """List model Subclassed QAbstractListModel"""

    EnableRole = QtCore.Qt.ItemDataRole.UserRole + 1
    NotificateRole = QtCore.Qt.ItemDataRole.UserRole + 2
    ExpandRole = QtCore.Qt.ItemDataRole.UserRole + 3

    def __init__(self, entries=None) -> None:
        args = [entries]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁ__init____mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁ__init____mutmut_orig(self, entries=None) -> None:
        """Initialise the model with an optional pre-populated list of ``ListItem``s."""
        super().__init__()
        self.entries: list[ListItem] = entries or []

    def xǁEntryListModelǁ__init____mutmut_1(self, entries=None) -> None:
        """Initialise the model with an optional pre-populated list of ``ListItem``s."""
        super().__init__()
        self.entries: list[ListItem] = None

    def xǁEntryListModelǁ__init____mutmut_2(self, entries=None) -> None:
        """Initialise the model with an optional pre-populated list of ``ListItem``s."""
        super().__init__()
        self.entries: list[ListItem] = entries and []
    
    xǁEntryListModelǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁ__init____mutmut_1': xǁEntryListModelǁ__init____mutmut_1, 
        'xǁEntryListModelǁ__init____mutmut_2': xǁEntryListModelǁ__init____mutmut_2
    }
    xǁEntryListModelǁ__init____mutmut_orig.__name__ = 'xǁEntryListModelǁ__init__'

    def rowCount(self, parent=QtCore.QModelIndex()) -> int:
        """Gets model row count"""
        return len(self.entries)

    def deleteLater(self) -> None:
        """subclass for deleting the object"""
        return super().deleteLater()

    def remove_item(self, item: ListItem) -> None:
        args = [item]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁremove_item__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁremove_item__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁremove_item__mutmut_orig(self, item: ListItem) -> None:
        """Removes one row item from the model"""
        if item in self.entries:
            index = self.entries.index(item)
            self.beginRemoveRows(QtCore.QModelIndex(), index, index)
            self.entries.pop(index)
            self.endRemoveRows()

    def xǁEntryListModelǁremove_item__mutmut_1(self, item: ListItem) -> None:
        """Removes one row item from the model"""
        if item not in self.entries:
            index = self.entries.index(item)
            self.beginRemoveRows(QtCore.QModelIndex(), index, index)
            self.entries.pop(index)
            self.endRemoveRows()

    def xǁEntryListModelǁremove_item__mutmut_2(self, item: ListItem) -> None:
        """Removes one row item from the model"""
        if item in self.entries:
            index = None
            self.beginRemoveRows(QtCore.QModelIndex(), index, index)
            self.entries.pop(index)
            self.endRemoveRows()

    def xǁEntryListModelǁremove_item__mutmut_3(self, item: ListItem) -> None:
        """Removes one row item from the model"""
        if item in self.entries:
            index = self.entries.index(None)
            self.beginRemoveRows(QtCore.QModelIndex(), index, index)
            self.entries.pop(index)
            self.endRemoveRows()

    def xǁEntryListModelǁremove_item__mutmut_4(self, item: ListItem) -> None:
        """Removes one row item from the model"""
        if item in self.entries:
            index = self.entries.rindex(item)
            self.beginRemoveRows(QtCore.QModelIndex(), index, index)
            self.entries.pop(index)
            self.endRemoveRows()

    def xǁEntryListModelǁremove_item__mutmut_5(self, item: ListItem) -> None:
        """Removes one row item from the model"""
        if item in self.entries:
            index = self.entries.index(item)
            self.beginRemoveRows(None, index, index)
            self.entries.pop(index)
            self.endRemoveRows()

    def xǁEntryListModelǁremove_item__mutmut_6(self, item: ListItem) -> None:
        """Removes one row item from the model"""
        if item in self.entries:
            index = self.entries.index(item)
            self.beginRemoveRows(QtCore.QModelIndex(), None, index)
            self.entries.pop(index)
            self.endRemoveRows()

    def xǁEntryListModelǁremove_item__mutmut_7(self, item: ListItem) -> None:
        """Removes one row item from the model"""
        if item in self.entries:
            index = self.entries.index(item)
            self.beginRemoveRows(QtCore.QModelIndex(), index, None)
            self.entries.pop(index)
            self.endRemoveRows()

    def xǁEntryListModelǁremove_item__mutmut_8(self, item: ListItem) -> None:
        """Removes one row item from the model"""
        if item in self.entries:
            index = self.entries.index(item)
            self.beginRemoveRows(index, index)
            self.entries.pop(index)
            self.endRemoveRows()

    def xǁEntryListModelǁremove_item__mutmut_9(self, item: ListItem) -> None:
        """Removes one row item from the model"""
        if item in self.entries:
            index = self.entries.index(item)
            self.beginRemoveRows(QtCore.QModelIndex(), index)
            self.entries.pop(index)
            self.endRemoveRows()

    def xǁEntryListModelǁremove_item__mutmut_10(self, item: ListItem) -> None:
        """Removes one row item from the model"""
        if item in self.entries:
            index = self.entries.index(item)
            self.beginRemoveRows(QtCore.QModelIndex(), index, )
            self.entries.pop(index)
            self.endRemoveRows()

    def xǁEntryListModelǁremove_item__mutmut_11(self, item: ListItem) -> None:
        """Removes one row item from the model"""
        if item in self.entries:
            index = self.entries.index(item)
            self.beginRemoveRows(QtCore.QModelIndex(), index, index)
            self.entries.pop(None)
            self.endRemoveRows()
    
    xǁEntryListModelǁremove_item__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁremove_item__mutmut_1': xǁEntryListModelǁremove_item__mutmut_1, 
        'xǁEntryListModelǁremove_item__mutmut_2': xǁEntryListModelǁremove_item__mutmut_2, 
        'xǁEntryListModelǁremove_item__mutmut_3': xǁEntryListModelǁremove_item__mutmut_3, 
        'xǁEntryListModelǁremove_item__mutmut_4': xǁEntryListModelǁremove_item__mutmut_4, 
        'xǁEntryListModelǁremove_item__mutmut_5': xǁEntryListModelǁremove_item__mutmut_5, 
        'xǁEntryListModelǁremove_item__mutmut_6': xǁEntryListModelǁremove_item__mutmut_6, 
        'xǁEntryListModelǁremove_item__mutmut_7': xǁEntryListModelǁremove_item__mutmut_7, 
        'xǁEntryListModelǁremove_item__mutmut_8': xǁEntryListModelǁremove_item__mutmut_8, 
        'xǁEntryListModelǁremove_item__mutmut_9': xǁEntryListModelǁremove_item__mutmut_9, 
        'xǁEntryListModelǁremove_item__mutmut_10': xǁEntryListModelǁremove_item__mutmut_10, 
        'xǁEntryListModelǁremove_item__mutmut_11': xǁEntryListModelǁremove_item__mutmut_11
    }
    xǁEntryListModelǁremove_item__mutmut_orig.__name__ = 'xǁEntryListModelǁremove_item'

    def delete_duplicates(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁdelete_duplicates__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁdelete_duplicates__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁdelete_duplicates__mutmut_orig(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = set()
        unique_entries: list[ListItem] = []

        for item in self.entries:
            text_val = item.text
            color_val = item.color
            time_val = item._cache.get(-1)

            identifier = (text_val, color_val, time_val)

            if identifier not in seen_identifiers:
                unique_entries.append(item)
                seen_identifiers.add(identifier)

        self.beginResetModel()
        self.entries = unique_entries
        self.endResetModel()

    def xǁEntryListModelǁdelete_duplicates__mutmut_1(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = None
        unique_entries: list[ListItem] = []

        for item in self.entries:
            text_val = item.text
            color_val = item.color
            time_val = item._cache.get(-1)

            identifier = (text_val, color_val, time_val)

            if identifier not in seen_identifiers:
                unique_entries.append(item)
                seen_identifiers.add(identifier)

        self.beginResetModel()
        self.entries = unique_entries
        self.endResetModel()

    def xǁEntryListModelǁdelete_duplicates__mutmut_2(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = set()
        unique_entries: list[ListItem] = None

        for item in self.entries:
            text_val = item.text
            color_val = item.color
            time_val = item._cache.get(-1)

            identifier = (text_val, color_val, time_val)

            if identifier not in seen_identifiers:
                unique_entries.append(item)
                seen_identifiers.add(identifier)

        self.beginResetModel()
        self.entries = unique_entries
        self.endResetModel()

    def xǁEntryListModelǁdelete_duplicates__mutmut_3(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = set()
        unique_entries: list[ListItem] = []

        for item in self.entries:
            text_val = None
            color_val = item.color
            time_val = item._cache.get(-1)

            identifier = (text_val, color_val, time_val)

            if identifier not in seen_identifiers:
                unique_entries.append(item)
                seen_identifiers.add(identifier)

        self.beginResetModel()
        self.entries = unique_entries
        self.endResetModel()

    def xǁEntryListModelǁdelete_duplicates__mutmut_4(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = set()
        unique_entries: list[ListItem] = []

        for item in self.entries:
            text_val = item.text
            color_val = None
            time_val = item._cache.get(-1)

            identifier = (text_val, color_val, time_val)

            if identifier not in seen_identifiers:
                unique_entries.append(item)
                seen_identifiers.add(identifier)

        self.beginResetModel()
        self.entries = unique_entries
        self.endResetModel()

    def xǁEntryListModelǁdelete_duplicates__mutmut_5(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = set()
        unique_entries: list[ListItem] = []

        for item in self.entries:
            text_val = item.text
            color_val = item.color
            time_val = None

            identifier = (text_val, color_val, time_val)

            if identifier not in seen_identifiers:
                unique_entries.append(item)
                seen_identifiers.add(identifier)

        self.beginResetModel()
        self.entries = unique_entries
        self.endResetModel()

    def xǁEntryListModelǁdelete_duplicates__mutmut_6(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = set()
        unique_entries: list[ListItem] = []

        for item in self.entries:
            text_val = item.text
            color_val = item.color
            time_val = item._cache.get(None)

            identifier = (text_val, color_val, time_val)

            if identifier not in seen_identifiers:
                unique_entries.append(item)
                seen_identifiers.add(identifier)

        self.beginResetModel()
        self.entries = unique_entries
        self.endResetModel()

    def xǁEntryListModelǁdelete_duplicates__mutmut_7(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = set()
        unique_entries: list[ListItem] = []

        for item in self.entries:
            text_val = item.text
            color_val = item.color
            time_val = item._cache.get(+1)

            identifier = (text_val, color_val, time_val)

            if identifier not in seen_identifiers:
                unique_entries.append(item)
                seen_identifiers.add(identifier)

        self.beginResetModel()
        self.entries = unique_entries
        self.endResetModel()

    def xǁEntryListModelǁdelete_duplicates__mutmut_8(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = set()
        unique_entries: list[ListItem] = []

        for item in self.entries:
            text_val = item.text
            color_val = item.color
            time_val = item._cache.get(-2)

            identifier = (text_val, color_val, time_val)

            if identifier not in seen_identifiers:
                unique_entries.append(item)
                seen_identifiers.add(identifier)

        self.beginResetModel()
        self.entries = unique_entries
        self.endResetModel()

    def xǁEntryListModelǁdelete_duplicates__mutmut_9(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = set()
        unique_entries: list[ListItem] = []

        for item in self.entries:
            text_val = item.text
            color_val = item.color
            time_val = item._cache.get(-1)

            identifier = None

            if identifier not in seen_identifiers:
                unique_entries.append(item)
                seen_identifiers.add(identifier)

        self.beginResetModel()
        self.entries = unique_entries
        self.endResetModel()

    def xǁEntryListModelǁdelete_duplicates__mutmut_10(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = set()
        unique_entries: list[ListItem] = []

        for item in self.entries:
            text_val = item.text
            color_val = item.color
            time_val = item._cache.get(-1)

            identifier = (text_val, color_val, time_val)

            if identifier in seen_identifiers:
                unique_entries.append(item)
                seen_identifiers.add(identifier)

        self.beginResetModel()
        self.entries = unique_entries
        self.endResetModel()

    def xǁEntryListModelǁdelete_duplicates__mutmut_11(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = set()
        unique_entries: list[ListItem] = []

        for item in self.entries:
            text_val = item.text
            color_val = item.color
            time_val = item._cache.get(-1)

            identifier = (text_val, color_val, time_val)

            if identifier not in seen_identifiers:
                unique_entries.append(None)
                seen_identifiers.add(identifier)

        self.beginResetModel()
        self.entries = unique_entries
        self.endResetModel()

    def xǁEntryListModelǁdelete_duplicates__mutmut_12(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = set()
        unique_entries: list[ListItem] = []

        for item in self.entries:
            text_val = item.text
            color_val = item.color
            time_val = item._cache.get(-1)

            identifier = (text_val, color_val, time_val)

            if identifier not in seen_identifiers:
                unique_entries.append(item)
                seen_identifiers.add(None)

        self.beginResetModel()
        self.entries = unique_entries
        self.endResetModel()

    def xǁEntryListModelǁdelete_duplicates__mutmut_13(self) -> None:
        """
        Removes items that have identical text, color, and
        last time entry (get(-1)).
        """
        seen_identifiers: set[tuple[str, str, str]] = set()
        unique_entries: list[ListItem] = []

        for item in self.entries:
            text_val = item.text
            color_val = item.color
            time_val = item._cache.get(-1)

            identifier = (text_val, color_val, time_val)

            if identifier not in seen_identifiers:
                unique_entries.append(item)
                seen_identifiers.add(identifier)

        self.beginResetModel()
        self.entries = None
        self.endResetModel()
    
    xǁEntryListModelǁdelete_duplicates__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁdelete_duplicates__mutmut_1': xǁEntryListModelǁdelete_duplicates__mutmut_1, 
        'xǁEntryListModelǁdelete_duplicates__mutmut_2': xǁEntryListModelǁdelete_duplicates__mutmut_2, 
        'xǁEntryListModelǁdelete_duplicates__mutmut_3': xǁEntryListModelǁdelete_duplicates__mutmut_3, 
        'xǁEntryListModelǁdelete_duplicates__mutmut_4': xǁEntryListModelǁdelete_duplicates__mutmut_4, 
        'xǁEntryListModelǁdelete_duplicates__mutmut_5': xǁEntryListModelǁdelete_duplicates__mutmut_5, 
        'xǁEntryListModelǁdelete_duplicates__mutmut_6': xǁEntryListModelǁdelete_duplicates__mutmut_6, 
        'xǁEntryListModelǁdelete_duplicates__mutmut_7': xǁEntryListModelǁdelete_duplicates__mutmut_7, 
        'xǁEntryListModelǁdelete_duplicates__mutmut_8': xǁEntryListModelǁdelete_duplicates__mutmut_8, 
        'xǁEntryListModelǁdelete_duplicates__mutmut_9': xǁEntryListModelǁdelete_duplicates__mutmut_9, 
        'xǁEntryListModelǁdelete_duplicates__mutmut_10': xǁEntryListModelǁdelete_duplicates__mutmut_10, 
        'xǁEntryListModelǁdelete_duplicates__mutmut_11': xǁEntryListModelǁdelete_duplicates__mutmut_11, 
        'xǁEntryListModelǁdelete_duplicates__mutmut_12': xǁEntryListModelǁdelete_duplicates__mutmut_12, 
        'xǁEntryListModelǁdelete_duplicates__mutmut_13': xǁEntryListModelǁdelete_duplicates__mutmut_13
    }
    xǁEntryListModelǁdelete_duplicates__mutmut_orig.__name__ = 'xǁEntryListModelǁdelete_duplicates'

    def clear(self) -> None:
        """Clear model rows"""
        self.beginResetModel()
        self.entries.clear()
        self.endResetModel()

    def add_item(self, item: ListItem) -> None:
        args = [item]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁadd_item__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁadd_item__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁadd_item__mutmut_orig(self, item: ListItem) -> None:
        """Adds one row item to the model"""
        row = len(self.entries)
        self.beginInsertRows(QtCore.QModelIndex(), row, row)
        self.entries.append(item)
        self.endInsertRows()

    def xǁEntryListModelǁadd_item__mutmut_1(self, item: ListItem) -> None:
        """Adds one row item to the model"""
        row = None
        self.beginInsertRows(QtCore.QModelIndex(), row, row)
        self.entries.append(item)
        self.endInsertRows()

    def xǁEntryListModelǁadd_item__mutmut_2(self, item: ListItem) -> None:
        """Adds one row item to the model"""
        row = len(self.entries)
        self.beginInsertRows(None, row, row)
        self.entries.append(item)
        self.endInsertRows()

    def xǁEntryListModelǁadd_item__mutmut_3(self, item: ListItem) -> None:
        """Adds one row item to the model"""
        row = len(self.entries)
        self.beginInsertRows(QtCore.QModelIndex(), None, row)
        self.entries.append(item)
        self.endInsertRows()

    def xǁEntryListModelǁadd_item__mutmut_4(self, item: ListItem) -> None:
        """Adds one row item to the model"""
        row = len(self.entries)
        self.beginInsertRows(QtCore.QModelIndex(), row, None)
        self.entries.append(item)
        self.endInsertRows()

    def xǁEntryListModelǁadd_item__mutmut_5(self, item: ListItem) -> None:
        """Adds one row item to the model"""
        row = len(self.entries)
        self.beginInsertRows(row, row)
        self.entries.append(item)
        self.endInsertRows()

    def xǁEntryListModelǁadd_item__mutmut_6(self, item: ListItem) -> None:
        """Adds one row item to the model"""
        row = len(self.entries)
        self.beginInsertRows(QtCore.QModelIndex(), row)
        self.entries.append(item)
        self.endInsertRows()

    def xǁEntryListModelǁadd_item__mutmut_7(self, item: ListItem) -> None:
        """Adds one row item to the model"""
        row = len(self.entries)
        self.beginInsertRows(QtCore.QModelIndex(), row, )
        self.entries.append(item)
        self.endInsertRows()

    def xǁEntryListModelǁadd_item__mutmut_8(self, item: ListItem) -> None:
        """Adds one row item to the model"""
        row = len(self.entries)
        self.beginInsertRows(QtCore.QModelIndex(), row, row)
        self.entries.append(None)
        self.endInsertRows()
    
    xǁEntryListModelǁadd_item__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁadd_item__mutmut_1': xǁEntryListModelǁadd_item__mutmut_1, 
        'xǁEntryListModelǁadd_item__mutmut_2': xǁEntryListModelǁadd_item__mutmut_2, 
        'xǁEntryListModelǁadd_item__mutmut_3': xǁEntryListModelǁadd_item__mutmut_3, 
        'xǁEntryListModelǁadd_item__mutmut_4': xǁEntryListModelǁadd_item__mutmut_4, 
        'xǁEntryListModelǁadd_item__mutmut_5': xǁEntryListModelǁadd_item__mutmut_5, 
        'xǁEntryListModelǁadd_item__mutmut_6': xǁEntryListModelǁadd_item__mutmut_6, 
        'xǁEntryListModelǁadd_item__mutmut_7': xǁEntryListModelǁadd_item__mutmut_7, 
        'xǁEntryListModelǁadd_item__mutmut_8': xǁEntryListModelǁadd_item__mutmut_8
    }
    xǁEntryListModelǁadd_item__mutmut_orig.__name__ = 'xǁEntryListModelǁadd_item'

    def remove_item_by_text(self, text: str) -> bool:
        args = [text]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁremove_item_by_text__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁremove_item_by_text__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁremove_item_by_text__mutmut_orig(self, text: str) -> bool:
        """Remove item from model by its text value.

        Args:
            text: The text value of the item to remove.

        Returns:
            True if item was found and removed, False otherwise.
        """
        for i, item in enumerate(self.entries):
            if item.text == text:
                self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                self.entries.pop(i)
                self.endRemoveRows()
                return True
        return False

    def xǁEntryListModelǁremove_item_by_text__mutmut_1(self, text: str) -> bool:
        """Remove item from model by its text value.

        Args:
            text: The text value of the item to remove.

        Returns:
            True if item was found and removed, False otherwise.
        """
        for i, item in enumerate(None):
            if item.text == text:
                self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                self.entries.pop(i)
                self.endRemoveRows()
                return True
        return False

    def xǁEntryListModelǁremove_item_by_text__mutmut_2(self, text: str) -> bool:
        """Remove item from model by its text value.

        Args:
            text: The text value of the item to remove.

        Returns:
            True if item was found and removed, False otherwise.
        """
        for i, item in enumerate(self.entries):
            if item.text != text:
                self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                self.entries.pop(i)
                self.endRemoveRows()
                return True
        return False

    def xǁEntryListModelǁremove_item_by_text__mutmut_3(self, text: str) -> bool:
        """Remove item from model by its text value.

        Args:
            text: The text value of the item to remove.

        Returns:
            True if item was found and removed, False otherwise.
        """
        for i, item in enumerate(self.entries):
            if item.text == text:
                self.beginRemoveRows(None, i, i)
                self.entries.pop(i)
                self.endRemoveRows()
                return True
        return False

    def xǁEntryListModelǁremove_item_by_text__mutmut_4(self, text: str) -> bool:
        """Remove item from model by its text value.

        Args:
            text: The text value of the item to remove.

        Returns:
            True if item was found and removed, False otherwise.
        """
        for i, item in enumerate(self.entries):
            if item.text == text:
                self.beginRemoveRows(QtCore.QModelIndex(), None, i)
                self.entries.pop(i)
                self.endRemoveRows()
                return True
        return False

    def xǁEntryListModelǁremove_item_by_text__mutmut_5(self, text: str) -> bool:
        """Remove item from model by its text value.

        Args:
            text: The text value of the item to remove.

        Returns:
            True if item was found and removed, False otherwise.
        """
        for i, item in enumerate(self.entries):
            if item.text == text:
                self.beginRemoveRows(QtCore.QModelIndex(), i, None)
                self.entries.pop(i)
                self.endRemoveRows()
                return True
        return False

    def xǁEntryListModelǁremove_item_by_text__mutmut_6(self, text: str) -> bool:
        """Remove item from model by its text value.

        Args:
            text: The text value of the item to remove.

        Returns:
            True if item was found and removed, False otherwise.
        """
        for i, item in enumerate(self.entries):
            if item.text == text:
                self.beginRemoveRows(i, i)
                self.entries.pop(i)
                self.endRemoveRows()
                return True
        return False

    def xǁEntryListModelǁremove_item_by_text__mutmut_7(self, text: str) -> bool:
        """Remove item from model by its text value.

        Args:
            text: The text value of the item to remove.

        Returns:
            True if item was found and removed, False otherwise.
        """
        for i, item in enumerate(self.entries):
            if item.text == text:
                self.beginRemoveRows(QtCore.QModelIndex(), i)
                self.entries.pop(i)
                self.endRemoveRows()
                return True
        return False

    def xǁEntryListModelǁremove_item_by_text__mutmut_8(self, text: str) -> bool:
        """Remove item from model by its text value.

        Args:
            text: The text value of the item to remove.

        Returns:
            True if item was found and removed, False otherwise.
        """
        for i, item in enumerate(self.entries):
            if item.text == text:
                self.beginRemoveRows(QtCore.QModelIndex(), i, )
                self.entries.pop(i)
                self.endRemoveRows()
                return True
        return False

    def xǁEntryListModelǁremove_item_by_text__mutmut_9(self, text: str) -> bool:
        """Remove item from model by its text value.

        Args:
            text: The text value of the item to remove.

        Returns:
            True if item was found and removed, False otherwise.
        """
        for i, item in enumerate(self.entries):
            if item.text == text:
                self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                self.entries.pop(None)
                self.endRemoveRows()
                return True
        return False

    def xǁEntryListModelǁremove_item_by_text__mutmut_10(self, text: str) -> bool:
        """Remove item from model by its text value.

        Args:
            text: The text value of the item to remove.

        Returns:
            True if item was found and removed, False otherwise.
        """
        for i, item in enumerate(self.entries):
            if item.text == text:
                self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                self.entries.pop(i)
                self.endRemoveRows()
                return False
        return False

    def xǁEntryListModelǁremove_item_by_text__mutmut_11(self, text: str) -> bool:
        """Remove item from model by its text value.

        Args:
            text: The text value of the item to remove.

        Returns:
            True if item was found and removed, False otherwise.
        """
        for i, item in enumerate(self.entries):
            if item.text == text:
                self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                self.entries.pop(i)
                self.endRemoveRows()
                return True
        return True
    
    xǁEntryListModelǁremove_item_by_text__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁremove_item_by_text__mutmut_1': xǁEntryListModelǁremove_item_by_text__mutmut_1, 
        'xǁEntryListModelǁremove_item_by_text__mutmut_2': xǁEntryListModelǁremove_item_by_text__mutmut_2, 
        'xǁEntryListModelǁremove_item_by_text__mutmut_3': xǁEntryListModelǁremove_item_by_text__mutmut_3, 
        'xǁEntryListModelǁremove_item_by_text__mutmut_4': xǁEntryListModelǁremove_item_by_text__mutmut_4, 
        'xǁEntryListModelǁremove_item_by_text__mutmut_5': xǁEntryListModelǁremove_item_by_text__mutmut_5, 
        'xǁEntryListModelǁremove_item_by_text__mutmut_6': xǁEntryListModelǁremove_item_by_text__mutmut_6, 
        'xǁEntryListModelǁremove_item_by_text__mutmut_7': xǁEntryListModelǁremove_item_by_text__mutmut_7, 
        'xǁEntryListModelǁremove_item_by_text__mutmut_8': xǁEntryListModelǁremove_item_by_text__mutmut_8, 
        'xǁEntryListModelǁremove_item_by_text__mutmut_9': xǁEntryListModelǁremove_item_by_text__mutmut_9, 
        'xǁEntryListModelǁremove_item_by_text__mutmut_10': xǁEntryListModelǁremove_item_by_text__mutmut_10, 
        'xǁEntryListModelǁremove_item_by_text__mutmut_11': xǁEntryListModelǁremove_item_by_text__mutmut_11
    }
    xǁEntryListModelǁremove_item_by_text__mutmut_orig.__name__ = 'xǁEntryListModelǁremove_item_by_text'

    def insert_item(self, position: int, item: ListItem) -> None:
        args = [position, item]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁinsert_item__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁinsert_item__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁinsert_item__mutmut_orig(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(position, len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_1(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = None
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_2(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(None, min(position, len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_3(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, None)
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_4(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(min(position, len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_5(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, )
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_6(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(1, min(position, len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_7(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(None, len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_8(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(position, None))
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_9(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_10(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(position, ))
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_11(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(position, len(self.entries)))
        self.beginInsertRows(None, position, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_12(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(position, len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), None, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_13(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(position, len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), position, None)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_14(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(position, len(self.entries)))
        self.beginInsertRows(position, position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_15(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(position, len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), position)
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_16(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(position, len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), position, )
        self.entries.insert(position, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_17(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(position, len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(None, item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_18(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(position, len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(position, None)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_19(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(position, len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(item)
        self.endInsertRows()

    def xǁEntryListModelǁinsert_item__mutmut_20(self, position: int, item: ListItem) -> None:
        """Insert item at a specific position in the model."""
        position = max(0, min(position, len(self.entries)))
        self.beginInsertRows(QtCore.QModelIndex(), position, position)
        self.entries.insert(position, )
        self.endInsertRows()
    
    xǁEntryListModelǁinsert_item__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁinsert_item__mutmut_1': xǁEntryListModelǁinsert_item__mutmut_1, 
        'xǁEntryListModelǁinsert_item__mutmut_2': xǁEntryListModelǁinsert_item__mutmut_2, 
        'xǁEntryListModelǁinsert_item__mutmut_3': xǁEntryListModelǁinsert_item__mutmut_3, 
        'xǁEntryListModelǁinsert_item__mutmut_4': xǁEntryListModelǁinsert_item__mutmut_4, 
        'xǁEntryListModelǁinsert_item__mutmut_5': xǁEntryListModelǁinsert_item__mutmut_5, 
        'xǁEntryListModelǁinsert_item__mutmut_6': xǁEntryListModelǁinsert_item__mutmut_6, 
        'xǁEntryListModelǁinsert_item__mutmut_7': xǁEntryListModelǁinsert_item__mutmut_7, 
        'xǁEntryListModelǁinsert_item__mutmut_8': xǁEntryListModelǁinsert_item__mutmut_8, 
        'xǁEntryListModelǁinsert_item__mutmut_9': xǁEntryListModelǁinsert_item__mutmut_9, 
        'xǁEntryListModelǁinsert_item__mutmut_10': xǁEntryListModelǁinsert_item__mutmut_10, 
        'xǁEntryListModelǁinsert_item__mutmut_11': xǁEntryListModelǁinsert_item__mutmut_11, 
        'xǁEntryListModelǁinsert_item__mutmut_12': xǁEntryListModelǁinsert_item__mutmut_12, 
        'xǁEntryListModelǁinsert_item__mutmut_13': xǁEntryListModelǁinsert_item__mutmut_13, 
        'xǁEntryListModelǁinsert_item__mutmut_14': xǁEntryListModelǁinsert_item__mutmut_14, 
        'xǁEntryListModelǁinsert_item__mutmut_15': xǁEntryListModelǁinsert_item__mutmut_15, 
        'xǁEntryListModelǁinsert_item__mutmut_16': xǁEntryListModelǁinsert_item__mutmut_16, 
        'xǁEntryListModelǁinsert_item__mutmut_17': xǁEntryListModelǁinsert_item__mutmut_17, 
        'xǁEntryListModelǁinsert_item__mutmut_18': xǁEntryListModelǁinsert_item__mutmut_18, 
        'xǁEntryListModelǁinsert_item__mutmut_19': xǁEntryListModelǁinsert_item__mutmut_19, 
        'xǁEntryListModelǁinsert_item__mutmut_20': xǁEntryListModelǁinsert_item__mutmut_20
    }
    xǁEntryListModelǁinsert_item__mutmut_orig.__name__ = 'xǁEntryListModelǁinsert_item'

    def remove_item_at(self, position: int) -> bool:
        args = [position]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁremove_item_at__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁremove_item_at__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁremove_item_at__mutmut_orig(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        self.beginRemoveRows(QtCore.QModelIndex(), position, position)
        self.entries.pop(position)
        self.endRemoveRows()
        return True

    def xǁEntryListModelǁremove_item_at__mutmut_1(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position < 0 and position >= len(self.entries):
            return False
        self.beginRemoveRows(QtCore.QModelIndex(), position, position)
        self.entries.pop(position)
        self.endRemoveRows()
        return True

    def xǁEntryListModelǁremove_item_at__mutmut_2(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position <= 0 or position >= len(self.entries):
            return False
        self.beginRemoveRows(QtCore.QModelIndex(), position, position)
        self.entries.pop(position)
        self.endRemoveRows()
        return True

    def xǁEntryListModelǁremove_item_at__mutmut_3(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position < 1 or position >= len(self.entries):
            return False
        self.beginRemoveRows(QtCore.QModelIndex(), position, position)
        self.entries.pop(position)
        self.endRemoveRows()
        return True

    def xǁEntryListModelǁremove_item_at__mutmut_4(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position < 0 or position > len(self.entries):
            return False
        self.beginRemoveRows(QtCore.QModelIndex(), position, position)
        self.entries.pop(position)
        self.endRemoveRows()
        return True

    def xǁEntryListModelǁremove_item_at__mutmut_5(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return True
        self.beginRemoveRows(QtCore.QModelIndex(), position, position)
        self.entries.pop(position)
        self.endRemoveRows()
        return True

    def xǁEntryListModelǁremove_item_at__mutmut_6(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        self.beginRemoveRows(None, position, position)
        self.entries.pop(position)
        self.endRemoveRows()
        return True

    def xǁEntryListModelǁremove_item_at__mutmut_7(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        self.beginRemoveRows(QtCore.QModelIndex(), None, position)
        self.entries.pop(position)
        self.endRemoveRows()
        return True

    def xǁEntryListModelǁremove_item_at__mutmut_8(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        self.beginRemoveRows(QtCore.QModelIndex(), position, None)
        self.entries.pop(position)
        self.endRemoveRows()
        return True

    def xǁEntryListModelǁremove_item_at__mutmut_9(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        self.beginRemoveRows(position, position)
        self.entries.pop(position)
        self.endRemoveRows()
        return True

    def xǁEntryListModelǁremove_item_at__mutmut_10(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        self.beginRemoveRows(QtCore.QModelIndex(), position)
        self.entries.pop(position)
        self.endRemoveRows()
        return True

    def xǁEntryListModelǁremove_item_at__mutmut_11(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        self.beginRemoveRows(QtCore.QModelIndex(), position, )
        self.entries.pop(position)
        self.endRemoveRows()
        return True

    def xǁEntryListModelǁremove_item_at__mutmut_12(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        self.beginRemoveRows(QtCore.QModelIndex(), position, position)
        self.entries.pop(None)
        self.endRemoveRows()
        return True

    def xǁEntryListModelǁremove_item_at__mutmut_13(self, position: int) -> bool:
        """Remove item at a specific position.

        Returns:
            True if item was removed, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        self.beginRemoveRows(QtCore.QModelIndex(), position, position)
        self.entries.pop(position)
        self.endRemoveRows()
        return False
    
    xǁEntryListModelǁremove_item_at__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁremove_item_at__mutmut_1': xǁEntryListModelǁremove_item_at__mutmut_1, 
        'xǁEntryListModelǁremove_item_at__mutmut_2': xǁEntryListModelǁremove_item_at__mutmut_2, 
        'xǁEntryListModelǁremove_item_at__mutmut_3': xǁEntryListModelǁremove_item_at__mutmut_3, 
        'xǁEntryListModelǁremove_item_at__mutmut_4': xǁEntryListModelǁremove_item_at__mutmut_4, 
        'xǁEntryListModelǁremove_item_at__mutmut_5': xǁEntryListModelǁremove_item_at__mutmut_5, 
        'xǁEntryListModelǁremove_item_at__mutmut_6': xǁEntryListModelǁremove_item_at__mutmut_6, 
        'xǁEntryListModelǁremove_item_at__mutmut_7': xǁEntryListModelǁremove_item_at__mutmut_7, 
        'xǁEntryListModelǁremove_item_at__mutmut_8': xǁEntryListModelǁremove_item_at__mutmut_8, 
        'xǁEntryListModelǁremove_item_at__mutmut_9': xǁEntryListModelǁremove_item_at__mutmut_9, 
        'xǁEntryListModelǁremove_item_at__mutmut_10': xǁEntryListModelǁremove_item_at__mutmut_10, 
        'xǁEntryListModelǁremove_item_at__mutmut_11': xǁEntryListModelǁremove_item_at__mutmut_11, 
        'xǁEntryListModelǁremove_item_at__mutmut_12': xǁEntryListModelǁremove_item_at__mutmut_12, 
        'xǁEntryListModelǁremove_item_at__mutmut_13': xǁEntryListModelǁremove_item_at__mutmut_13
    }
    xǁEntryListModelǁremove_item_at__mutmut_orig.__name__ = 'xǁEntryListModelǁremove_item_at'

    def update_item_at(self, position: int, item: ListItem) -> bool:
        args = [position, item]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁupdate_item_at__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁupdate_item_at__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁupdate_item_at__mutmut_orig(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_1(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 and position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_2(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position <= 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_3(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 1 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_4(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position > len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_5(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return True
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_6(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = None
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_7(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = None
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_8(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = None
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_9(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = None
        idx = self.index(position)
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_10(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = None
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_11(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(None)
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_12(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.rindex(position)
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_13(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(None, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_14(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, None, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_15(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, idx, None)
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_16(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_17(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, [QtCore.Qt.ItemDataRole.UserRole])
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_18(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, idx, )
        return True

    def xǁEntryListModelǁupdate_item_at__mutmut_19(self, position: int, item: ListItem) -> bool:
        """Update an existing item's display data in-place.

        Copies visual fields (left_icon, right_text, right_icon) from
        *item* into the entry at *position* and emits ``dataChanged``.

        Returns:
            True if updated, False if position is out of range.
        """
        if position < 0 or position >= len(self.entries):
            return False
        existing = self.entries[position]
        existing.left_icon = item.left_icon
        existing.right_text = item.right_text
        existing.right_icon = item.right_icon
        idx = self.index(position)
        self.dataChanged.emit(idx, idx, [QtCore.Qt.ItemDataRole.UserRole])
        return False
    
    xǁEntryListModelǁupdate_item_at__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁupdate_item_at__mutmut_1': xǁEntryListModelǁupdate_item_at__mutmut_1, 
        'xǁEntryListModelǁupdate_item_at__mutmut_2': xǁEntryListModelǁupdate_item_at__mutmut_2, 
        'xǁEntryListModelǁupdate_item_at__mutmut_3': xǁEntryListModelǁupdate_item_at__mutmut_3, 
        'xǁEntryListModelǁupdate_item_at__mutmut_4': xǁEntryListModelǁupdate_item_at__mutmut_4, 
        'xǁEntryListModelǁupdate_item_at__mutmut_5': xǁEntryListModelǁupdate_item_at__mutmut_5, 
        'xǁEntryListModelǁupdate_item_at__mutmut_6': xǁEntryListModelǁupdate_item_at__mutmut_6, 
        'xǁEntryListModelǁupdate_item_at__mutmut_7': xǁEntryListModelǁupdate_item_at__mutmut_7, 
        'xǁEntryListModelǁupdate_item_at__mutmut_8': xǁEntryListModelǁupdate_item_at__mutmut_8, 
        'xǁEntryListModelǁupdate_item_at__mutmut_9': xǁEntryListModelǁupdate_item_at__mutmut_9, 
        'xǁEntryListModelǁupdate_item_at__mutmut_10': xǁEntryListModelǁupdate_item_at__mutmut_10, 
        'xǁEntryListModelǁupdate_item_at__mutmut_11': xǁEntryListModelǁupdate_item_at__mutmut_11, 
        'xǁEntryListModelǁupdate_item_at__mutmut_12': xǁEntryListModelǁupdate_item_at__mutmut_12, 
        'xǁEntryListModelǁupdate_item_at__mutmut_13': xǁEntryListModelǁupdate_item_at__mutmut_13, 
        'xǁEntryListModelǁupdate_item_at__mutmut_14': xǁEntryListModelǁupdate_item_at__mutmut_14, 
        'xǁEntryListModelǁupdate_item_at__mutmut_15': xǁEntryListModelǁupdate_item_at__mutmut_15, 
        'xǁEntryListModelǁupdate_item_at__mutmut_16': xǁEntryListModelǁupdate_item_at__mutmut_16, 
        'xǁEntryListModelǁupdate_item_at__mutmut_17': xǁEntryListModelǁupdate_item_at__mutmut_17, 
        'xǁEntryListModelǁupdate_item_at__mutmut_18': xǁEntryListModelǁupdate_item_at__mutmut_18, 
        'xǁEntryListModelǁupdate_item_at__mutmut_19': xǁEntryListModelǁupdate_item_at__mutmut_19
    }
    xǁEntryListModelǁupdate_item_at__mutmut_orig.__name__ = 'xǁEntryListModelǁupdate_item_at'

    def reconcile(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        args = [desired, key_fn]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁreconcile__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁreconcile__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁreconcile__mutmut_orig(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_1(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = None
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_2(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(None) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_3(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(None, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_4(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, None)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_5(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_6(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, )

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_7(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = None

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_8(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(None)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_9(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(None):
            if self._apply_desired_item(target_idx, desired_item, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_10(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(None, desired_item, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_11(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, None, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_12(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, None, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_13(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, current_map, None):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_14(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(desired_item, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_15(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, current_map, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_16(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, key_fn):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_17(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, current_map, ):
                current_map = self._current_key_map(key_fn)

    def xǁEntryListModelǁreconcile__mutmut_18(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, current_map, key_fn):
                current_map = None

    def xǁEntryListModelǁreconcile__mutmut_19(
        self,
        desired: list[ListItem],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Diff current entries against *desired* and apply minimal mutations.

        Uses *key_fn* to derive a unique identity string for each item.
        """
        desired_keys = {key_fn(d) for d in desired}
        self._remove_stale_entries(desired_keys, key_fn)

        current_map = self._current_key_map(key_fn)

        for target_idx, desired_item in enumerate(desired):
            if self._apply_desired_item(target_idx, desired_item, current_map, key_fn):
                current_map = self._current_key_map(None)
    
    xǁEntryListModelǁreconcile__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁreconcile__mutmut_1': xǁEntryListModelǁreconcile__mutmut_1, 
        'xǁEntryListModelǁreconcile__mutmut_2': xǁEntryListModelǁreconcile__mutmut_2, 
        'xǁEntryListModelǁreconcile__mutmut_3': xǁEntryListModelǁreconcile__mutmut_3, 
        'xǁEntryListModelǁreconcile__mutmut_4': xǁEntryListModelǁreconcile__mutmut_4, 
        'xǁEntryListModelǁreconcile__mutmut_5': xǁEntryListModelǁreconcile__mutmut_5, 
        'xǁEntryListModelǁreconcile__mutmut_6': xǁEntryListModelǁreconcile__mutmut_6, 
        'xǁEntryListModelǁreconcile__mutmut_7': xǁEntryListModelǁreconcile__mutmut_7, 
        'xǁEntryListModelǁreconcile__mutmut_8': xǁEntryListModelǁreconcile__mutmut_8, 
        'xǁEntryListModelǁreconcile__mutmut_9': xǁEntryListModelǁreconcile__mutmut_9, 
        'xǁEntryListModelǁreconcile__mutmut_10': xǁEntryListModelǁreconcile__mutmut_10, 
        'xǁEntryListModelǁreconcile__mutmut_11': xǁEntryListModelǁreconcile__mutmut_11, 
        'xǁEntryListModelǁreconcile__mutmut_12': xǁEntryListModelǁreconcile__mutmut_12, 
        'xǁEntryListModelǁreconcile__mutmut_13': xǁEntryListModelǁreconcile__mutmut_13, 
        'xǁEntryListModelǁreconcile__mutmut_14': xǁEntryListModelǁreconcile__mutmut_14, 
        'xǁEntryListModelǁreconcile__mutmut_15': xǁEntryListModelǁreconcile__mutmut_15, 
        'xǁEntryListModelǁreconcile__mutmut_16': xǁEntryListModelǁreconcile__mutmut_16, 
        'xǁEntryListModelǁreconcile__mutmut_17': xǁEntryListModelǁreconcile__mutmut_17, 
        'xǁEntryListModelǁreconcile__mutmut_18': xǁEntryListModelǁreconcile__mutmut_18, 
        'xǁEntryListModelǁreconcile__mutmut_19': xǁEntryListModelǁreconcile__mutmut_19
    }
    xǁEntryListModelǁreconcile__mutmut_orig.__name__ = 'xǁEntryListModelǁreconcile'

    def _remove_stale_entries(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        args = [desired_keys, key_fn]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁ_remove_stale_entries__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁ_remove_stale_entries__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_orig(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_1(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = None
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_2(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = None
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_3(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(None)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_4(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(2 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_5(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(None) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_6(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_7(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count != 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_8(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 1:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_9(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 or n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_10(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count >= n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_11(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing / 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_12(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 3 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_13(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing >= 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_14(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 5:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_15(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = None
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_16(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(None) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_17(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) not in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_18(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = None
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_19(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(None, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_20(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, None, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_21(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, None):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_22(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(-1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_23(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_24(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, ):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_25(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing + 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_26(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 2, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_27(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, +1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_28(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -2, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_29(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, +1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_30(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -2):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_31(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(None) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_32(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_33(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(None, i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_34(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), None, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_35(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, None)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_36(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(i, i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_37(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i)
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_38(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, )
                    self.entries.pop(i)
                    self.endRemoveRows()

    def xǁEntryListModelǁ_remove_stale_entries__mutmut_39(
        self,
        desired_keys: set[str],
        key_fn: typing.Callable[[ListItem], str],
    ) -> None:
        """Remove entries whose key is not in *desired_keys*."""
        n_existing = len(self.entries)
        stale_count = sum(1 for e in self.entries if key_fn(e) not in desired_keys)
        if stale_count == 0:
            return

        if stale_count > n_existing // 2 and n_existing > 4:
            keep = [e for e in self.entries if key_fn(e) in desired_keys]
            self.beginResetModel()
            self.entries[:] = keep
            self.endResetModel()
        else:
            for i in range(n_existing - 1, -1, -1):
                if key_fn(self.entries[i]) not in desired_keys:
                    self.beginRemoveRows(QtCore.QModelIndex(), i, i)
                    self.entries.pop(None)
                    self.endRemoveRows()
    
    xǁEntryListModelǁ_remove_stale_entries__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁ_remove_stale_entries__mutmut_1': xǁEntryListModelǁ_remove_stale_entries__mutmut_1, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_2': xǁEntryListModelǁ_remove_stale_entries__mutmut_2, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_3': xǁEntryListModelǁ_remove_stale_entries__mutmut_3, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_4': xǁEntryListModelǁ_remove_stale_entries__mutmut_4, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_5': xǁEntryListModelǁ_remove_stale_entries__mutmut_5, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_6': xǁEntryListModelǁ_remove_stale_entries__mutmut_6, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_7': xǁEntryListModelǁ_remove_stale_entries__mutmut_7, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_8': xǁEntryListModelǁ_remove_stale_entries__mutmut_8, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_9': xǁEntryListModelǁ_remove_stale_entries__mutmut_9, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_10': xǁEntryListModelǁ_remove_stale_entries__mutmut_10, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_11': xǁEntryListModelǁ_remove_stale_entries__mutmut_11, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_12': xǁEntryListModelǁ_remove_stale_entries__mutmut_12, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_13': xǁEntryListModelǁ_remove_stale_entries__mutmut_13, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_14': xǁEntryListModelǁ_remove_stale_entries__mutmut_14, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_15': xǁEntryListModelǁ_remove_stale_entries__mutmut_15, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_16': xǁEntryListModelǁ_remove_stale_entries__mutmut_16, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_17': xǁEntryListModelǁ_remove_stale_entries__mutmut_17, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_18': xǁEntryListModelǁ_remove_stale_entries__mutmut_18, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_19': xǁEntryListModelǁ_remove_stale_entries__mutmut_19, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_20': xǁEntryListModelǁ_remove_stale_entries__mutmut_20, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_21': xǁEntryListModelǁ_remove_stale_entries__mutmut_21, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_22': xǁEntryListModelǁ_remove_stale_entries__mutmut_22, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_23': xǁEntryListModelǁ_remove_stale_entries__mutmut_23, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_24': xǁEntryListModelǁ_remove_stale_entries__mutmut_24, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_25': xǁEntryListModelǁ_remove_stale_entries__mutmut_25, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_26': xǁEntryListModelǁ_remove_stale_entries__mutmut_26, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_27': xǁEntryListModelǁ_remove_stale_entries__mutmut_27, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_28': xǁEntryListModelǁ_remove_stale_entries__mutmut_28, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_29': xǁEntryListModelǁ_remove_stale_entries__mutmut_29, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_30': xǁEntryListModelǁ_remove_stale_entries__mutmut_30, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_31': xǁEntryListModelǁ_remove_stale_entries__mutmut_31, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_32': xǁEntryListModelǁ_remove_stale_entries__mutmut_32, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_33': xǁEntryListModelǁ_remove_stale_entries__mutmut_33, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_34': xǁEntryListModelǁ_remove_stale_entries__mutmut_34, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_35': xǁEntryListModelǁ_remove_stale_entries__mutmut_35, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_36': xǁEntryListModelǁ_remove_stale_entries__mutmut_36, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_37': xǁEntryListModelǁ_remove_stale_entries__mutmut_37, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_38': xǁEntryListModelǁ_remove_stale_entries__mutmut_38, 
        'xǁEntryListModelǁ_remove_stale_entries__mutmut_39': xǁEntryListModelǁ_remove_stale_entries__mutmut_39
    }
    xǁEntryListModelǁ_remove_stale_entries__mutmut_orig.__name__ = 'xǁEntryListModelǁ_remove_stale_entries'

    def _current_key_map(
        self, key_fn: typing.Callable[[ListItem], str]
    ) -> dict[str, int]:
        args = [key_fn]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁ_current_key_map__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁ_current_key_map__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁ_current_key_map__mutmut_orig(
        self, key_fn: typing.Callable[[ListItem], str]
    ) -> dict[str, int]:
        """Build a ``{key: index}`` map of current entries."""
        return {key_fn(entry): i for i, entry in enumerate(self.entries)}

    def xǁEntryListModelǁ_current_key_map__mutmut_1(
        self, key_fn: typing.Callable[[ListItem], str]
    ) -> dict[str, int]:
        """Build a ``{key: index}`` map of current entries."""
        return {key_fn(None): i for i, entry in enumerate(self.entries)}

    def xǁEntryListModelǁ_current_key_map__mutmut_2(
        self, key_fn: typing.Callable[[ListItem], str]
    ) -> dict[str, int]:
        """Build a ``{key: index}`` map of current entries."""
        return {key_fn(entry): i for i, entry in enumerate(None)}
    
    xǁEntryListModelǁ_current_key_map__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁ_current_key_map__mutmut_1': xǁEntryListModelǁ_current_key_map__mutmut_1, 
        'xǁEntryListModelǁ_current_key_map__mutmut_2': xǁEntryListModelǁ_current_key_map__mutmut_2
    }
    xǁEntryListModelǁ_current_key_map__mutmut_orig.__name__ = 'xǁEntryListModelǁ_current_key_map'

    def _apply_desired_item(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        args = [target_idx, desired_item, current_map, key_fn]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁ_apply_desired_item__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁ_apply_desired_item__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁ_apply_desired_item__mutmut_orig(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_1(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = None
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_2(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(None)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_3(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = None

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_4(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(None)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_5(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_6(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx != target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_7(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(None, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_8(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, None)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_9(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_10(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, )
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_11(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return True

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_12(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(None, current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_13(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), None, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_14(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, None)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_15(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_16(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_17(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, )
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_18(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(None)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_19(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(None, target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_20(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), None, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_21(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, None)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_22(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_23(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_24(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, )
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_25(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(None, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_26(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, None)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_27(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_28(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, )
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_29(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return False

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_30(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(None, target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_31(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), None, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_32(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, None)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_33(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_34(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_35(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, )
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_36(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(None, desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_37(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, None)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_38(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(desired_item)
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_39(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, )
        self.endInsertRows()
        return True

    def xǁEntryListModelǁ_apply_desired_item__mutmut_40(
        self,
        target_idx: int,
        desired_item: ListItem,
        current_map: dict[str, int],
        key_fn: typing.Callable[[ListItem], str],
    ) -> bool:
        """Insert, update, or reposition one item. Returns True if map is now stale."""
        key = key_fn(desired_item)
        current_idx = current_map.get(key)

        if current_idx is not None:
            if current_idx == target_idx:
                self.update_item_at(current_idx, desired_item)
                return False

            self.beginRemoveRows(QtCore.QModelIndex(), current_idx, current_idx)
            self.entries.pop(current_idx)
            self.endRemoveRows()

            self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
            self.entries.insert(target_idx, desired_item)
            self.endInsertRows()
            return True

        self.beginInsertRows(QtCore.QModelIndex(), target_idx, target_idx)
        self.entries.insert(target_idx, desired_item)
        self.endInsertRows()
        return False
    
    xǁEntryListModelǁ_apply_desired_item__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁ_apply_desired_item__mutmut_1': xǁEntryListModelǁ_apply_desired_item__mutmut_1, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_2': xǁEntryListModelǁ_apply_desired_item__mutmut_2, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_3': xǁEntryListModelǁ_apply_desired_item__mutmut_3, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_4': xǁEntryListModelǁ_apply_desired_item__mutmut_4, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_5': xǁEntryListModelǁ_apply_desired_item__mutmut_5, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_6': xǁEntryListModelǁ_apply_desired_item__mutmut_6, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_7': xǁEntryListModelǁ_apply_desired_item__mutmut_7, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_8': xǁEntryListModelǁ_apply_desired_item__mutmut_8, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_9': xǁEntryListModelǁ_apply_desired_item__mutmut_9, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_10': xǁEntryListModelǁ_apply_desired_item__mutmut_10, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_11': xǁEntryListModelǁ_apply_desired_item__mutmut_11, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_12': xǁEntryListModelǁ_apply_desired_item__mutmut_12, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_13': xǁEntryListModelǁ_apply_desired_item__mutmut_13, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_14': xǁEntryListModelǁ_apply_desired_item__mutmut_14, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_15': xǁEntryListModelǁ_apply_desired_item__mutmut_15, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_16': xǁEntryListModelǁ_apply_desired_item__mutmut_16, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_17': xǁEntryListModelǁ_apply_desired_item__mutmut_17, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_18': xǁEntryListModelǁ_apply_desired_item__mutmut_18, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_19': xǁEntryListModelǁ_apply_desired_item__mutmut_19, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_20': xǁEntryListModelǁ_apply_desired_item__mutmut_20, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_21': xǁEntryListModelǁ_apply_desired_item__mutmut_21, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_22': xǁEntryListModelǁ_apply_desired_item__mutmut_22, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_23': xǁEntryListModelǁ_apply_desired_item__mutmut_23, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_24': xǁEntryListModelǁ_apply_desired_item__mutmut_24, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_25': xǁEntryListModelǁ_apply_desired_item__mutmut_25, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_26': xǁEntryListModelǁ_apply_desired_item__mutmut_26, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_27': xǁEntryListModelǁ_apply_desired_item__mutmut_27, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_28': xǁEntryListModelǁ_apply_desired_item__mutmut_28, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_29': xǁEntryListModelǁ_apply_desired_item__mutmut_29, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_30': xǁEntryListModelǁ_apply_desired_item__mutmut_30, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_31': xǁEntryListModelǁ_apply_desired_item__mutmut_31, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_32': xǁEntryListModelǁ_apply_desired_item__mutmut_32, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_33': xǁEntryListModelǁ_apply_desired_item__mutmut_33, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_34': xǁEntryListModelǁ_apply_desired_item__mutmut_34, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_35': xǁEntryListModelǁ_apply_desired_item__mutmut_35, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_36': xǁEntryListModelǁ_apply_desired_item__mutmut_36, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_37': xǁEntryListModelǁ_apply_desired_item__mutmut_37, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_38': xǁEntryListModelǁ_apply_desired_item__mutmut_38, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_39': xǁEntryListModelǁ_apply_desired_item__mutmut_39, 
        'xǁEntryListModelǁ_apply_desired_item__mutmut_40': xǁEntryListModelǁ_apply_desired_item__mutmut_40
    }
    xǁEntryListModelǁ_apply_desired_item__mutmut_orig.__name__ = 'xǁEntryListModelǁ_apply_desired_item'

    def flags(self, index) -> QtCore.Qt.ItemFlag:
        args = [index]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁflags__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁflags__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁflags__mutmut_orig(self, index) -> QtCore.Qt.ItemFlag:
        """Models item flags, re-implemented method"""
        item = self.entries[index.row()]
        flags = QtCore.Qt.ItemFlag.ItemIsEnabled | QtCore.Qt.ItemFlag.ItemIsSelectable
        if item.not_clickable:
            return QtCore.Qt.ItemFlag.NoItemFlags
        if item.allow_check:
            flags |= QtCore.Qt.ItemFlag.ItemIsUserCheckable
        return flags

    def xǁEntryListModelǁflags__mutmut_1(self, index) -> QtCore.Qt.ItemFlag:
        """Models item flags, re-implemented method"""
        item = None
        flags = QtCore.Qt.ItemFlag.ItemIsEnabled | QtCore.Qt.ItemFlag.ItemIsSelectable
        if item.not_clickable:
            return QtCore.Qt.ItemFlag.NoItemFlags
        if item.allow_check:
            flags |= QtCore.Qt.ItemFlag.ItemIsUserCheckable
        return flags

    def xǁEntryListModelǁflags__mutmut_2(self, index) -> QtCore.Qt.ItemFlag:
        """Models item flags, re-implemented method"""
        item = self.entries[index.row()]
        flags = None
        if item.not_clickable:
            return QtCore.Qt.ItemFlag.NoItemFlags
        if item.allow_check:
            flags |= QtCore.Qt.ItemFlag.ItemIsUserCheckable
        return flags

    def xǁEntryListModelǁflags__mutmut_3(self, index) -> QtCore.Qt.ItemFlag:
        """Models item flags, re-implemented method"""
        item = self.entries[index.row()]
        flags = QtCore.Qt.ItemFlag.ItemIsEnabled & QtCore.Qt.ItemFlag.ItemIsSelectable
        if item.not_clickable:
            return QtCore.Qt.ItemFlag.NoItemFlags
        if item.allow_check:
            flags |= QtCore.Qt.ItemFlag.ItemIsUserCheckable
        return flags

    def xǁEntryListModelǁflags__mutmut_4(self, index) -> QtCore.Qt.ItemFlag:
        """Models item flags, re-implemented method"""
        item = self.entries[index.row()]
        flags = QtCore.Qt.ItemFlag.ItemIsEnabled | QtCore.Qt.ItemFlag.ItemIsSelectable
        if item.not_clickable:
            return QtCore.Qt.ItemFlag.NoItemFlags
        if item.allow_check:
            flags = QtCore.Qt.ItemFlag.ItemIsUserCheckable
        return flags

    def xǁEntryListModelǁflags__mutmut_5(self, index) -> QtCore.Qt.ItemFlag:
        """Models item flags, re-implemented method"""
        item = self.entries[index.row()]
        flags = QtCore.Qt.ItemFlag.ItemIsEnabled | QtCore.Qt.ItemFlag.ItemIsSelectable
        if item.not_clickable:
            return QtCore.Qt.ItemFlag.NoItemFlags
        if item.allow_check:
            flags &= QtCore.Qt.ItemFlag.ItemIsUserCheckable
        return flags
    
    xǁEntryListModelǁflags__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁflags__mutmut_1': xǁEntryListModelǁflags__mutmut_1, 
        'xǁEntryListModelǁflags__mutmut_2': xǁEntryListModelǁflags__mutmut_2, 
        'xǁEntryListModelǁflags__mutmut_3': xǁEntryListModelǁflags__mutmut_3, 
        'xǁEntryListModelǁflags__mutmut_4': xǁEntryListModelǁflags__mutmut_4, 
        'xǁEntryListModelǁflags__mutmut_5': xǁEntryListModelǁflags__mutmut_5
    }
    xǁEntryListModelǁflags__mutmut_orig.__name__ = 'xǁEntryListModelǁflags'

    def setData(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        args = [index, value, role]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁsetData__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁsetData__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁsetData__mutmut_orig(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_1(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_2(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return True
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_3(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role != EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_4(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = None
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_5(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = None
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_6(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(None, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_7(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, None, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_8(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, None)
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_9(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_10(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_11(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, )
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_12(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return False
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_13(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role != EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_14(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = None
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_15(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = None
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_16(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(None, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_17(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, None, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_18(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, None)
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_19(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_20(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_21(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, )
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_22(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return False
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_23(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role != EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_24(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = None
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_25(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = None
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_26(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(None, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_27(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, None, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_28(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, None)
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_29(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_30(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_31(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, )
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_32(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role != QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_33(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(None, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_34(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, None, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_35(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, None)
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_36(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_37(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_38(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, )
            return True
        return False

    def xǁEntryListModelǁsetData__mutmut_39(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return False
        return False

    def xǁEntryListModelǁsetData__mutmut_40(self, index: QtCore.QModelIndex, value: typing.Any, role: int) -> bool:
        """Set data for items, re-implemented method"""
        if not index.isValid():
            return False
        if role == EntryListModel.EnableRole:
            item = self.entries[index.row()]
            item.selected = value
            self.dataChanged.emit(index, index, [EntryListModel.EnableRole])
            return True
        if role == EntryListModel.NotificateRole:
            item = self.entries[index.row()]
            item.notificate = value
            self.dataChanged.emit(index, index, [EntryListModel.NotificateRole])
            return True
        if role == EntryListModel.ExpandRole:
            item = self.entries[index.row()]
            item.is_expanded = value
            self.layoutChanged.emit()
            self.dataChanged.emit(index, index, [EntryListModel.ExpandRole])
        if role == QtCore.Qt.ItemDataRole.UserRole:
            self.dataChanged.emit(index, index, [QtCore.Qt.ItemDataRole.UserRole])
            return True
        return True
    
    xǁEntryListModelǁsetData__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁsetData__mutmut_1': xǁEntryListModelǁsetData__mutmut_1, 
        'xǁEntryListModelǁsetData__mutmut_2': xǁEntryListModelǁsetData__mutmut_2, 
        'xǁEntryListModelǁsetData__mutmut_3': xǁEntryListModelǁsetData__mutmut_3, 
        'xǁEntryListModelǁsetData__mutmut_4': xǁEntryListModelǁsetData__mutmut_4, 
        'xǁEntryListModelǁsetData__mutmut_5': xǁEntryListModelǁsetData__mutmut_5, 
        'xǁEntryListModelǁsetData__mutmut_6': xǁEntryListModelǁsetData__mutmut_6, 
        'xǁEntryListModelǁsetData__mutmut_7': xǁEntryListModelǁsetData__mutmut_7, 
        'xǁEntryListModelǁsetData__mutmut_8': xǁEntryListModelǁsetData__mutmut_8, 
        'xǁEntryListModelǁsetData__mutmut_9': xǁEntryListModelǁsetData__mutmut_9, 
        'xǁEntryListModelǁsetData__mutmut_10': xǁEntryListModelǁsetData__mutmut_10, 
        'xǁEntryListModelǁsetData__mutmut_11': xǁEntryListModelǁsetData__mutmut_11, 
        'xǁEntryListModelǁsetData__mutmut_12': xǁEntryListModelǁsetData__mutmut_12, 
        'xǁEntryListModelǁsetData__mutmut_13': xǁEntryListModelǁsetData__mutmut_13, 
        'xǁEntryListModelǁsetData__mutmut_14': xǁEntryListModelǁsetData__mutmut_14, 
        'xǁEntryListModelǁsetData__mutmut_15': xǁEntryListModelǁsetData__mutmut_15, 
        'xǁEntryListModelǁsetData__mutmut_16': xǁEntryListModelǁsetData__mutmut_16, 
        'xǁEntryListModelǁsetData__mutmut_17': xǁEntryListModelǁsetData__mutmut_17, 
        'xǁEntryListModelǁsetData__mutmut_18': xǁEntryListModelǁsetData__mutmut_18, 
        'xǁEntryListModelǁsetData__mutmut_19': xǁEntryListModelǁsetData__mutmut_19, 
        'xǁEntryListModelǁsetData__mutmut_20': xǁEntryListModelǁsetData__mutmut_20, 
        'xǁEntryListModelǁsetData__mutmut_21': xǁEntryListModelǁsetData__mutmut_21, 
        'xǁEntryListModelǁsetData__mutmut_22': xǁEntryListModelǁsetData__mutmut_22, 
        'xǁEntryListModelǁsetData__mutmut_23': xǁEntryListModelǁsetData__mutmut_23, 
        'xǁEntryListModelǁsetData__mutmut_24': xǁEntryListModelǁsetData__mutmut_24, 
        'xǁEntryListModelǁsetData__mutmut_25': xǁEntryListModelǁsetData__mutmut_25, 
        'xǁEntryListModelǁsetData__mutmut_26': xǁEntryListModelǁsetData__mutmut_26, 
        'xǁEntryListModelǁsetData__mutmut_27': xǁEntryListModelǁsetData__mutmut_27, 
        'xǁEntryListModelǁsetData__mutmut_28': xǁEntryListModelǁsetData__mutmut_28, 
        'xǁEntryListModelǁsetData__mutmut_29': xǁEntryListModelǁsetData__mutmut_29, 
        'xǁEntryListModelǁsetData__mutmut_30': xǁEntryListModelǁsetData__mutmut_30, 
        'xǁEntryListModelǁsetData__mutmut_31': xǁEntryListModelǁsetData__mutmut_31, 
        'xǁEntryListModelǁsetData__mutmut_32': xǁEntryListModelǁsetData__mutmut_32, 
        'xǁEntryListModelǁsetData__mutmut_33': xǁEntryListModelǁsetData__mutmut_33, 
        'xǁEntryListModelǁsetData__mutmut_34': xǁEntryListModelǁsetData__mutmut_34, 
        'xǁEntryListModelǁsetData__mutmut_35': xǁEntryListModelǁsetData__mutmut_35, 
        'xǁEntryListModelǁsetData__mutmut_36': xǁEntryListModelǁsetData__mutmut_36, 
        'xǁEntryListModelǁsetData__mutmut_37': xǁEntryListModelǁsetData__mutmut_37, 
        'xǁEntryListModelǁsetData__mutmut_38': xǁEntryListModelǁsetData__mutmut_38, 
        'xǁEntryListModelǁsetData__mutmut_39': xǁEntryListModelǁsetData__mutmut_39, 
        'xǁEntryListModelǁsetData__mutmut_40': xǁEntryListModelǁsetData__mutmut_40
    }
    xǁEntryListModelǁsetData__mutmut_orig.__name__ = 'xǁEntryListModelǁsetData'

    def data(self, index: QtCore.QModelIndex, role: int) -> typing.Any:
        args = [index, role]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryListModelǁdata__mutmut_orig'), object.__getattribute__(self, 'xǁEntryListModelǁdata__mutmut_mutants'), args, kwargs, self)

    def xǁEntryListModelǁdata__mutmut_orig(self, index: QtCore.QModelIndex, role: int) -> typing.Any:
        """Gets item data, re-implemented method"""
        if not index.isValid():
            return None
        item: ListItem = self.entries[index.row()]
        if role == EntryListModel.EnableRole:
            return item.selected
        if role == EntryListModel.NotificateRole:
            return item.notificate
        if role == EntryListModel.ExpandRole:
            return item.is_expanded
        if role == QtCore.Qt.ItemDataRole.UserRole:
            return item
        return None

    def xǁEntryListModelǁdata__mutmut_1(self, index: QtCore.QModelIndex, role: int) -> typing.Any:
        """Gets item data, re-implemented method"""
        if index.isValid():
            return None
        item: ListItem = self.entries[index.row()]
        if role == EntryListModel.EnableRole:
            return item.selected
        if role == EntryListModel.NotificateRole:
            return item.notificate
        if role == EntryListModel.ExpandRole:
            return item.is_expanded
        if role == QtCore.Qt.ItemDataRole.UserRole:
            return item
        return None

    def xǁEntryListModelǁdata__mutmut_2(self, index: QtCore.QModelIndex, role: int) -> typing.Any:
        """Gets item data, re-implemented method"""
        if not index.isValid():
            return None
        item: ListItem = None
        if role == EntryListModel.EnableRole:
            return item.selected
        if role == EntryListModel.NotificateRole:
            return item.notificate
        if role == EntryListModel.ExpandRole:
            return item.is_expanded
        if role == QtCore.Qt.ItemDataRole.UserRole:
            return item
        return None

    def xǁEntryListModelǁdata__mutmut_3(self, index: QtCore.QModelIndex, role: int) -> typing.Any:
        """Gets item data, re-implemented method"""
        if not index.isValid():
            return None
        item: ListItem = self.entries[index.row()]
        if role != EntryListModel.EnableRole:
            return item.selected
        if role == EntryListModel.NotificateRole:
            return item.notificate
        if role == EntryListModel.ExpandRole:
            return item.is_expanded
        if role == QtCore.Qt.ItemDataRole.UserRole:
            return item
        return None

    def xǁEntryListModelǁdata__mutmut_4(self, index: QtCore.QModelIndex, role: int) -> typing.Any:
        """Gets item data, re-implemented method"""
        if not index.isValid():
            return None
        item: ListItem = self.entries[index.row()]
        if role == EntryListModel.EnableRole:
            return item.selected
        if role != EntryListModel.NotificateRole:
            return item.notificate
        if role == EntryListModel.ExpandRole:
            return item.is_expanded
        if role == QtCore.Qt.ItemDataRole.UserRole:
            return item
        return None

    def xǁEntryListModelǁdata__mutmut_5(self, index: QtCore.QModelIndex, role: int) -> typing.Any:
        """Gets item data, re-implemented method"""
        if not index.isValid():
            return None
        item: ListItem = self.entries[index.row()]
        if role == EntryListModel.EnableRole:
            return item.selected
        if role == EntryListModel.NotificateRole:
            return item.notificate
        if role != EntryListModel.ExpandRole:
            return item.is_expanded
        if role == QtCore.Qt.ItemDataRole.UserRole:
            return item
        return None

    def xǁEntryListModelǁdata__mutmut_6(self, index: QtCore.QModelIndex, role: int) -> typing.Any:
        """Gets item data, re-implemented method"""
        if not index.isValid():
            return None
        item: ListItem = self.entries[index.row()]
        if role == EntryListModel.EnableRole:
            return item.selected
        if role == EntryListModel.NotificateRole:
            return item.notificate
        if role == EntryListModel.ExpandRole:
            return item.is_expanded
        if role != QtCore.Qt.ItemDataRole.UserRole:
            return item
        return None
    
    xǁEntryListModelǁdata__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryListModelǁdata__mutmut_1': xǁEntryListModelǁdata__mutmut_1, 
        'xǁEntryListModelǁdata__mutmut_2': xǁEntryListModelǁdata__mutmut_2, 
        'xǁEntryListModelǁdata__mutmut_3': xǁEntryListModelǁdata__mutmut_3, 
        'xǁEntryListModelǁdata__mutmut_4': xǁEntryListModelǁdata__mutmut_4, 
        'xǁEntryListModelǁdata__mutmut_5': xǁEntryListModelǁdata__mutmut_5, 
        'xǁEntryListModelǁdata__mutmut_6': xǁEntryListModelǁdata__mutmut_6
    }
    xǁEntryListModelǁdata__mutmut_orig.__name__ = 'xǁEntryListModelǁdata'


class EntryDelegate(QtWidgets.QStyledItemDelegate):
    """Renders each item in the view model, provides user interaction to the items"""

    item_selected: typing.ClassVar[QtCore.pyqtSignal] = QtCore.pyqtSignal(
        ListItem, name="item-selected"
    )

    def __init__(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryDelegateǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁEntryDelegateǁ__init____mutmut_mutants'), args, kwargs, self)

    def xǁEntryDelegateǁ__init____mutmut_orig(self) -> None:
        """Initialise the delegate with a scaled-pixmap cache and default item height."""
        super().__init__()
        self.prev_index: int = 0
        self.height: int = 60
        self._scaled_cache: dict[tuple[int, int, int], QtGui.QPixmap] = {}

    def xǁEntryDelegateǁ__init____mutmut_1(self) -> None:
        """Initialise the delegate with a scaled-pixmap cache and default item height."""
        super().__init__()
        self.prev_index: int = None
        self.height: int = 60
        self._scaled_cache: dict[tuple[int, int, int], QtGui.QPixmap] = {}

    def xǁEntryDelegateǁ__init____mutmut_2(self) -> None:
        """Initialise the delegate with a scaled-pixmap cache and default item height."""
        super().__init__()
        self.prev_index: int = 1
        self.height: int = 60
        self._scaled_cache: dict[tuple[int, int, int], QtGui.QPixmap] = {}

    def xǁEntryDelegateǁ__init____mutmut_3(self) -> None:
        """Initialise the delegate with a scaled-pixmap cache and default item height."""
        super().__init__()
        self.prev_index: int = 0
        self.height: int = None
        self._scaled_cache: dict[tuple[int, int, int], QtGui.QPixmap] = {}

    def xǁEntryDelegateǁ__init____mutmut_4(self) -> None:
        """Initialise the delegate with a scaled-pixmap cache and default item height."""
        super().__init__()
        self.prev_index: int = 0
        self.height: int = 61
        self._scaled_cache: dict[tuple[int, int, int], QtGui.QPixmap] = {}

    def xǁEntryDelegateǁ__init____mutmut_5(self) -> None:
        """Initialise the delegate with a scaled-pixmap cache and default item height."""
        super().__init__()
        self.prev_index: int = 0
        self.height: int = 60
        self._scaled_cache: dict[tuple[int, int, int], QtGui.QPixmap] = None
    
    xǁEntryDelegateǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryDelegateǁ__init____mutmut_1': xǁEntryDelegateǁ__init____mutmut_1, 
        'xǁEntryDelegateǁ__init____mutmut_2': xǁEntryDelegateǁ__init____mutmut_2, 
        'xǁEntryDelegateǁ__init____mutmut_3': xǁEntryDelegateǁ__init____mutmut_3, 
        'xǁEntryDelegateǁ__init____mutmut_4': xǁEntryDelegateǁ__init____mutmut_4, 
        'xǁEntryDelegateǁ__init____mutmut_5': xǁEntryDelegateǁ__init____mutmut_5
    }
    xǁEntryDelegateǁ__init____mutmut_orig.__name__ = 'xǁEntryDelegateǁ__init__'

    def _get_scaled(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        args = [pixmap, size]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryDelegateǁ_get_scaled__mutmut_orig'), object.__getattribute__(self, 'xǁEntryDelegateǁ_get_scaled__mutmut_mutants'), args, kwargs, self)

    def xǁEntryDelegateǁ_get_scaled__mutmut_orig(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_1(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = None
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_2(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = None
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_3(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(None)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_4(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_5(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = None
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_6(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            None,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_7(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            None,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_8(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            None,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_9(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_10(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_11(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_12(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = None
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_13(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) >= 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_14(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 65:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_15(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = None
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_16(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(None)
            for k in keys[:32]:
                del self._scaled_cache[k]
        return scaled

    def xǁEntryDelegateǁ_get_scaled__mutmut_17(
        self,
        pixmap: QtGui.QPixmap,
        size: QtCore.QSize,
    ) -> QtGui.QPixmap:
        """Return *pixmap* scaled to *size*, using a cache to avoid
        re-scaling the same icon every paint frame.

        The cache key is (QPixmap.cacheKey(), width, height) which
        correctly invalidates when the source pixmap changes.
        """
        key = (pixmap.cacheKey(), size.width(), size.height())
        cached = self._scaled_cache.get(key)
        if cached is not None:
            return cached
        scaled = pixmap.scaled(
            size,
            QtCore.Qt.AspectRatioMode.KeepAspectRatio,
            QtCore.Qt.TransformationMode.SmoothTransformation,
        )
        self._scaled_cache[key] = scaled
        # Prevent unbounded growth — 64 entries covers all wifi
        # bar variants × protected/open × left/right icons easily.
        if len(self._scaled_cache) > 64:
            # Drop oldest half
            keys = list(self._scaled_cache)
            for k in keys[:33]:
                del self._scaled_cache[k]
        return scaled
    
    xǁEntryDelegateǁ_get_scaled__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryDelegateǁ_get_scaled__mutmut_1': xǁEntryDelegateǁ_get_scaled__mutmut_1, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_2': xǁEntryDelegateǁ_get_scaled__mutmut_2, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_3': xǁEntryDelegateǁ_get_scaled__mutmut_3, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_4': xǁEntryDelegateǁ_get_scaled__mutmut_4, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_5': xǁEntryDelegateǁ_get_scaled__mutmut_5, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_6': xǁEntryDelegateǁ_get_scaled__mutmut_6, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_7': xǁEntryDelegateǁ_get_scaled__mutmut_7, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_8': xǁEntryDelegateǁ_get_scaled__mutmut_8, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_9': xǁEntryDelegateǁ_get_scaled__mutmut_9, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_10': xǁEntryDelegateǁ_get_scaled__mutmut_10, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_11': xǁEntryDelegateǁ_get_scaled__mutmut_11, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_12': xǁEntryDelegateǁ_get_scaled__mutmut_12, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_13': xǁEntryDelegateǁ_get_scaled__mutmut_13, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_14': xǁEntryDelegateǁ_get_scaled__mutmut_14, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_15': xǁEntryDelegateǁ_get_scaled__mutmut_15, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_16': xǁEntryDelegateǁ_get_scaled__mutmut_16, 
        'xǁEntryDelegateǁ_get_scaled__mutmut_17': xǁEntryDelegateǁ_get_scaled__mutmut_17
    }
    xǁEntryDelegateǁ_get_scaled__mutmut_orig.__name__ = 'xǁEntryDelegateǁ_get_scaled'

    def clear(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryDelegateǁclear__mutmut_orig'), object.__getattribute__(self, 'xǁEntryDelegateǁclear__mutmut_mutants'), args, kwargs, self)

    def xǁEntryDelegateǁclear__mutmut_orig(self) -> None:
        """Clears delegate indexing"""
        self.prev_index = 0

    def xǁEntryDelegateǁclear__mutmut_1(self) -> None:
        """Clears delegate indexing"""
        self.prev_index = None

    def xǁEntryDelegateǁclear__mutmut_2(self) -> None:
        """Clears delegate indexing"""
        self.prev_index = 1
    
    xǁEntryDelegateǁclear__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryDelegateǁclear__mutmut_1': xǁEntryDelegateǁclear__mutmut_1, 
        'xǁEntryDelegateǁclear__mutmut_2': xǁEntryDelegateǁclear__mutmut_2
    }
    xǁEntryDelegateǁclear__mutmut_orig.__name__ = 'xǁEntryDelegateǁclear'

    def sizeHint(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        args = [option, index]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryDelegateǁsizeHint__mutmut_orig'), object.__getattribute__(self, 'xǁEntryDelegateǁsizeHint__mutmut_mutants'), args, kwargs, self)

    def xǁEntryDelegateǁsizeHint__mutmut_orig(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_1(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = None
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_2(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(None)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_3(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = None

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_4(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = None
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_5(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = None

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_6(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h / 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_7(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 1.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_8(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = None

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_9(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = None
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_10(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 11
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_11(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = None

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_12(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size - 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_13(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) - ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_14(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h / 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_15(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 1.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_16(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 9

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_17(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 or item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_18(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize >= 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_19(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 1 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_20(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize == option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_21(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = None
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_22(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(None)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_23(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(None)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_24(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = None
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_25(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(None)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_26(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = None

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_27(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 or item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_28(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize >= 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_29(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 1 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_30(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize == option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_31(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = None
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_32(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(None)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_33(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(None)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_34(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = None
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_35(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(None)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_36(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = None
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_37(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved = fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_38(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved -= fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_39(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) - 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_40(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(None) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_41(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 11

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_42(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved = ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_43(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved -= ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_44(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = None
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_45(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved + right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_46(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width + left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_47(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width <= 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_48(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 51:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_49(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = None

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_50(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 51

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_51(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = None

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_52(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(None)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_53(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = None

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_54(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width >= text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_55(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_56(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(None, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_57(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, None)

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_58(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_59(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, )

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_60(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(None))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_61(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height / 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_62(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 2.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_63(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = None

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_64(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            None,
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_65(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            None,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_66(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            None,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_67(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_68(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_69(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_70(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(None, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_71(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, None, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_72(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, None, 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_73(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), None),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_74(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_75(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_76(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_77(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), ),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_78(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(1, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_79(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 1, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_80(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(None), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_81(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 1),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_82(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft & QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_83(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = None
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_84(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(None, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_85(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, None)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_86(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_87(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, )
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_88(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() + 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_89(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 2)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_90(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = None
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_91(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height - 20
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_92(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 21
        return QtCore.QSize(target_width, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_93(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(None, int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_94(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, None)

    def xǁEntryDelegateǁsizeHint__mutmut_95(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(int(final_height * 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_96(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, )

    def xǁEntryDelegateǁsizeHint__mutmut_97(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(None))

    def xǁEntryDelegateǁsizeHint__mutmut_98(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height / 1.2))

    def xǁEntryDelegateǁsizeHint__mutmut_99(
        self, option: QtWidgets.QStyleOptionViewItem, index: QtCore.QModelIndex
    ):
        """
        Calculates size AND determines if expansion is needed.
        """
        item: ListItem = index.data(QtCore.Qt.ItemDataRole.UserRole)
        target_width = option.rect.width()

        base_h = item.height
        ellipse_size = base_h * 0.8

        right_reserved = base_h

        left_reserved = 10
        if item.left_icon:
            left_reserved = (base_h * 0.1) + ellipse_size + 8

        if item._lfontsize > 0 and item._lfontsize != option.font.pointSize():
            f = QtGui.QFont(option.font)
            f.setPointSize(item._lfontsize)
            fm = QtGui.QFontMetrics(f)
        else:
            fm = option.fontMetrics

        if item.right_text:
            if item._rfontsize > 0 and item._rfontsize != option.font.pointSize():
                fr = QtGui.QFont(option.font)
                fr.setPointSize(item._rfontsize)
                fmr = QtGui.QFontMetrics(fr)
            else:
                fmr = option.fontMetrics
            right_reserved += fmr.horizontalAdvance(item.right_text) + 10

        if item.right_icon:
            right_reserved += ellipse_size

        text_avail_width = target_width - left_reserved - right_reserved
        if text_avail_width < 50:
            text_avail_width = 50

        single_line_width = fm.horizontalAdvance(item.text)

        item.needs_expansion = single_line_width > text_avail_width

        if not item.is_expanded:
            return QtCore.QSize(target_width, int(item.height * 1.1))

        text_rect = fm.boundingRect(
            QtCore.QRect(0, 0, int(text_avail_width), 0),
            QtCore.Qt.AlignmentFlag.AlignLeft | QtCore.Qt.TextFlag.TextWordWrap,
            item.text,
        )

        final_height = max(item.height, text_rect.height() - 1)
        # Cache it
        item._cache[target_width] = final_height + 20
        return QtCore.QSize(target_width, int(final_height * 2.2))
    
    xǁEntryDelegateǁsizeHint__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryDelegateǁsizeHint__mutmut_1': xǁEntryDelegateǁsizeHint__mutmut_1, 
        'xǁEntryDelegateǁsizeHint__mutmut_2': xǁEntryDelegateǁsizeHint__mutmut_2, 
        'xǁEntryDelegateǁsizeHint__mutmut_3': xǁEntryDelegateǁsizeHint__mutmut_3, 
        'xǁEntryDelegateǁsizeHint__mutmut_4': xǁEntryDelegateǁsizeHint__mutmut_4, 
        'xǁEntryDelegateǁsizeHint__mutmut_5': xǁEntryDelegateǁsizeHint__mutmut_5, 
        'xǁEntryDelegateǁsizeHint__mutmut_6': xǁEntryDelegateǁsizeHint__mutmut_6, 
        'xǁEntryDelegateǁsizeHint__mutmut_7': xǁEntryDelegateǁsizeHint__mutmut_7, 
        'xǁEntryDelegateǁsizeHint__mutmut_8': xǁEntryDelegateǁsizeHint__mutmut_8, 
        'xǁEntryDelegateǁsizeHint__mutmut_9': xǁEntryDelegateǁsizeHint__mutmut_9, 
        'xǁEntryDelegateǁsizeHint__mutmut_10': xǁEntryDelegateǁsizeHint__mutmut_10, 
        'xǁEntryDelegateǁsizeHint__mutmut_11': xǁEntryDelegateǁsizeHint__mutmut_11, 
        'xǁEntryDelegateǁsizeHint__mutmut_12': xǁEntryDelegateǁsizeHint__mutmut_12, 
        'xǁEntryDelegateǁsizeHint__mutmut_13': xǁEntryDelegateǁsizeHint__mutmut_13, 
        'xǁEntryDelegateǁsizeHint__mutmut_14': xǁEntryDelegateǁsizeHint__mutmut_14, 
        'xǁEntryDelegateǁsizeHint__mutmut_15': xǁEntryDelegateǁsizeHint__mutmut_15, 
        'xǁEntryDelegateǁsizeHint__mutmut_16': xǁEntryDelegateǁsizeHint__mutmut_16, 
        'xǁEntryDelegateǁsizeHint__mutmut_17': xǁEntryDelegateǁsizeHint__mutmut_17, 
        'xǁEntryDelegateǁsizeHint__mutmut_18': xǁEntryDelegateǁsizeHint__mutmut_18, 
        'xǁEntryDelegateǁsizeHint__mutmut_19': xǁEntryDelegateǁsizeHint__mutmut_19, 
        'xǁEntryDelegateǁsizeHint__mutmut_20': xǁEntryDelegateǁsizeHint__mutmut_20, 
        'xǁEntryDelegateǁsizeHint__mutmut_21': xǁEntryDelegateǁsizeHint__mutmut_21, 
        'xǁEntryDelegateǁsizeHint__mutmut_22': xǁEntryDelegateǁsizeHint__mutmut_22, 
        'xǁEntryDelegateǁsizeHint__mutmut_23': xǁEntryDelegateǁsizeHint__mutmut_23, 
        'xǁEntryDelegateǁsizeHint__mutmut_24': xǁEntryDelegateǁsizeHint__mutmut_24, 
        'xǁEntryDelegateǁsizeHint__mutmut_25': xǁEntryDelegateǁsizeHint__mutmut_25, 
        'xǁEntryDelegateǁsizeHint__mutmut_26': xǁEntryDelegateǁsizeHint__mutmut_26, 
        'xǁEntryDelegateǁsizeHint__mutmut_27': xǁEntryDelegateǁsizeHint__mutmut_27, 
        'xǁEntryDelegateǁsizeHint__mutmut_28': xǁEntryDelegateǁsizeHint__mutmut_28, 
        'xǁEntryDelegateǁsizeHint__mutmut_29': xǁEntryDelegateǁsizeHint__mutmut_29, 
        'xǁEntryDelegateǁsizeHint__mutmut_30': xǁEntryDelegateǁsizeHint__mutmut_30, 
        'xǁEntryDelegateǁsizeHint__mutmut_31': xǁEntryDelegateǁsizeHint__mutmut_31, 
        'xǁEntryDelegateǁsizeHint__mutmut_32': xǁEntryDelegateǁsizeHint__mutmut_32, 
        'xǁEntryDelegateǁsizeHint__mutmut_33': xǁEntryDelegateǁsizeHint__mutmut_33, 
        'xǁEntryDelegateǁsizeHint__mutmut_34': xǁEntryDelegateǁsizeHint__mutmut_34, 
        'xǁEntryDelegateǁsizeHint__mutmut_35': xǁEntryDelegateǁsizeHint__mutmut_35, 
        'xǁEntryDelegateǁsizeHint__mutmut_36': xǁEntryDelegateǁsizeHint__mutmut_36, 
        'xǁEntryDelegateǁsizeHint__mutmut_37': xǁEntryDelegateǁsizeHint__mutmut_37, 
        'xǁEntryDelegateǁsizeHint__mutmut_38': xǁEntryDelegateǁsizeHint__mutmut_38, 
        'xǁEntryDelegateǁsizeHint__mutmut_39': xǁEntryDelegateǁsizeHint__mutmut_39, 
        'xǁEntryDelegateǁsizeHint__mutmut_40': xǁEntryDelegateǁsizeHint__mutmut_40, 
        'xǁEntryDelegateǁsizeHint__mutmut_41': xǁEntryDelegateǁsizeHint__mutmut_41, 
        'xǁEntryDelegateǁsizeHint__mutmut_42': xǁEntryDelegateǁsizeHint__mutmut_42, 
        'xǁEntryDelegateǁsizeHint__mutmut_43': xǁEntryDelegateǁsizeHint__mutmut_43, 
        'xǁEntryDelegateǁsizeHint__mutmut_44': xǁEntryDelegateǁsizeHint__mutmut_44, 
        'xǁEntryDelegateǁsizeHint__mutmut_45': xǁEntryDelegateǁsizeHint__mutmut_45, 
        'xǁEntryDelegateǁsizeHint__mutmut_46': xǁEntryDelegateǁsizeHint__mutmut_46, 
        'xǁEntryDelegateǁsizeHint__mutmut_47': xǁEntryDelegateǁsizeHint__mutmut_47, 
        'xǁEntryDelegateǁsizeHint__mutmut_48': xǁEntryDelegateǁsizeHint__mutmut_48, 
        'xǁEntryDelegateǁsizeHint__mutmut_49': xǁEntryDelegateǁsizeHint__mutmut_49, 
        'xǁEntryDelegateǁsizeHint__mutmut_50': xǁEntryDelegateǁsizeHint__mutmut_50, 
        'xǁEntryDelegateǁsizeHint__mutmut_51': xǁEntryDelegateǁsizeHint__mutmut_51, 
        'xǁEntryDelegateǁsizeHint__mutmut_52': xǁEntryDelegateǁsizeHint__mutmut_52, 
        'xǁEntryDelegateǁsizeHint__mutmut_53': xǁEntryDelegateǁsizeHint__mutmut_53, 
        'xǁEntryDelegateǁsizeHint__mutmut_54': xǁEntryDelegateǁsizeHint__mutmut_54, 
        'xǁEntryDelegateǁsizeHint__mutmut_55': xǁEntryDelegateǁsizeHint__mutmut_55, 
        'xǁEntryDelegateǁsizeHint__mutmut_56': xǁEntryDelegateǁsizeHint__mutmut_56, 
        'xǁEntryDelegateǁsizeHint__mutmut_57': xǁEntryDelegateǁsizeHint__mutmut_57, 
        'xǁEntryDelegateǁsizeHint__mutmut_58': xǁEntryDelegateǁsizeHint__mutmut_58, 
        'xǁEntryDelegateǁsizeHint__mutmut_59': xǁEntryDelegateǁsizeHint__mutmut_59, 
        'xǁEntryDelegateǁsizeHint__mutmut_60': xǁEntryDelegateǁsizeHint__mutmut_60, 
        'xǁEntryDelegateǁsizeHint__mutmut_61': xǁEntryDelegateǁsizeHint__mutmut_61, 
        'xǁEntryDelegateǁsizeHint__mutmut_62': xǁEntryDelegateǁsizeHint__mutmut_62, 
        'xǁEntryDelegateǁsizeHint__mutmut_63': xǁEntryDelegateǁsizeHint__mutmut_63, 
        'xǁEntryDelegateǁsizeHint__mutmut_64': xǁEntryDelegateǁsizeHint__mutmut_64, 
        'xǁEntryDelegateǁsizeHint__mutmut_65': xǁEntryDelegateǁsizeHint__mutmut_65, 
        'xǁEntryDelegateǁsizeHint__mutmut_66': xǁEntryDelegateǁsizeHint__mutmut_66, 
        'xǁEntryDelegateǁsizeHint__mutmut_67': xǁEntryDelegateǁsizeHint__mutmut_67, 
        'xǁEntryDelegateǁsizeHint__mutmut_68': xǁEntryDelegateǁsizeHint__mutmut_68, 
        'xǁEntryDelegateǁsizeHint__mutmut_69': xǁEntryDelegateǁsizeHint__mutmut_69, 
        'xǁEntryDelegateǁsizeHint__mutmut_70': xǁEntryDelegateǁsizeHint__mutmut_70, 
        'xǁEntryDelegateǁsizeHint__mutmut_71': xǁEntryDelegateǁsizeHint__mutmut_71, 
        'xǁEntryDelegateǁsizeHint__mutmut_72': xǁEntryDelegateǁsizeHint__mutmut_72, 
        'xǁEntryDelegateǁsizeHint__mutmut_73': xǁEntryDelegateǁsizeHint__mutmut_73, 
        'xǁEntryDelegateǁsizeHint__mutmut_74': xǁEntryDelegateǁsizeHint__mutmut_74, 
        'xǁEntryDelegateǁsizeHint__mutmut_75': xǁEntryDelegateǁsizeHint__mutmut_75, 
        'xǁEntryDelegateǁsizeHint__mutmut_76': xǁEntryDelegateǁsizeHint__mutmut_76, 
        'xǁEntryDelegateǁsizeHint__mutmut_77': xǁEntryDelegateǁsizeHint__mutmut_77, 
        'xǁEntryDelegateǁsizeHint__mutmut_78': xǁEntryDelegateǁsizeHint__mutmut_78, 
        'xǁEntryDelegateǁsizeHint__mutmut_79': xǁEntryDelegateǁsizeHint__mutmut_79, 
        'xǁEntryDelegateǁsizeHint__mutmut_80': xǁEntryDelegateǁsizeHint__mutmut_80, 
        'xǁEntryDelegateǁsizeHint__mutmut_81': xǁEntryDelegateǁsizeHint__mutmut_81, 
        'xǁEntryDelegateǁsizeHint__mutmut_82': xǁEntryDelegateǁsizeHint__mutmut_82, 
        'xǁEntryDelegateǁsizeHint__mutmut_83': xǁEntryDelegateǁsizeHint__mutmut_83, 
        'xǁEntryDelegateǁsizeHint__mutmut_84': xǁEntryDelegateǁsizeHint__mutmut_84, 
        'xǁEntryDelegateǁsizeHint__mutmut_85': xǁEntryDelegateǁsizeHint__mutmut_85, 
        'xǁEntryDelegateǁsizeHint__mutmut_86': xǁEntryDelegateǁsizeHint__mutmut_86, 
        'xǁEntryDelegateǁsizeHint__mutmut_87': xǁEntryDelegateǁsizeHint__mutmut_87, 
        'xǁEntryDelegateǁsizeHint__mutmut_88': xǁEntryDelegateǁsizeHint__mutmut_88, 
        'xǁEntryDelegateǁsizeHint__mutmut_89': xǁEntryDelegateǁsizeHint__mutmut_89, 
        'xǁEntryDelegateǁsizeHint__mutmut_90': xǁEntryDelegateǁsizeHint__mutmut_90, 
        'xǁEntryDelegateǁsizeHint__mutmut_91': xǁEntryDelegateǁsizeHint__mutmut_91, 
        'xǁEntryDelegateǁsizeHint__mutmut_92': xǁEntryDelegateǁsizeHint__mutmut_92, 
        'xǁEntryDelegateǁsizeHint__mutmut_93': xǁEntryDelegateǁsizeHint__mutmut_93, 
        'xǁEntryDelegateǁsizeHint__mutmut_94': xǁEntryDelegateǁsizeHint__mutmut_94, 
        'xǁEntryDelegateǁsizeHint__mutmut_95': xǁEntryDelegateǁsizeHint__mutmut_95, 
        'xǁEntryDelegateǁsizeHint__mutmut_96': xǁEntryDelegateǁsizeHint__mutmut_96, 
        'xǁEntryDelegateǁsizeHint__mutmut_97': xǁEntryDelegateǁsizeHint__mutmut_97, 
        'xǁEntryDelegateǁsizeHint__mutmut_98': xǁEntryDelegateǁsizeHint__mutmut_98, 
        'xǁEntryDelegateǁsizeHint__mutmut_99': xǁEntryDelegateǁsizeHint__mutmut_99
    }
    xǁEntryDelegateǁsizeHint__mutmut_orig.__name__ = 'xǁEntryDelegateǁsizeHint'

    def paint(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        args = [painter, option, index]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryDelegateǁpaint__mutmut_orig'), object.__getattribute__(self, 'xǁEntryDelegateǁpaint__mutmut_mutants'), args, kwargs, self)

    def xǁEntryDelegateǁpaint__mutmut_orig(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_1(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(None, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_2(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, None)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_3(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_4(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, )
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_5(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, False)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_6(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(None, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_7(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, None)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_8(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_9(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, )

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_10(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, False)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_11(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = None
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_12(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(None)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_13(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = None

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_14(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(None, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_15(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, None, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_16(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, None, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_17(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, None)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_18(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_19(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_20(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_21(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, )

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_22(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(3, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_23(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 3, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_24(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, +2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_25(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -3, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_26(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, +2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_27(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -3)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_28(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = None
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_29(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(None, 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_30(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), None, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_31(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, None)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_32(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_33(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_34(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, )

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_35(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(None), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_36(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 13, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_37(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 13)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_38(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand or item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_39(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = None

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_40(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(None)
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_41(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap("XX:/arrow_icons/media/btn_icons/arrow_down.svgXX")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_42(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/ARROW_ICONS/MEDIA/BTN_ICONS/ARROW_DOWN.SVG")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_43(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(None)
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_44(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap("XX:/arrow_icons/media/btn_icons/arrow_right.svgXX")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_45(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/ARROW_ICONS/MEDIA/BTN_ICONS/ARROW_RIGHT.SVG")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_46(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = None
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_47(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor(None)
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_48(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("XX#1A8FBFXX")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_49(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1a8fbf")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_50(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(None)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_51(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(91 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_52(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 21)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_53(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(None)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_54(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(None)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_55(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(None, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_56(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, None)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_57(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_58(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, )

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_59(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = None
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_60(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height / 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_61(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 1.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_62(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = None
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_63(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) * 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_64(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height + ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_65(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 3
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_66(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = None

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_67(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            None,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_68(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            None,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_69(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            None,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_70(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            None,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_71(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_72(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_73(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_74(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_75(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin + ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_76(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() + ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_77(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() - ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_78(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = None
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_79(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                None,
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_80(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                None,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_81(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                None,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_82(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_83(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_84(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_85(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                None,
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_86(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                None,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_87(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_88(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_89(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = None
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_90(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 11
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_91(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = None

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_92(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            None,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_93(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            None,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_94(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            None,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_95(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            None,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_96(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_97(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_98(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_99(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_100(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() - ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_101(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() - ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_102(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = None

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_103(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                None,
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_104(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                None,
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_105(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                None,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_106(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                None,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_107(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_108(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_109(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_110(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_111(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(None),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_112(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(None),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_113(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = None
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_114(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(None)
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_115(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(None)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_116(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = None
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_117(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(None)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_118(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(None, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_119(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, None, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_120(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, None)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_121(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_122(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_123(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, )
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_124(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(1, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_125(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 1, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_126(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    None
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_127(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(None, QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_128(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), None)
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_129(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_130(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), )
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_131(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(None))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_132(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    None,
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_133(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    None,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_134(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_135(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_136(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    None,
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_137(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    None,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_138(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_139(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_140(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = None

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_141(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            None
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_142(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin + rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_143(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size + ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_144(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() + ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_145(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() / 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_146(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 1.1
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_147(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = None

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_148(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            None,
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_149(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            None,
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_150(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            None,
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_151(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            None,
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_152(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_153(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_154(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_155(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_156(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin - (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_157(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left() - left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_158(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 1),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_159(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin + (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_160(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left() + left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_161(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin + rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_162(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 1),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_163(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(None)

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_164(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(None, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_165(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, None, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_166(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, None))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_167(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_168(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_169(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, ))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_170(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(256, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_171(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 256, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_172(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 256))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_173(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = None
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_174(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize >= 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_175(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 1:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_176(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(None)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_177(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(None)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_178(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = None

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_179(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(None)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_180(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = None
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_181(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(None)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_182(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize >= 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_183(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 1:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_184(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(None)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_185(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = None

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_186(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(None)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_187(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = None

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_188(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width() + left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_189(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text) + left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_190(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right() + right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_191(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(None)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_192(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = None
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_193(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace(None, "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_194(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", None)
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_195(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_196(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", )
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_197(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("XX\nXX", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_198(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "XXXX")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_199(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_200(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = None
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_201(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x + left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_202(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = None
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_203(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                None,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_204(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                None,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_205(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                None,
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_206(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_207(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_208(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_209(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(None),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_210(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                None,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_211(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                None,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_212(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                None,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_213(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_214(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_215(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_216(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                None,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_217(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                None,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_218(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                None,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_219(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_220(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_221(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_222(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter & QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_223(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft & QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_224(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(None)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_225(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(None)
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_226(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(None, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_227(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, None, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_228(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, None))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_229(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_230(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_231(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, ))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_232(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(161, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_233(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 161, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_234(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 161))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_235(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                None,
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_236(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                None,
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_237(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                None,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_238(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_239(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_240(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_241(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(None),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_242(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    None
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_243(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top() - (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_244(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) * 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_245(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() - right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_246(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 3
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_247(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = None
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_248(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() / 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_249(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 1.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_250(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = None
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_251(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter + 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_252(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() + dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_253(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 6
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_254(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = None
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_255(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(None, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_256(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, None, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_257(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, None)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_258(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_259(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_260(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, )
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_261(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(227, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_262(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 32, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_263(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 32)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_264(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(None)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_265(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(None)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_266(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = None
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_267(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(None, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_268(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, None, dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_269(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), None, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_270(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, None)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_271(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_272(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, dot_diameter, dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_273(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter)
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_274(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, )
            painter.drawEllipse(dot_rect)

        painter.restore()

    def xǁEntryDelegateǁpaint__mutmut_275(
        self,
        painter: QtGui.QPainter,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Renders each item"""
        painter.save()
        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)
        painter.setRenderHint(QtGui.QPainter.RenderHint.SmoothPixmapTransform, True)

        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        rect = option.rect.adjusted(2, 2, -2, -2)

        path = QtGui.QPainterPath()
        path.addRoundedRect(QtCore.QRectF(rect), 12, 12)

        if item.not_clickable:
            painter.restore()
            return
        if item.allow_expand and item.needs_expansion:
            item.right_icon = (
                QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_down.svg")
                if item.is_expanded
                else QtGui.QPixmap(":/arrow_icons/media/btn_icons/arrow_right.svg")
            )

        # Background Color
        pressed_color = QtGui.QColor("#1A8FBF")
        pressed_color.setAlpha(90 if item.selected else 20)

        painter.setPen(QtCore.Qt.PenStyle.NoPen)
        painter.setBrush(pressed_color)
        painter.fillPath(path, pressed_color)

        # Geometry Calc

        # ICON SPACEEE
        ellipse_size = item.height * 0.8
        ellipse_margin = (item.height - ellipse_size) / 2
        ellipse_rect = QtCore.QRectF(
            rect.right() - ellipse_margin - ellipse_size,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.right_icon:
            icon_scaled = item.right_icon.scaled(
                ellipse_rect.size().toSize(),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )
            painter.drawPixmap(
                ellipse_rect.toRect(),
                icon_scaled,
            )

        left_margin = 10
        left_icon_rect = QtCore.QRectF(
            rect.left() + ellipse_margin,
            rect.top() + ellipse_margin,
            ellipse_size,
            ellipse_size,
        )

        if item.left_icon:
            l_icon_scaled = item.left_icon.scaled(
                int(left_icon_rect.width()),
                int(left_icon_rect.height()),
                QtCore.Qt.AspectRatioMode.KeepAspectRatio,
                QtCore.Qt.TransformationMode.SmoothTransformation,
            )

            if item.color_left_icon:
                tinted = QtGui.QPixmap(l_icon_scaled.size())
                tinted.fill(QtCore.Qt.GlobalColor.transparent)
                p2 = QtGui.QPainter(tinted)
                p2.drawPixmap(0, 0, l_icon_scaled)
                p2.setCompositionMode(
                    QtGui.QPainter.CompositionMode.CompositionMode_SourceIn
                )
                p2.fillRect(tinted.rect(), QtGui.QColor(item.color))
                p2.end()
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    tinted,
                )
            else:
                painter.drawPixmap(
                    left_icon_rect.toRect(),
                    l_icon_scaled,
                )

        text_margin = int(
            rect.right() - ellipse_size - ellipse_margin - rect.height() * 0.10
        )

        text_rect = QtCore.QRectF(
            rect.left()
            + left_margin
            + (left_icon_rect.width() if item.left_icon else 0),
            rect.top(),
            text_margin
            - rect.left()
            - left_margin
            - (left_icon_rect.width() if item.left_icon else 0),
            rect.height(),
        )

        painter.setPen(QtGui.QColor(255, 255, 255))

        _font = painter.font()
        if item._lfontsize > 0:
            _font.setPointSize(item._lfontsize)
        painter.setFont(_font)

        metrics = QtGui.QFontMetrics(_font)

        right_font = QtGui.QFont(_font)
        if item._rfontsize > 0:
            right_font.setPointSize(item._rfontsize)

        right_metrics = QtGui.QFontMetrics(right_font)

        right_text_x = (
            ellipse_rect.right()
            - right_metrics.horizontalAdvance(item.right_text)
            - left_icon_rect.width()
            - left_margin
        )

        text = item.text.replace("\n", "")
        # Logic: If not expanded, OR if expansion is not needed, draw single line
        if not item.is_expanded:
            max_main_text_width = right_text_x - left_margin
            text = metrics.elidedText(
                text,
                QtCore.Qt.TextElideMode.ElideRight,
                int(max_main_text_width),
            )
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignVCenter,
                text,
            )
        else:
            # Expanded mode
            painter.drawText(
                text_rect,
                QtCore.Qt.AlignmentFlag.AlignLeft
                | QtCore.Qt.AlignmentFlag.AlignVCenter
                | QtCore.Qt.TextFlag.TextWordWrap,
                text,
            )

        if item.right_text:
            painter.setFont(right_font)
            painter.setPen(QtGui.QColor(160, 160, 160))
            painter.drawText(
                int(right_text_x),
                int(
                    ellipse_rect.top()
                    + (ellipse_rect.height() + right_metrics.ascent()) / 2
                ),
                item.right_text,
            )

        if item.notificate:
            dot_diameter = rect.height() * 0.3
            dot_x = rect.width() - dot_diameter - 5
            notification_color = QtGui.QColor(226, 31, 31)
            painter.setBrush(notification_color)
            painter.setPen(QtCore.Qt.PenStyle.NoPen)
            dot_rect = QtCore.QRectF(dot_x, rect.top(), dot_diameter, dot_diameter)
            painter.drawEllipse(None)

        painter.restore()
    
    xǁEntryDelegateǁpaint__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryDelegateǁpaint__mutmut_1': xǁEntryDelegateǁpaint__mutmut_1, 
        'xǁEntryDelegateǁpaint__mutmut_2': xǁEntryDelegateǁpaint__mutmut_2, 
        'xǁEntryDelegateǁpaint__mutmut_3': xǁEntryDelegateǁpaint__mutmut_3, 
        'xǁEntryDelegateǁpaint__mutmut_4': xǁEntryDelegateǁpaint__mutmut_4, 
        'xǁEntryDelegateǁpaint__mutmut_5': xǁEntryDelegateǁpaint__mutmut_5, 
        'xǁEntryDelegateǁpaint__mutmut_6': xǁEntryDelegateǁpaint__mutmut_6, 
        'xǁEntryDelegateǁpaint__mutmut_7': xǁEntryDelegateǁpaint__mutmut_7, 
        'xǁEntryDelegateǁpaint__mutmut_8': xǁEntryDelegateǁpaint__mutmut_8, 
        'xǁEntryDelegateǁpaint__mutmut_9': xǁEntryDelegateǁpaint__mutmut_9, 
        'xǁEntryDelegateǁpaint__mutmut_10': xǁEntryDelegateǁpaint__mutmut_10, 
        'xǁEntryDelegateǁpaint__mutmut_11': xǁEntryDelegateǁpaint__mutmut_11, 
        'xǁEntryDelegateǁpaint__mutmut_12': xǁEntryDelegateǁpaint__mutmut_12, 
        'xǁEntryDelegateǁpaint__mutmut_13': xǁEntryDelegateǁpaint__mutmut_13, 
        'xǁEntryDelegateǁpaint__mutmut_14': xǁEntryDelegateǁpaint__mutmut_14, 
        'xǁEntryDelegateǁpaint__mutmut_15': xǁEntryDelegateǁpaint__mutmut_15, 
        'xǁEntryDelegateǁpaint__mutmut_16': xǁEntryDelegateǁpaint__mutmut_16, 
        'xǁEntryDelegateǁpaint__mutmut_17': xǁEntryDelegateǁpaint__mutmut_17, 
        'xǁEntryDelegateǁpaint__mutmut_18': xǁEntryDelegateǁpaint__mutmut_18, 
        'xǁEntryDelegateǁpaint__mutmut_19': xǁEntryDelegateǁpaint__mutmut_19, 
        'xǁEntryDelegateǁpaint__mutmut_20': xǁEntryDelegateǁpaint__mutmut_20, 
        'xǁEntryDelegateǁpaint__mutmut_21': xǁEntryDelegateǁpaint__mutmut_21, 
        'xǁEntryDelegateǁpaint__mutmut_22': xǁEntryDelegateǁpaint__mutmut_22, 
        'xǁEntryDelegateǁpaint__mutmut_23': xǁEntryDelegateǁpaint__mutmut_23, 
        'xǁEntryDelegateǁpaint__mutmut_24': xǁEntryDelegateǁpaint__mutmut_24, 
        'xǁEntryDelegateǁpaint__mutmut_25': xǁEntryDelegateǁpaint__mutmut_25, 
        'xǁEntryDelegateǁpaint__mutmut_26': xǁEntryDelegateǁpaint__mutmut_26, 
        'xǁEntryDelegateǁpaint__mutmut_27': xǁEntryDelegateǁpaint__mutmut_27, 
        'xǁEntryDelegateǁpaint__mutmut_28': xǁEntryDelegateǁpaint__mutmut_28, 
        'xǁEntryDelegateǁpaint__mutmut_29': xǁEntryDelegateǁpaint__mutmut_29, 
        'xǁEntryDelegateǁpaint__mutmut_30': xǁEntryDelegateǁpaint__mutmut_30, 
        'xǁEntryDelegateǁpaint__mutmut_31': xǁEntryDelegateǁpaint__mutmut_31, 
        'xǁEntryDelegateǁpaint__mutmut_32': xǁEntryDelegateǁpaint__mutmut_32, 
        'xǁEntryDelegateǁpaint__mutmut_33': xǁEntryDelegateǁpaint__mutmut_33, 
        'xǁEntryDelegateǁpaint__mutmut_34': xǁEntryDelegateǁpaint__mutmut_34, 
        'xǁEntryDelegateǁpaint__mutmut_35': xǁEntryDelegateǁpaint__mutmut_35, 
        'xǁEntryDelegateǁpaint__mutmut_36': xǁEntryDelegateǁpaint__mutmut_36, 
        'xǁEntryDelegateǁpaint__mutmut_37': xǁEntryDelegateǁpaint__mutmut_37, 
        'xǁEntryDelegateǁpaint__mutmut_38': xǁEntryDelegateǁpaint__mutmut_38, 
        'xǁEntryDelegateǁpaint__mutmut_39': xǁEntryDelegateǁpaint__mutmut_39, 
        'xǁEntryDelegateǁpaint__mutmut_40': xǁEntryDelegateǁpaint__mutmut_40, 
        'xǁEntryDelegateǁpaint__mutmut_41': xǁEntryDelegateǁpaint__mutmut_41, 
        'xǁEntryDelegateǁpaint__mutmut_42': xǁEntryDelegateǁpaint__mutmut_42, 
        'xǁEntryDelegateǁpaint__mutmut_43': xǁEntryDelegateǁpaint__mutmut_43, 
        'xǁEntryDelegateǁpaint__mutmut_44': xǁEntryDelegateǁpaint__mutmut_44, 
        'xǁEntryDelegateǁpaint__mutmut_45': xǁEntryDelegateǁpaint__mutmut_45, 
        'xǁEntryDelegateǁpaint__mutmut_46': xǁEntryDelegateǁpaint__mutmut_46, 
        'xǁEntryDelegateǁpaint__mutmut_47': xǁEntryDelegateǁpaint__mutmut_47, 
        'xǁEntryDelegateǁpaint__mutmut_48': xǁEntryDelegateǁpaint__mutmut_48, 
        'xǁEntryDelegateǁpaint__mutmut_49': xǁEntryDelegateǁpaint__mutmut_49, 
        'xǁEntryDelegateǁpaint__mutmut_50': xǁEntryDelegateǁpaint__mutmut_50, 
        'xǁEntryDelegateǁpaint__mutmut_51': xǁEntryDelegateǁpaint__mutmut_51, 
        'xǁEntryDelegateǁpaint__mutmut_52': xǁEntryDelegateǁpaint__mutmut_52, 
        'xǁEntryDelegateǁpaint__mutmut_53': xǁEntryDelegateǁpaint__mutmut_53, 
        'xǁEntryDelegateǁpaint__mutmut_54': xǁEntryDelegateǁpaint__mutmut_54, 
        'xǁEntryDelegateǁpaint__mutmut_55': xǁEntryDelegateǁpaint__mutmut_55, 
        'xǁEntryDelegateǁpaint__mutmut_56': xǁEntryDelegateǁpaint__mutmut_56, 
        'xǁEntryDelegateǁpaint__mutmut_57': xǁEntryDelegateǁpaint__mutmut_57, 
        'xǁEntryDelegateǁpaint__mutmut_58': xǁEntryDelegateǁpaint__mutmut_58, 
        'xǁEntryDelegateǁpaint__mutmut_59': xǁEntryDelegateǁpaint__mutmut_59, 
        'xǁEntryDelegateǁpaint__mutmut_60': xǁEntryDelegateǁpaint__mutmut_60, 
        'xǁEntryDelegateǁpaint__mutmut_61': xǁEntryDelegateǁpaint__mutmut_61, 
        'xǁEntryDelegateǁpaint__mutmut_62': xǁEntryDelegateǁpaint__mutmut_62, 
        'xǁEntryDelegateǁpaint__mutmut_63': xǁEntryDelegateǁpaint__mutmut_63, 
        'xǁEntryDelegateǁpaint__mutmut_64': xǁEntryDelegateǁpaint__mutmut_64, 
        'xǁEntryDelegateǁpaint__mutmut_65': xǁEntryDelegateǁpaint__mutmut_65, 
        'xǁEntryDelegateǁpaint__mutmut_66': xǁEntryDelegateǁpaint__mutmut_66, 
        'xǁEntryDelegateǁpaint__mutmut_67': xǁEntryDelegateǁpaint__mutmut_67, 
        'xǁEntryDelegateǁpaint__mutmut_68': xǁEntryDelegateǁpaint__mutmut_68, 
        'xǁEntryDelegateǁpaint__mutmut_69': xǁEntryDelegateǁpaint__mutmut_69, 
        'xǁEntryDelegateǁpaint__mutmut_70': xǁEntryDelegateǁpaint__mutmut_70, 
        'xǁEntryDelegateǁpaint__mutmut_71': xǁEntryDelegateǁpaint__mutmut_71, 
        'xǁEntryDelegateǁpaint__mutmut_72': xǁEntryDelegateǁpaint__mutmut_72, 
        'xǁEntryDelegateǁpaint__mutmut_73': xǁEntryDelegateǁpaint__mutmut_73, 
        'xǁEntryDelegateǁpaint__mutmut_74': xǁEntryDelegateǁpaint__mutmut_74, 
        'xǁEntryDelegateǁpaint__mutmut_75': xǁEntryDelegateǁpaint__mutmut_75, 
        'xǁEntryDelegateǁpaint__mutmut_76': xǁEntryDelegateǁpaint__mutmut_76, 
        'xǁEntryDelegateǁpaint__mutmut_77': xǁEntryDelegateǁpaint__mutmut_77, 
        'xǁEntryDelegateǁpaint__mutmut_78': xǁEntryDelegateǁpaint__mutmut_78, 
        'xǁEntryDelegateǁpaint__mutmut_79': xǁEntryDelegateǁpaint__mutmut_79, 
        'xǁEntryDelegateǁpaint__mutmut_80': xǁEntryDelegateǁpaint__mutmut_80, 
        'xǁEntryDelegateǁpaint__mutmut_81': xǁEntryDelegateǁpaint__mutmut_81, 
        'xǁEntryDelegateǁpaint__mutmut_82': xǁEntryDelegateǁpaint__mutmut_82, 
        'xǁEntryDelegateǁpaint__mutmut_83': xǁEntryDelegateǁpaint__mutmut_83, 
        'xǁEntryDelegateǁpaint__mutmut_84': xǁEntryDelegateǁpaint__mutmut_84, 
        'xǁEntryDelegateǁpaint__mutmut_85': xǁEntryDelegateǁpaint__mutmut_85, 
        'xǁEntryDelegateǁpaint__mutmut_86': xǁEntryDelegateǁpaint__mutmut_86, 
        'xǁEntryDelegateǁpaint__mutmut_87': xǁEntryDelegateǁpaint__mutmut_87, 
        'xǁEntryDelegateǁpaint__mutmut_88': xǁEntryDelegateǁpaint__mutmut_88, 
        'xǁEntryDelegateǁpaint__mutmut_89': xǁEntryDelegateǁpaint__mutmut_89, 
        'xǁEntryDelegateǁpaint__mutmut_90': xǁEntryDelegateǁpaint__mutmut_90, 
        'xǁEntryDelegateǁpaint__mutmut_91': xǁEntryDelegateǁpaint__mutmut_91, 
        'xǁEntryDelegateǁpaint__mutmut_92': xǁEntryDelegateǁpaint__mutmut_92, 
        'xǁEntryDelegateǁpaint__mutmut_93': xǁEntryDelegateǁpaint__mutmut_93, 
        'xǁEntryDelegateǁpaint__mutmut_94': xǁEntryDelegateǁpaint__mutmut_94, 
        'xǁEntryDelegateǁpaint__mutmut_95': xǁEntryDelegateǁpaint__mutmut_95, 
        'xǁEntryDelegateǁpaint__mutmut_96': xǁEntryDelegateǁpaint__mutmut_96, 
        'xǁEntryDelegateǁpaint__mutmut_97': xǁEntryDelegateǁpaint__mutmut_97, 
        'xǁEntryDelegateǁpaint__mutmut_98': xǁEntryDelegateǁpaint__mutmut_98, 
        'xǁEntryDelegateǁpaint__mutmut_99': xǁEntryDelegateǁpaint__mutmut_99, 
        'xǁEntryDelegateǁpaint__mutmut_100': xǁEntryDelegateǁpaint__mutmut_100, 
        'xǁEntryDelegateǁpaint__mutmut_101': xǁEntryDelegateǁpaint__mutmut_101, 
        'xǁEntryDelegateǁpaint__mutmut_102': xǁEntryDelegateǁpaint__mutmut_102, 
        'xǁEntryDelegateǁpaint__mutmut_103': xǁEntryDelegateǁpaint__mutmut_103, 
        'xǁEntryDelegateǁpaint__mutmut_104': xǁEntryDelegateǁpaint__mutmut_104, 
        'xǁEntryDelegateǁpaint__mutmut_105': xǁEntryDelegateǁpaint__mutmut_105, 
        'xǁEntryDelegateǁpaint__mutmut_106': xǁEntryDelegateǁpaint__mutmut_106, 
        'xǁEntryDelegateǁpaint__mutmut_107': xǁEntryDelegateǁpaint__mutmut_107, 
        'xǁEntryDelegateǁpaint__mutmut_108': xǁEntryDelegateǁpaint__mutmut_108, 
        'xǁEntryDelegateǁpaint__mutmut_109': xǁEntryDelegateǁpaint__mutmut_109, 
        'xǁEntryDelegateǁpaint__mutmut_110': xǁEntryDelegateǁpaint__mutmut_110, 
        'xǁEntryDelegateǁpaint__mutmut_111': xǁEntryDelegateǁpaint__mutmut_111, 
        'xǁEntryDelegateǁpaint__mutmut_112': xǁEntryDelegateǁpaint__mutmut_112, 
        'xǁEntryDelegateǁpaint__mutmut_113': xǁEntryDelegateǁpaint__mutmut_113, 
        'xǁEntryDelegateǁpaint__mutmut_114': xǁEntryDelegateǁpaint__mutmut_114, 
        'xǁEntryDelegateǁpaint__mutmut_115': xǁEntryDelegateǁpaint__mutmut_115, 
        'xǁEntryDelegateǁpaint__mutmut_116': xǁEntryDelegateǁpaint__mutmut_116, 
        'xǁEntryDelegateǁpaint__mutmut_117': xǁEntryDelegateǁpaint__mutmut_117, 
        'xǁEntryDelegateǁpaint__mutmut_118': xǁEntryDelegateǁpaint__mutmut_118, 
        'xǁEntryDelegateǁpaint__mutmut_119': xǁEntryDelegateǁpaint__mutmut_119, 
        'xǁEntryDelegateǁpaint__mutmut_120': xǁEntryDelegateǁpaint__mutmut_120, 
        'xǁEntryDelegateǁpaint__mutmut_121': xǁEntryDelegateǁpaint__mutmut_121, 
        'xǁEntryDelegateǁpaint__mutmut_122': xǁEntryDelegateǁpaint__mutmut_122, 
        'xǁEntryDelegateǁpaint__mutmut_123': xǁEntryDelegateǁpaint__mutmut_123, 
        'xǁEntryDelegateǁpaint__mutmut_124': xǁEntryDelegateǁpaint__mutmut_124, 
        'xǁEntryDelegateǁpaint__mutmut_125': xǁEntryDelegateǁpaint__mutmut_125, 
        'xǁEntryDelegateǁpaint__mutmut_126': xǁEntryDelegateǁpaint__mutmut_126, 
        'xǁEntryDelegateǁpaint__mutmut_127': xǁEntryDelegateǁpaint__mutmut_127, 
        'xǁEntryDelegateǁpaint__mutmut_128': xǁEntryDelegateǁpaint__mutmut_128, 
        'xǁEntryDelegateǁpaint__mutmut_129': xǁEntryDelegateǁpaint__mutmut_129, 
        'xǁEntryDelegateǁpaint__mutmut_130': xǁEntryDelegateǁpaint__mutmut_130, 
        'xǁEntryDelegateǁpaint__mutmut_131': xǁEntryDelegateǁpaint__mutmut_131, 
        'xǁEntryDelegateǁpaint__mutmut_132': xǁEntryDelegateǁpaint__mutmut_132, 
        'xǁEntryDelegateǁpaint__mutmut_133': xǁEntryDelegateǁpaint__mutmut_133, 
        'xǁEntryDelegateǁpaint__mutmut_134': xǁEntryDelegateǁpaint__mutmut_134, 
        'xǁEntryDelegateǁpaint__mutmut_135': xǁEntryDelegateǁpaint__mutmut_135, 
        'xǁEntryDelegateǁpaint__mutmut_136': xǁEntryDelegateǁpaint__mutmut_136, 
        'xǁEntryDelegateǁpaint__mutmut_137': xǁEntryDelegateǁpaint__mutmut_137, 
        'xǁEntryDelegateǁpaint__mutmut_138': xǁEntryDelegateǁpaint__mutmut_138, 
        'xǁEntryDelegateǁpaint__mutmut_139': xǁEntryDelegateǁpaint__mutmut_139, 
        'xǁEntryDelegateǁpaint__mutmut_140': xǁEntryDelegateǁpaint__mutmut_140, 
        'xǁEntryDelegateǁpaint__mutmut_141': xǁEntryDelegateǁpaint__mutmut_141, 
        'xǁEntryDelegateǁpaint__mutmut_142': xǁEntryDelegateǁpaint__mutmut_142, 
        'xǁEntryDelegateǁpaint__mutmut_143': xǁEntryDelegateǁpaint__mutmut_143, 
        'xǁEntryDelegateǁpaint__mutmut_144': xǁEntryDelegateǁpaint__mutmut_144, 
        'xǁEntryDelegateǁpaint__mutmut_145': xǁEntryDelegateǁpaint__mutmut_145, 
        'xǁEntryDelegateǁpaint__mutmut_146': xǁEntryDelegateǁpaint__mutmut_146, 
        'xǁEntryDelegateǁpaint__mutmut_147': xǁEntryDelegateǁpaint__mutmut_147, 
        'xǁEntryDelegateǁpaint__mutmut_148': xǁEntryDelegateǁpaint__mutmut_148, 
        'xǁEntryDelegateǁpaint__mutmut_149': xǁEntryDelegateǁpaint__mutmut_149, 
        'xǁEntryDelegateǁpaint__mutmut_150': xǁEntryDelegateǁpaint__mutmut_150, 
        'xǁEntryDelegateǁpaint__mutmut_151': xǁEntryDelegateǁpaint__mutmut_151, 
        'xǁEntryDelegateǁpaint__mutmut_152': xǁEntryDelegateǁpaint__mutmut_152, 
        'xǁEntryDelegateǁpaint__mutmut_153': xǁEntryDelegateǁpaint__mutmut_153, 
        'xǁEntryDelegateǁpaint__mutmut_154': xǁEntryDelegateǁpaint__mutmut_154, 
        'xǁEntryDelegateǁpaint__mutmut_155': xǁEntryDelegateǁpaint__mutmut_155, 
        'xǁEntryDelegateǁpaint__mutmut_156': xǁEntryDelegateǁpaint__mutmut_156, 
        'xǁEntryDelegateǁpaint__mutmut_157': xǁEntryDelegateǁpaint__mutmut_157, 
        'xǁEntryDelegateǁpaint__mutmut_158': xǁEntryDelegateǁpaint__mutmut_158, 
        'xǁEntryDelegateǁpaint__mutmut_159': xǁEntryDelegateǁpaint__mutmut_159, 
        'xǁEntryDelegateǁpaint__mutmut_160': xǁEntryDelegateǁpaint__mutmut_160, 
        'xǁEntryDelegateǁpaint__mutmut_161': xǁEntryDelegateǁpaint__mutmut_161, 
        'xǁEntryDelegateǁpaint__mutmut_162': xǁEntryDelegateǁpaint__mutmut_162, 
        'xǁEntryDelegateǁpaint__mutmut_163': xǁEntryDelegateǁpaint__mutmut_163, 
        'xǁEntryDelegateǁpaint__mutmut_164': xǁEntryDelegateǁpaint__mutmut_164, 
        'xǁEntryDelegateǁpaint__mutmut_165': xǁEntryDelegateǁpaint__mutmut_165, 
        'xǁEntryDelegateǁpaint__mutmut_166': xǁEntryDelegateǁpaint__mutmut_166, 
        'xǁEntryDelegateǁpaint__mutmut_167': xǁEntryDelegateǁpaint__mutmut_167, 
        'xǁEntryDelegateǁpaint__mutmut_168': xǁEntryDelegateǁpaint__mutmut_168, 
        'xǁEntryDelegateǁpaint__mutmut_169': xǁEntryDelegateǁpaint__mutmut_169, 
        'xǁEntryDelegateǁpaint__mutmut_170': xǁEntryDelegateǁpaint__mutmut_170, 
        'xǁEntryDelegateǁpaint__mutmut_171': xǁEntryDelegateǁpaint__mutmut_171, 
        'xǁEntryDelegateǁpaint__mutmut_172': xǁEntryDelegateǁpaint__mutmut_172, 
        'xǁEntryDelegateǁpaint__mutmut_173': xǁEntryDelegateǁpaint__mutmut_173, 
        'xǁEntryDelegateǁpaint__mutmut_174': xǁEntryDelegateǁpaint__mutmut_174, 
        'xǁEntryDelegateǁpaint__mutmut_175': xǁEntryDelegateǁpaint__mutmut_175, 
        'xǁEntryDelegateǁpaint__mutmut_176': xǁEntryDelegateǁpaint__mutmut_176, 
        'xǁEntryDelegateǁpaint__mutmut_177': xǁEntryDelegateǁpaint__mutmut_177, 
        'xǁEntryDelegateǁpaint__mutmut_178': xǁEntryDelegateǁpaint__mutmut_178, 
        'xǁEntryDelegateǁpaint__mutmut_179': xǁEntryDelegateǁpaint__mutmut_179, 
        'xǁEntryDelegateǁpaint__mutmut_180': xǁEntryDelegateǁpaint__mutmut_180, 
        'xǁEntryDelegateǁpaint__mutmut_181': xǁEntryDelegateǁpaint__mutmut_181, 
        'xǁEntryDelegateǁpaint__mutmut_182': xǁEntryDelegateǁpaint__mutmut_182, 
        'xǁEntryDelegateǁpaint__mutmut_183': xǁEntryDelegateǁpaint__mutmut_183, 
        'xǁEntryDelegateǁpaint__mutmut_184': xǁEntryDelegateǁpaint__mutmut_184, 
        'xǁEntryDelegateǁpaint__mutmut_185': xǁEntryDelegateǁpaint__mutmut_185, 
        'xǁEntryDelegateǁpaint__mutmut_186': xǁEntryDelegateǁpaint__mutmut_186, 
        'xǁEntryDelegateǁpaint__mutmut_187': xǁEntryDelegateǁpaint__mutmut_187, 
        'xǁEntryDelegateǁpaint__mutmut_188': xǁEntryDelegateǁpaint__mutmut_188, 
        'xǁEntryDelegateǁpaint__mutmut_189': xǁEntryDelegateǁpaint__mutmut_189, 
        'xǁEntryDelegateǁpaint__mutmut_190': xǁEntryDelegateǁpaint__mutmut_190, 
        'xǁEntryDelegateǁpaint__mutmut_191': xǁEntryDelegateǁpaint__mutmut_191, 
        'xǁEntryDelegateǁpaint__mutmut_192': xǁEntryDelegateǁpaint__mutmut_192, 
        'xǁEntryDelegateǁpaint__mutmut_193': xǁEntryDelegateǁpaint__mutmut_193, 
        'xǁEntryDelegateǁpaint__mutmut_194': xǁEntryDelegateǁpaint__mutmut_194, 
        'xǁEntryDelegateǁpaint__mutmut_195': xǁEntryDelegateǁpaint__mutmut_195, 
        'xǁEntryDelegateǁpaint__mutmut_196': xǁEntryDelegateǁpaint__mutmut_196, 
        'xǁEntryDelegateǁpaint__mutmut_197': xǁEntryDelegateǁpaint__mutmut_197, 
        'xǁEntryDelegateǁpaint__mutmut_198': xǁEntryDelegateǁpaint__mutmut_198, 
        'xǁEntryDelegateǁpaint__mutmut_199': xǁEntryDelegateǁpaint__mutmut_199, 
        'xǁEntryDelegateǁpaint__mutmut_200': xǁEntryDelegateǁpaint__mutmut_200, 
        'xǁEntryDelegateǁpaint__mutmut_201': xǁEntryDelegateǁpaint__mutmut_201, 
        'xǁEntryDelegateǁpaint__mutmut_202': xǁEntryDelegateǁpaint__mutmut_202, 
        'xǁEntryDelegateǁpaint__mutmut_203': xǁEntryDelegateǁpaint__mutmut_203, 
        'xǁEntryDelegateǁpaint__mutmut_204': xǁEntryDelegateǁpaint__mutmut_204, 
        'xǁEntryDelegateǁpaint__mutmut_205': xǁEntryDelegateǁpaint__mutmut_205, 
        'xǁEntryDelegateǁpaint__mutmut_206': xǁEntryDelegateǁpaint__mutmut_206, 
        'xǁEntryDelegateǁpaint__mutmut_207': xǁEntryDelegateǁpaint__mutmut_207, 
        'xǁEntryDelegateǁpaint__mutmut_208': xǁEntryDelegateǁpaint__mutmut_208, 
        'xǁEntryDelegateǁpaint__mutmut_209': xǁEntryDelegateǁpaint__mutmut_209, 
        'xǁEntryDelegateǁpaint__mutmut_210': xǁEntryDelegateǁpaint__mutmut_210, 
        'xǁEntryDelegateǁpaint__mutmut_211': xǁEntryDelegateǁpaint__mutmut_211, 
        'xǁEntryDelegateǁpaint__mutmut_212': xǁEntryDelegateǁpaint__mutmut_212, 
        'xǁEntryDelegateǁpaint__mutmut_213': xǁEntryDelegateǁpaint__mutmut_213, 
        'xǁEntryDelegateǁpaint__mutmut_214': xǁEntryDelegateǁpaint__mutmut_214, 
        'xǁEntryDelegateǁpaint__mutmut_215': xǁEntryDelegateǁpaint__mutmut_215, 
        'xǁEntryDelegateǁpaint__mutmut_216': xǁEntryDelegateǁpaint__mutmut_216, 
        'xǁEntryDelegateǁpaint__mutmut_217': xǁEntryDelegateǁpaint__mutmut_217, 
        'xǁEntryDelegateǁpaint__mutmut_218': xǁEntryDelegateǁpaint__mutmut_218, 
        'xǁEntryDelegateǁpaint__mutmut_219': xǁEntryDelegateǁpaint__mutmut_219, 
        'xǁEntryDelegateǁpaint__mutmut_220': xǁEntryDelegateǁpaint__mutmut_220, 
        'xǁEntryDelegateǁpaint__mutmut_221': xǁEntryDelegateǁpaint__mutmut_221, 
        'xǁEntryDelegateǁpaint__mutmut_222': xǁEntryDelegateǁpaint__mutmut_222, 
        'xǁEntryDelegateǁpaint__mutmut_223': xǁEntryDelegateǁpaint__mutmut_223, 
        'xǁEntryDelegateǁpaint__mutmut_224': xǁEntryDelegateǁpaint__mutmut_224, 
        'xǁEntryDelegateǁpaint__mutmut_225': xǁEntryDelegateǁpaint__mutmut_225, 
        'xǁEntryDelegateǁpaint__mutmut_226': xǁEntryDelegateǁpaint__mutmut_226, 
        'xǁEntryDelegateǁpaint__mutmut_227': xǁEntryDelegateǁpaint__mutmut_227, 
        'xǁEntryDelegateǁpaint__mutmut_228': xǁEntryDelegateǁpaint__mutmut_228, 
        'xǁEntryDelegateǁpaint__mutmut_229': xǁEntryDelegateǁpaint__mutmut_229, 
        'xǁEntryDelegateǁpaint__mutmut_230': xǁEntryDelegateǁpaint__mutmut_230, 
        'xǁEntryDelegateǁpaint__mutmut_231': xǁEntryDelegateǁpaint__mutmut_231, 
        'xǁEntryDelegateǁpaint__mutmut_232': xǁEntryDelegateǁpaint__mutmut_232, 
        'xǁEntryDelegateǁpaint__mutmut_233': xǁEntryDelegateǁpaint__mutmut_233, 
        'xǁEntryDelegateǁpaint__mutmut_234': xǁEntryDelegateǁpaint__mutmut_234, 
        'xǁEntryDelegateǁpaint__mutmut_235': xǁEntryDelegateǁpaint__mutmut_235, 
        'xǁEntryDelegateǁpaint__mutmut_236': xǁEntryDelegateǁpaint__mutmut_236, 
        'xǁEntryDelegateǁpaint__mutmut_237': xǁEntryDelegateǁpaint__mutmut_237, 
        'xǁEntryDelegateǁpaint__mutmut_238': xǁEntryDelegateǁpaint__mutmut_238, 
        'xǁEntryDelegateǁpaint__mutmut_239': xǁEntryDelegateǁpaint__mutmut_239, 
        'xǁEntryDelegateǁpaint__mutmut_240': xǁEntryDelegateǁpaint__mutmut_240, 
        'xǁEntryDelegateǁpaint__mutmut_241': xǁEntryDelegateǁpaint__mutmut_241, 
        'xǁEntryDelegateǁpaint__mutmut_242': xǁEntryDelegateǁpaint__mutmut_242, 
        'xǁEntryDelegateǁpaint__mutmut_243': xǁEntryDelegateǁpaint__mutmut_243, 
        'xǁEntryDelegateǁpaint__mutmut_244': xǁEntryDelegateǁpaint__mutmut_244, 
        'xǁEntryDelegateǁpaint__mutmut_245': xǁEntryDelegateǁpaint__mutmut_245, 
        'xǁEntryDelegateǁpaint__mutmut_246': xǁEntryDelegateǁpaint__mutmut_246, 
        'xǁEntryDelegateǁpaint__mutmut_247': xǁEntryDelegateǁpaint__mutmut_247, 
        'xǁEntryDelegateǁpaint__mutmut_248': xǁEntryDelegateǁpaint__mutmut_248, 
        'xǁEntryDelegateǁpaint__mutmut_249': xǁEntryDelegateǁpaint__mutmut_249, 
        'xǁEntryDelegateǁpaint__mutmut_250': xǁEntryDelegateǁpaint__mutmut_250, 
        'xǁEntryDelegateǁpaint__mutmut_251': xǁEntryDelegateǁpaint__mutmut_251, 
        'xǁEntryDelegateǁpaint__mutmut_252': xǁEntryDelegateǁpaint__mutmut_252, 
        'xǁEntryDelegateǁpaint__mutmut_253': xǁEntryDelegateǁpaint__mutmut_253, 
        'xǁEntryDelegateǁpaint__mutmut_254': xǁEntryDelegateǁpaint__mutmut_254, 
        'xǁEntryDelegateǁpaint__mutmut_255': xǁEntryDelegateǁpaint__mutmut_255, 
        'xǁEntryDelegateǁpaint__mutmut_256': xǁEntryDelegateǁpaint__mutmut_256, 
        'xǁEntryDelegateǁpaint__mutmut_257': xǁEntryDelegateǁpaint__mutmut_257, 
        'xǁEntryDelegateǁpaint__mutmut_258': xǁEntryDelegateǁpaint__mutmut_258, 
        'xǁEntryDelegateǁpaint__mutmut_259': xǁEntryDelegateǁpaint__mutmut_259, 
        'xǁEntryDelegateǁpaint__mutmut_260': xǁEntryDelegateǁpaint__mutmut_260, 
        'xǁEntryDelegateǁpaint__mutmut_261': xǁEntryDelegateǁpaint__mutmut_261, 
        'xǁEntryDelegateǁpaint__mutmut_262': xǁEntryDelegateǁpaint__mutmut_262, 
        'xǁEntryDelegateǁpaint__mutmut_263': xǁEntryDelegateǁpaint__mutmut_263, 
        'xǁEntryDelegateǁpaint__mutmut_264': xǁEntryDelegateǁpaint__mutmut_264, 
        'xǁEntryDelegateǁpaint__mutmut_265': xǁEntryDelegateǁpaint__mutmut_265, 
        'xǁEntryDelegateǁpaint__mutmut_266': xǁEntryDelegateǁpaint__mutmut_266, 
        'xǁEntryDelegateǁpaint__mutmut_267': xǁEntryDelegateǁpaint__mutmut_267, 
        'xǁEntryDelegateǁpaint__mutmut_268': xǁEntryDelegateǁpaint__mutmut_268, 
        'xǁEntryDelegateǁpaint__mutmut_269': xǁEntryDelegateǁpaint__mutmut_269, 
        'xǁEntryDelegateǁpaint__mutmut_270': xǁEntryDelegateǁpaint__mutmut_270, 
        'xǁEntryDelegateǁpaint__mutmut_271': xǁEntryDelegateǁpaint__mutmut_271, 
        'xǁEntryDelegateǁpaint__mutmut_272': xǁEntryDelegateǁpaint__mutmut_272, 
        'xǁEntryDelegateǁpaint__mutmut_273': xǁEntryDelegateǁpaint__mutmut_273, 
        'xǁEntryDelegateǁpaint__mutmut_274': xǁEntryDelegateǁpaint__mutmut_274, 
        'xǁEntryDelegateǁpaint__mutmut_275': xǁEntryDelegateǁpaint__mutmut_275
    }
    xǁEntryDelegateǁpaint__mutmut_orig.__name__ = 'xǁEntryDelegateǁpaint'

    def editorEvent(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        args = [event, model, option, index]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁEntryDelegateǁeditorEvent__mutmut_orig'), object.__getattribute__(self, 'xǁEntryDelegateǁeditorEvent__mutmut_mutants'), args, kwargs, self)

    def xǁEntryDelegateǁeditorEvent__mutmut_orig(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_1(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = None
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_2(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(None)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_3(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() != QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_4(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item or item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_5(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return False

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_6(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback or callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_7(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(None):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_8(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is not None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_9(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return True

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_10(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = None
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_11(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height / 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_12(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 1.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_13(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = None
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_14(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) * 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_15(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height + ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_16(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 3
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_17(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = None
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_18(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                None,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_19(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                None,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_20(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                None,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_21(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                None,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_22(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_23(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_24(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_25(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_26(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin + ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_27(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() + ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_28(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() - ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_29(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = None

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_30(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand or item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_31(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos) or item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_32(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(None)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_33(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = None
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_34(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_35(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(None, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_36(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, None, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_37(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, None)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_38(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_39(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_40(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, )
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_41(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return False

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_42(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index == index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_43(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = None
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_44(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(None)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_45(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.rindex(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_46(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(None, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_47(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, None, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_48(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, None)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_49(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_50(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_51(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, )
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_52(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, True, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_53(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = None

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_54(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(None, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_55(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, None, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_56(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, None)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_57(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_58(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_59(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, )
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_60(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, False, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_61(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(None)
            return True
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_62(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return False
        return False

    def xǁEntryDelegateǁeditorEvent__mutmut_63(  # pylint: disable=invalid-name
        self,
        event: QtCore.QEvent,
        model: EntryListModel,
        option: QtWidgets.QStyleOptionViewItem,
        index: QtCore.QModelIndex,
    ):
        """Capture view model events"""
        item = index.data(QtCore.Qt.ItemDataRole.UserRole)
        if event.type() == QtCore.QEvent.Type.MouseButtonPress:
            if item and item.not_clickable:
                return True

            if item.callback and callable(item.callback):
                item.callback()

            if self.prev_index is None:
                return False

            ellipse_size = item.height * 0.8
            ellipse_margin = (item.height - ellipse_size) / 2
            ellipse_rect = QtCore.QRectF(
                option.rect.right() - ellipse_margin - ellipse_size,
                option.rect.top() + ellipse_margin,
                ellipse_size,
                ellipse_size,
            )
            pos = event.position()

            # --- Logic Check ---
            # Only allow toggle if allow_expand AND text actually needs expansion
            if (
                ellipse_rect.contains(pos)
                and item.allow_expand
                and item.needs_expansion
            ):
                new_state = not item.is_expanded
                model.setData(index, new_state, EntryListModel.ExpandRole)
                return True

            if self.prev_index != index.row():
                prev_index: QtCore.QModelIndex = model.index(self.prev_index)
                if prev_index.isValid():
                    model.setData(prev_index, False, EntryListModel.EnableRole)
                self.prev_index = index.row()

            model.setData(index, True, EntryListModel.EnableRole)
            self.item_selected.emit(item)
            return True
        return True
    
    xǁEntryDelegateǁeditorEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁEntryDelegateǁeditorEvent__mutmut_1': xǁEntryDelegateǁeditorEvent__mutmut_1, 
        'xǁEntryDelegateǁeditorEvent__mutmut_2': xǁEntryDelegateǁeditorEvent__mutmut_2, 
        'xǁEntryDelegateǁeditorEvent__mutmut_3': xǁEntryDelegateǁeditorEvent__mutmut_3, 
        'xǁEntryDelegateǁeditorEvent__mutmut_4': xǁEntryDelegateǁeditorEvent__mutmut_4, 
        'xǁEntryDelegateǁeditorEvent__mutmut_5': xǁEntryDelegateǁeditorEvent__mutmut_5, 
        'xǁEntryDelegateǁeditorEvent__mutmut_6': xǁEntryDelegateǁeditorEvent__mutmut_6, 
        'xǁEntryDelegateǁeditorEvent__mutmut_7': xǁEntryDelegateǁeditorEvent__mutmut_7, 
        'xǁEntryDelegateǁeditorEvent__mutmut_8': xǁEntryDelegateǁeditorEvent__mutmut_8, 
        'xǁEntryDelegateǁeditorEvent__mutmut_9': xǁEntryDelegateǁeditorEvent__mutmut_9, 
        'xǁEntryDelegateǁeditorEvent__mutmut_10': xǁEntryDelegateǁeditorEvent__mutmut_10, 
        'xǁEntryDelegateǁeditorEvent__mutmut_11': xǁEntryDelegateǁeditorEvent__mutmut_11, 
        'xǁEntryDelegateǁeditorEvent__mutmut_12': xǁEntryDelegateǁeditorEvent__mutmut_12, 
        'xǁEntryDelegateǁeditorEvent__mutmut_13': xǁEntryDelegateǁeditorEvent__mutmut_13, 
        'xǁEntryDelegateǁeditorEvent__mutmut_14': xǁEntryDelegateǁeditorEvent__mutmut_14, 
        'xǁEntryDelegateǁeditorEvent__mutmut_15': xǁEntryDelegateǁeditorEvent__mutmut_15, 
        'xǁEntryDelegateǁeditorEvent__mutmut_16': xǁEntryDelegateǁeditorEvent__mutmut_16, 
        'xǁEntryDelegateǁeditorEvent__mutmut_17': xǁEntryDelegateǁeditorEvent__mutmut_17, 
        'xǁEntryDelegateǁeditorEvent__mutmut_18': xǁEntryDelegateǁeditorEvent__mutmut_18, 
        'xǁEntryDelegateǁeditorEvent__mutmut_19': xǁEntryDelegateǁeditorEvent__mutmut_19, 
        'xǁEntryDelegateǁeditorEvent__mutmut_20': xǁEntryDelegateǁeditorEvent__mutmut_20, 
        'xǁEntryDelegateǁeditorEvent__mutmut_21': xǁEntryDelegateǁeditorEvent__mutmut_21, 
        'xǁEntryDelegateǁeditorEvent__mutmut_22': xǁEntryDelegateǁeditorEvent__mutmut_22, 
        'xǁEntryDelegateǁeditorEvent__mutmut_23': xǁEntryDelegateǁeditorEvent__mutmut_23, 
        'xǁEntryDelegateǁeditorEvent__mutmut_24': xǁEntryDelegateǁeditorEvent__mutmut_24, 
        'xǁEntryDelegateǁeditorEvent__mutmut_25': xǁEntryDelegateǁeditorEvent__mutmut_25, 
        'xǁEntryDelegateǁeditorEvent__mutmut_26': xǁEntryDelegateǁeditorEvent__mutmut_26, 
        'xǁEntryDelegateǁeditorEvent__mutmut_27': xǁEntryDelegateǁeditorEvent__mutmut_27, 
        'xǁEntryDelegateǁeditorEvent__mutmut_28': xǁEntryDelegateǁeditorEvent__mutmut_28, 
        'xǁEntryDelegateǁeditorEvent__mutmut_29': xǁEntryDelegateǁeditorEvent__mutmut_29, 
        'xǁEntryDelegateǁeditorEvent__mutmut_30': xǁEntryDelegateǁeditorEvent__mutmut_30, 
        'xǁEntryDelegateǁeditorEvent__mutmut_31': xǁEntryDelegateǁeditorEvent__mutmut_31, 
        'xǁEntryDelegateǁeditorEvent__mutmut_32': xǁEntryDelegateǁeditorEvent__mutmut_32, 
        'xǁEntryDelegateǁeditorEvent__mutmut_33': xǁEntryDelegateǁeditorEvent__mutmut_33, 
        'xǁEntryDelegateǁeditorEvent__mutmut_34': xǁEntryDelegateǁeditorEvent__mutmut_34, 
        'xǁEntryDelegateǁeditorEvent__mutmut_35': xǁEntryDelegateǁeditorEvent__mutmut_35, 
        'xǁEntryDelegateǁeditorEvent__mutmut_36': xǁEntryDelegateǁeditorEvent__mutmut_36, 
        'xǁEntryDelegateǁeditorEvent__mutmut_37': xǁEntryDelegateǁeditorEvent__mutmut_37, 
        'xǁEntryDelegateǁeditorEvent__mutmut_38': xǁEntryDelegateǁeditorEvent__mutmut_38, 
        'xǁEntryDelegateǁeditorEvent__mutmut_39': xǁEntryDelegateǁeditorEvent__mutmut_39, 
        'xǁEntryDelegateǁeditorEvent__mutmut_40': xǁEntryDelegateǁeditorEvent__mutmut_40, 
        'xǁEntryDelegateǁeditorEvent__mutmut_41': xǁEntryDelegateǁeditorEvent__mutmut_41, 
        'xǁEntryDelegateǁeditorEvent__mutmut_42': xǁEntryDelegateǁeditorEvent__mutmut_42, 
        'xǁEntryDelegateǁeditorEvent__mutmut_43': xǁEntryDelegateǁeditorEvent__mutmut_43, 
        'xǁEntryDelegateǁeditorEvent__mutmut_44': xǁEntryDelegateǁeditorEvent__mutmut_44, 
        'xǁEntryDelegateǁeditorEvent__mutmut_45': xǁEntryDelegateǁeditorEvent__mutmut_45, 
        'xǁEntryDelegateǁeditorEvent__mutmut_46': xǁEntryDelegateǁeditorEvent__mutmut_46, 
        'xǁEntryDelegateǁeditorEvent__mutmut_47': xǁEntryDelegateǁeditorEvent__mutmut_47, 
        'xǁEntryDelegateǁeditorEvent__mutmut_48': xǁEntryDelegateǁeditorEvent__mutmut_48, 
        'xǁEntryDelegateǁeditorEvent__mutmut_49': xǁEntryDelegateǁeditorEvent__mutmut_49, 
        'xǁEntryDelegateǁeditorEvent__mutmut_50': xǁEntryDelegateǁeditorEvent__mutmut_50, 
        'xǁEntryDelegateǁeditorEvent__mutmut_51': xǁEntryDelegateǁeditorEvent__mutmut_51, 
        'xǁEntryDelegateǁeditorEvent__mutmut_52': xǁEntryDelegateǁeditorEvent__mutmut_52, 
        'xǁEntryDelegateǁeditorEvent__mutmut_53': xǁEntryDelegateǁeditorEvent__mutmut_53, 
        'xǁEntryDelegateǁeditorEvent__mutmut_54': xǁEntryDelegateǁeditorEvent__mutmut_54, 
        'xǁEntryDelegateǁeditorEvent__mutmut_55': xǁEntryDelegateǁeditorEvent__mutmut_55, 
        'xǁEntryDelegateǁeditorEvent__mutmut_56': xǁEntryDelegateǁeditorEvent__mutmut_56, 
        'xǁEntryDelegateǁeditorEvent__mutmut_57': xǁEntryDelegateǁeditorEvent__mutmut_57, 
        'xǁEntryDelegateǁeditorEvent__mutmut_58': xǁEntryDelegateǁeditorEvent__mutmut_58, 
        'xǁEntryDelegateǁeditorEvent__mutmut_59': xǁEntryDelegateǁeditorEvent__mutmut_59, 
        'xǁEntryDelegateǁeditorEvent__mutmut_60': xǁEntryDelegateǁeditorEvent__mutmut_60, 
        'xǁEntryDelegateǁeditorEvent__mutmut_61': xǁEntryDelegateǁeditorEvent__mutmut_61, 
        'xǁEntryDelegateǁeditorEvent__mutmut_62': xǁEntryDelegateǁeditorEvent__mutmut_62, 
        'xǁEntryDelegateǁeditorEvent__mutmut_63': xǁEntryDelegateǁeditorEvent__mutmut_63
    }
    xǁEntryDelegateǁeditorEvent__mutmut_orig.__name__ = 'xǁEntryDelegateǁeditorEvent'
