from PyQt6 import QtCore, QtGui, QtWidgets
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


class BlocksSlider(QtWidgets.QSlider):
    def __init__(self, parent) -> None:
        args = [parent]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksSliderǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁBlocksSliderǁ__init____mutmut_mutants'), args, kwargs, self)
    def xǁBlocksSliderǁ__init____mutmut_orig(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_1(self, parent) -> None:
        super().__init__(None)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_2(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = None
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_3(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "XX#2AC9F9XX"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_4(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2ac9f9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_5(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = None
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_6(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(None, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_7(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, None)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_8(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_9(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, )
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_10(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(1.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_11(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 1.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_12(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(None, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_13(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, None)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_14(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_15(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, )
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_16(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(301, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_17(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 101)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_18(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(None, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_19(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, None)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_20(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_21(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, )
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_22(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(401, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_23(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 101)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_24(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(None)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_25(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(False)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_26(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(None)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_27(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(False)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_28(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(None)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_29(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(None)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_30(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(21)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_31(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(None)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_32(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(1)
        self.setMaximum(100)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_33(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(None)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_34(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(101)
        self.setPageStep(0)
    def xǁBlocksSliderǁ__init____mutmut_35(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(None)
    def xǁBlocksSliderǁ__init____mutmut_36(self, parent) -> None:
        super().__init__(parent)
        self.highlight_color = "#2AC9F9"
        self.gradient_pos = QtCore.QPointF(0.0, 0.0)
        self.setMinimumSize(300, 100)
        self.setMaximumSize(400, 100)
        self.setMouseTracking(True)
        self.setTracking(True)
        self.setOrientation(QtCore.Qt.Orientation.Horizontal)
        self.setTickInterval(20)
        self.setMinimum(0)
        self.setMaximum(100)
        self.setPageStep(1)
    
    xǁBlocksSliderǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksSliderǁ__init____mutmut_1': xǁBlocksSliderǁ__init____mutmut_1, 
        'xǁBlocksSliderǁ__init____mutmut_2': xǁBlocksSliderǁ__init____mutmut_2, 
        'xǁBlocksSliderǁ__init____mutmut_3': xǁBlocksSliderǁ__init____mutmut_3, 
        'xǁBlocksSliderǁ__init____mutmut_4': xǁBlocksSliderǁ__init____mutmut_4, 
        'xǁBlocksSliderǁ__init____mutmut_5': xǁBlocksSliderǁ__init____mutmut_5, 
        'xǁBlocksSliderǁ__init____mutmut_6': xǁBlocksSliderǁ__init____mutmut_6, 
        'xǁBlocksSliderǁ__init____mutmut_7': xǁBlocksSliderǁ__init____mutmut_7, 
        'xǁBlocksSliderǁ__init____mutmut_8': xǁBlocksSliderǁ__init____mutmut_8, 
        'xǁBlocksSliderǁ__init____mutmut_9': xǁBlocksSliderǁ__init____mutmut_9, 
        'xǁBlocksSliderǁ__init____mutmut_10': xǁBlocksSliderǁ__init____mutmut_10, 
        'xǁBlocksSliderǁ__init____mutmut_11': xǁBlocksSliderǁ__init____mutmut_11, 
        'xǁBlocksSliderǁ__init____mutmut_12': xǁBlocksSliderǁ__init____mutmut_12, 
        'xǁBlocksSliderǁ__init____mutmut_13': xǁBlocksSliderǁ__init____mutmut_13, 
        'xǁBlocksSliderǁ__init____mutmut_14': xǁBlocksSliderǁ__init____mutmut_14, 
        'xǁBlocksSliderǁ__init____mutmut_15': xǁBlocksSliderǁ__init____mutmut_15, 
        'xǁBlocksSliderǁ__init____mutmut_16': xǁBlocksSliderǁ__init____mutmut_16, 
        'xǁBlocksSliderǁ__init____mutmut_17': xǁBlocksSliderǁ__init____mutmut_17, 
        'xǁBlocksSliderǁ__init____mutmut_18': xǁBlocksSliderǁ__init____mutmut_18, 
        'xǁBlocksSliderǁ__init____mutmut_19': xǁBlocksSliderǁ__init____mutmut_19, 
        'xǁBlocksSliderǁ__init____mutmut_20': xǁBlocksSliderǁ__init____mutmut_20, 
        'xǁBlocksSliderǁ__init____mutmut_21': xǁBlocksSliderǁ__init____mutmut_21, 
        'xǁBlocksSliderǁ__init____mutmut_22': xǁBlocksSliderǁ__init____mutmut_22, 
        'xǁBlocksSliderǁ__init____mutmut_23': xǁBlocksSliderǁ__init____mutmut_23, 
        'xǁBlocksSliderǁ__init____mutmut_24': xǁBlocksSliderǁ__init____mutmut_24, 
        'xǁBlocksSliderǁ__init____mutmut_25': xǁBlocksSliderǁ__init____mutmut_25, 
        'xǁBlocksSliderǁ__init____mutmut_26': xǁBlocksSliderǁ__init____mutmut_26, 
        'xǁBlocksSliderǁ__init____mutmut_27': xǁBlocksSliderǁ__init____mutmut_27, 
        'xǁBlocksSliderǁ__init____mutmut_28': xǁBlocksSliderǁ__init____mutmut_28, 
        'xǁBlocksSliderǁ__init____mutmut_29': xǁBlocksSliderǁ__init____mutmut_29, 
        'xǁBlocksSliderǁ__init____mutmut_30': xǁBlocksSliderǁ__init____mutmut_30, 
        'xǁBlocksSliderǁ__init____mutmut_31': xǁBlocksSliderǁ__init____mutmut_31, 
        'xǁBlocksSliderǁ__init____mutmut_32': xǁBlocksSliderǁ__init____mutmut_32, 
        'xǁBlocksSliderǁ__init____mutmut_33': xǁBlocksSliderǁ__init____mutmut_33, 
        'xǁBlocksSliderǁ__init____mutmut_34': xǁBlocksSliderǁ__init____mutmut_34, 
        'xǁBlocksSliderǁ__init____mutmut_35': xǁBlocksSliderǁ__init____mutmut_35, 
        'xǁBlocksSliderǁ__init____mutmut_36': xǁBlocksSliderǁ__init____mutmut_36
    }
    xǁBlocksSliderǁ__init____mutmut_orig.__name__ = 'xǁBlocksSliderǁ__init__'

    def mousePressEvent(self, ev: QtGui.QMouseEvent) -> None:
        args = [ev]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksSliderǁmousePressEvent__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksSliderǁmousePressEvent__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksSliderǁmousePressEvent__mutmut_orig(self, ev: QtGui.QMouseEvent) -> None:
        """Re-implemented method, Handle mouse press events"""
        if (ev.button() == QtCore.Qt.MouseButton.LeftButton) and self.hit_test(
            ev.position().toPoint().toPointF()
        ):
            self.setSliderDown(True)
            ev.accept()
        else:
            return super().mousePressEvent(ev)

    def xǁBlocksSliderǁmousePressEvent__mutmut_1(self, ev: QtGui.QMouseEvent) -> None:
        """Re-implemented method, Handle mouse press events"""
        if (ev.button() == QtCore.Qt.MouseButton.LeftButton) or self.hit_test(
            ev.position().toPoint().toPointF()
        ):
            self.setSliderDown(True)
            ev.accept()
        else:
            return super().mousePressEvent(ev)

    def xǁBlocksSliderǁmousePressEvent__mutmut_2(self, ev: QtGui.QMouseEvent) -> None:
        """Re-implemented method, Handle mouse press events"""
        if (ev.button() != QtCore.Qt.MouseButton.LeftButton) and self.hit_test(
            ev.position().toPoint().toPointF()
        ):
            self.setSliderDown(True)
            ev.accept()
        else:
            return super().mousePressEvent(ev)

    def xǁBlocksSliderǁmousePressEvent__mutmut_3(self, ev: QtGui.QMouseEvent) -> None:
        """Re-implemented method, Handle mouse press events"""
        if (ev.button() == QtCore.Qt.MouseButton.LeftButton) and self.hit_test(
            None
        ):
            self.setSliderDown(True)
            ev.accept()
        else:
            return super().mousePressEvent(ev)

    def xǁBlocksSliderǁmousePressEvent__mutmut_4(self, ev: QtGui.QMouseEvent) -> None:
        """Re-implemented method, Handle mouse press events"""
        if (ev.button() == QtCore.Qt.MouseButton.LeftButton) and self.hit_test(
            ev.position().toPoint().toPointF()
        ):
            self.setSliderDown(None)
            ev.accept()
        else:
            return super().mousePressEvent(ev)

    def xǁBlocksSliderǁmousePressEvent__mutmut_5(self, ev: QtGui.QMouseEvent) -> None:
        """Re-implemented method, Handle mouse press events"""
        if (ev.button() == QtCore.Qt.MouseButton.LeftButton) and self.hit_test(
            ev.position().toPoint().toPointF()
        ):
            self.setSliderDown(False)
            ev.accept()
        else:
            return super().mousePressEvent(ev)

    def xǁBlocksSliderǁmousePressEvent__mutmut_6(self, ev: QtGui.QMouseEvent) -> None:
        """Re-implemented method, Handle mouse press events"""
        if (ev.button() == QtCore.Qt.MouseButton.LeftButton) and self.hit_test(
            ev.position().toPoint().toPointF()
        ):
            self.setSliderDown(True)
            ev.accept()
        else:
            return super().mousePressEvent(None)
    
    xǁBlocksSliderǁmousePressEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksSliderǁmousePressEvent__mutmut_1': xǁBlocksSliderǁmousePressEvent__mutmut_1, 
        'xǁBlocksSliderǁmousePressEvent__mutmut_2': xǁBlocksSliderǁmousePressEvent__mutmut_2, 
        'xǁBlocksSliderǁmousePressEvent__mutmut_3': xǁBlocksSliderǁmousePressEvent__mutmut_3, 
        'xǁBlocksSliderǁmousePressEvent__mutmut_4': xǁBlocksSliderǁmousePressEvent__mutmut_4, 
        'xǁBlocksSliderǁmousePressEvent__mutmut_5': xǁBlocksSliderǁmousePressEvent__mutmut_5, 
        'xǁBlocksSliderǁmousePressEvent__mutmut_6': xǁBlocksSliderǁmousePressEvent__mutmut_6
    }
    xǁBlocksSliderǁmousePressEvent__mutmut_orig.__name__ = 'xǁBlocksSliderǁmousePressEvent'

    def mouseReleaseEvent(self, ev: QtGui.QMouseEvent) -> None:
        args = [ev]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksSliderǁmouseReleaseEvent__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksSliderǁmouseReleaseEvent__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksSliderǁmouseReleaseEvent__mutmut_orig(self, ev: QtGui.QMouseEvent) -> None:
        """Handle mouse release events"""
        if self.isSliderDown():
            self.setSliderDown(False)
        return super().mouseReleaseEvent(ev)

    def xǁBlocksSliderǁmouseReleaseEvent__mutmut_1(self, ev: QtGui.QMouseEvent) -> None:
        """Handle mouse release events"""
        if self.isSliderDown():
            self.setSliderDown(None)
        return super().mouseReleaseEvent(ev)

    def xǁBlocksSliderǁmouseReleaseEvent__mutmut_2(self, ev: QtGui.QMouseEvent) -> None:
        """Handle mouse release events"""
        if self.isSliderDown():
            self.setSliderDown(True)
        return super().mouseReleaseEvent(ev)

    def xǁBlocksSliderǁmouseReleaseEvent__mutmut_3(self, ev: QtGui.QMouseEvent) -> None:
        """Handle mouse release events"""
        if self.isSliderDown():
            self.setSliderDown(False)
        return super().mouseReleaseEvent(None)
    
    xǁBlocksSliderǁmouseReleaseEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksSliderǁmouseReleaseEvent__mutmut_1': xǁBlocksSliderǁmouseReleaseEvent__mutmut_1, 
        'xǁBlocksSliderǁmouseReleaseEvent__mutmut_2': xǁBlocksSliderǁmouseReleaseEvent__mutmut_2, 
        'xǁBlocksSliderǁmouseReleaseEvent__mutmut_3': xǁBlocksSliderǁmouseReleaseEvent__mutmut_3
    }
    xǁBlocksSliderǁmouseReleaseEvent__mutmut_orig.__name__ = 'xǁBlocksSliderǁmouseReleaseEvent'

    def mouseMoveEvent(self, ev: QtGui.QMouseEvent) -> None:
        args = [ev]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksSliderǁmouseMoveEvent__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksSliderǁmouseMoveEvent__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksSliderǁmouseMoveEvent__mutmut_orig(self, ev: QtGui.QMouseEvent) -> None:
        """Handle mouse move events"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        if self.isSliderDown():
            self._set_slider_pos(ev.position().toPoint().toPointF())
            self.gradient_pos = ev.position().toPoint().toPointF()
            self.update()
            ev.accept()
        else:
            return super().mouseMoveEvent(ev)

    def xǁBlocksSliderǁmouseMoveEvent__mutmut_1(self, ev: QtGui.QMouseEvent) -> None:
        """Handle mouse move events"""
        opt = None
        self.initStyleOption(opt)
        if self.isSliderDown():
            self._set_slider_pos(ev.position().toPoint().toPointF())
            self.gradient_pos = ev.position().toPoint().toPointF()
            self.update()
            ev.accept()
        else:
            return super().mouseMoveEvent(ev)

    def xǁBlocksSliderǁmouseMoveEvent__mutmut_2(self, ev: QtGui.QMouseEvent) -> None:
        """Handle mouse move events"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(None)
        if self.isSliderDown():
            self._set_slider_pos(ev.position().toPoint().toPointF())
            self.gradient_pos = ev.position().toPoint().toPointF()
            self.update()
            ev.accept()
        else:
            return super().mouseMoveEvent(ev)

    def xǁBlocksSliderǁmouseMoveEvent__mutmut_3(self, ev: QtGui.QMouseEvent) -> None:
        """Handle mouse move events"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        if self.isSliderDown():
            self._set_slider_pos(None)
            self.gradient_pos = ev.position().toPoint().toPointF()
            self.update()
            ev.accept()
        else:
            return super().mouseMoveEvent(ev)

    def xǁBlocksSliderǁmouseMoveEvent__mutmut_4(self, ev: QtGui.QMouseEvent) -> None:
        """Handle mouse move events"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        if self.isSliderDown():
            self._set_slider_pos(ev.position().toPoint().toPointF())
            self.gradient_pos = None
            self.update()
            ev.accept()
        else:
            return super().mouseMoveEvent(ev)

    def xǁBlocksSliderǁmouseMoveEvent__mutmut_5(self, ev: QtGui.QMouseEvent) -> None:
        """Handle mouse move events"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        if self.isSliderDown():
            self._set_slider_pos(ev.position().toPoint().toPointF())
            self.gradient_pos = ev.position().toPoint().toPointF()
            self.update()
            ev.accept()
        else:
            return super().mouseMoveEvent(None)
    
    xǁBlocksSliderǁmouseMoveEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksSliderǁmouseMoveEvent__mutmut_1': xǁBlocksSliderǁmouseMoveEvent__mutmut_1, 
        'xǁBlocksSliderǁmouseMoveEvent__mutmut_2': xǁBlocksSliderǁmouseMoveEvent__mutmut_2, 
        'xǁBlocksSliderǁmouseMoveEvent__mutmut_3': xǁBlocksSliderǁmouseMoveEvent__mutmut_3, 
        'xǁBlocksSliderǁmouseMoveEvent__mutmut_4': xǁBlocksSliderǁmouseMoveEvent__mutmut_4, 
        'xǁBlocksSliderǁmouseMoveEvent__mutmut_5': xǁBlocksSliderǁmouseMoveEvent__mutmut_5
    }
    xǁBlocksSliderǁmouseMoveEvent__mutmut_orig.__name__ = 'xǁBlocksSliderǁmouseMoveEvent'

    def hit_test(self, pos: QtCore.QPointF) -> bool:
        args = [pos]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksSliderǁhit_test__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksSliderǁhit_test__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksSliderǁhit_test__mutmut_orig(self, pos: QtCore.QPointF) -> bool:
        """Hit test to allow dragging larger handle area

        Args:
            pos (QtCore.QPointF): _description_

        Returns:
            bool: If the handle contains the specified position
        """
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 6, 6)
        return _handle_path.contains(pos)

    def xǁBlocksSliderǁhit_test__mutmut_1(self, pos: QtCore.QPointF) -> bool:
        """Hit test to allow dragging larger handle area

        Args:
            pos (QtCore.QPointF): _description_

        Returns:
            bool: If the handle contains the specified position
        """
        _handle_path = None
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 6, 6)
        return _handle_path.contains(pos)

    def xǁBlocksSliderǁhit_test__mutmut_2(self, pos: QtCore.QPointF) -> bool:
        """Hit test to allow dragging larger handle area

        Args:
            pos (QtCore.QPointF): _description_

        Returns:
            bool: If the handle contains the specified position
        """
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(None, 6, 6)
        return _handle_path.contains(pos)

    def xǁBlocksSliderǁhit_test__mutmut_3(self, pos: QtCore.QPointF) -> bool:
        """Hit test to allow dragging larger handle area

        Args:
            pos (QtCore.QPointF): _description_

        Returns:
            bool: If the handle contains the specified position
        """
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), None, 6)
        return _handle_path.contains(pos)

    def xǁBlocksSliderǁhit_test__mutmut_4(self, pos: QtCore.QPointF) -> bool:
        """Hit test to allow dragging larger handle area

        Args:
            pos (QtCore.QPointF): _description_

        Returns:
            bool: If the handle contains the specified position
        """
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 6, None)
        return _handle_path.contains(pos)

    def xǁBlocksSliderǁhit_test__mutmut_5(self, pos: QtCore.QPointF) -> bool:
        """Hit test to allow dragging larger handle area

        Args:
            pos (QtCore.QPointF): _description_

        Returns:
            bool: If the handle contains the specified position
        """
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(6, 6)
        return _handle_path.contains(pos)

    def xǁBlocksSliderǁhit_test__mutmut_6(self, pos: QtCore.QPointF) -> bool:
        """Hit test to allow dragging larger handle area

        Args:
            pos (QtCore.QPointF): _description_

        Returns:
            bool: If the handle contains the specified position
        """
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 6)
        return _handle_path.contains(pos)

    def xǁBlocksSliderǁhit_test__mutmut_7(self, pos: QtCore.QPointF) -> bool:
        """Hit test to allow dragging larger handle area

        Args:
            pos (QtCore.QPointF): _description_

        Returns:
            bool: If the handle contains the specified position
        """
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 6, )
        return _handle_path.contains(pos)

    def xǁBlocksSliderǁhit_test__mutmut_8(self, pos: QtCore.QPointF) -> bool:
        """Hit test to allow dragging larger handle area

        Args:
            pos (QtCore.QPointF): _description_

        Returns:
            bool: If the handle contains the specified position
        """
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 7, 6)
        return _handle_path.contains(pos)

    def xǁBlocksSliderǁhit_test__mutmut_9(self, pos: QtCore.QPointF) -> bool:
        """Hit test to allow dragging larger handle area

        Args:
            pos (QtCore.QPointF): _description_

        Returns:
            bool: If the handle contains the specified position
        """
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 6, 7)
        return _handle_path.contains(pos)

    def xǁBlocksSliderǁhit_test__mutmut_10(self, pos: QtCore.QPointF) -> bool:
        """Hit test to allow dragging larger handle area

        Args:
            pos (QtCore.QPointF): _description_

        Returns:
            bool: If the handle contains the specified position
        """
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 6, 6)
        return _handle_path.contains(None)
    
    xǁBlocksSliderǁhit_test__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksSliderǁhit_test__mutmut_1': xǁBlocksSliderǁhit_test__mutmut_1, 
        'xǁBlocksSliderǁhit_test__mutmut_2': xǁBlocksSliderǁhit_test__mutmut_2, 
        'xǁBlocksSliderǁhit_test__mutmut_3': xǁBlocksSliderǁhit_test__mutmut_3, 
        'xǁBlocksSliderǁhit_test__mutmut_4': xǁBlocksSliderǁhit_test__mutmut_4, 
        'xǁBlocksSliderǁhit_test__mutmut_5': xǁBlocksSliderǁhit_test__mutmut_5, 
        'xǁBlocksSliderǁhit_test__mutmut_6': xǁBlocksSliderǁhit_test__mutmut_6, 
        'xǁBlocksSliderǁhit_test__mutmut_7': xǁBlocksSliderǁhit_test__mutmut_7, 
        'xǁBlocksSliderǁhit_test__mutmut_8': xǁBlocksSliderǁhit_test__mutmut_8, 
        'xǁBlocksSliderǁhit_test__mutmut_9': xǁBlocksSliderǁhit_test__mutmut_9, 
        'xǁBlocksSliderǁhit_test__mutmut_10': xǁBlocksSliderǁhit_test__mutmut_10
    }
    xǁBlocksSliderǁhit_test__mutmut_orig.__name__ = 'xǁBlocksSliderǁhit_test'

    def _set_slider_pos(self, pos: QtCore.QPointF):
        args = [pos]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksSliderǁ_set_slider_pos__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksSliderǁ_set_slider_pos__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_orig(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_1(self, pos: QtCore.QPointF):
        min_val = None
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_2(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = None
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_3(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() != QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_4(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = None
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_5(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = None
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_6(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = None
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_7(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = None
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_8(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val - (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_9(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) / slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_10(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) / (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_11(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val + min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_12(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x + slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_13(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = None
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_14(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = None
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_15(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = None
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_16(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = None
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_17(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val - (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_18(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) * slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_19(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) / (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_20(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val + min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_21(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y + slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_22(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(None)
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_23(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(None))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_24(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(None)))
        self.setValue(int(round(new_val)))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_25(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(None)
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_26(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(None))
        self.update()

    def xǁBlocksSliderǁ_set_slider_pos__mutmut_27(self, pos: QtCore.QPointF):
        min_val = self.minimum()
        max_val = self.maximum()
        if self.orientation() == QtCore.Qt.Orientation.Horizontal:
            slider_length = self._groove_rect.width()
            slider_start = self._groove_rect.x()
            pos_x = pos.x()
            new_val = (
                min_val + (max_val - min_val) * (pos_x - slider_start) // slider_length
            )
        else:
            slider_length = self._groove_rect.height()
            slider_start = self._groove_rect.y()
            pos_y = pos.y()
            new_val = (
                min_val + (max_val - min_val) * (pos_y - slider_start) / slider_length
            )
        self.setSliderPosition(int(round(new_val)))
        self.setValue(int(round(None)))
        self.update()
    
    xǁBlocksSliderǁ_set_slider_pos__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksSliderǁ_set_slider_pos__mutmut_1': xǁBlocksSliderǁ_set_slider_pos__mutmut_1, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_2': xǁBlocksSliderǁ_set_slider_pos__mutmut_2, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_3': xǁBlocksSliderǁ_set_slider_pos__mutmut_3, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_4': xǁBlocksSliderǁ_set_slider_pos__mutmut_4, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_5': xǁBlocksSliderǁ_set_slider_pos__mutmut_5, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_6': xǁBlocksSliderǁ_set_slider_pos__mutmut_6, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_7': xǁBlocksSliderǁ_set_slider_pos__mutmut_7, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_8': xǁBlocksSliderǁ_set_slider_pos__mutmut_8, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_9': xǁBlocksSliderǁ_set_slider_pos__mutmut_9, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_10': xǁBlocksSliderǁ_set_slider_pos__mutmut_10, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_11': xǁBlocksSliderǁ_set_slider_pos__mutmut_11, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_12': xǁBlocksSliderǁ_set_slider_pos__mutmut_12, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_13': xǁBlocksSliderǁ_set_slider_pos__mutmut_13, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_14': xǁBlocksSliderǁ_set_slider_pos__mutmut_14, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_15': xǁBlocksSliderǁ_set_slider_pos__mutmut_15, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_16': xǁBlocksSliderǁ_set_slider_pos__mutmut_16, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_17': xǁBlocksSliderǁ_set_slider_pos__mutmut_17, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_18': xǁBlocksSliderǁ_set_slider_pos__mutmut_18, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_19': xǁBlocksSliderǁ_set_slider_pos__mutmut_19, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_20': xǁBlocksSliderǁ_set_slider_pos__mutmut_20, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_21': xǁBlocksSliderǁ_set_slider_pos__mutmut_21, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_22': xǁBlocksSliderǁ_set_slider_pos__mutmut_22, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_23': xǁBlocksSliderǁ_set_slider_pos__mutmut_23, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_24': xǁBlocksSliderǁ_set_slider_pos__mutmut_24, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_25': xǁBlocksSliderǁ_set_slider_pos__mutmut_25, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_26': xǁBlocksSliderǁ_set_slider_pos__mutmut_26, 
        'xǁBlocksSliderǁ_set_slider_pos__mutmut_27': xǁBlocksSliderǁ_set_slider_pos__mutmut_27
    }
    xǁBlocksSliderǁ_set_slider_pos__mutmut_orig.__name__ = 'xǁBlocksSliderǁ_set_slider_pos'

    def paintEvent(self, ev: QtGui.QPaintEvent) -> None:
        args = [ev]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁBlocksSliderǁpaintEvent__mutmut_orig'), object.__getattribute__(self, 'xǁBlocksSliderǁpaintEvent__mutmut_mutants'), args, kwargs, self)

    def xǁBlocksSliderǁpaintEvent__mutmut_orig(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_1(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = None
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_2(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(None)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_3(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = None

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_4(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = None  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_5(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(None, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_6(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, None, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_7(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, None, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_8(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, None)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_9(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_10(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_11(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_12(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, )  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_13(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(13, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_14(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 11, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_15(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, +18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_16(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -19, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_17(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 21)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_18(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = None

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_19(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            None,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_20(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            None,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_21(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            None,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_22(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            None,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_23(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_24(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_25(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_26(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_27(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(None)

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_28(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(None, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_29(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, None))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_30(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_31(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, ))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_32(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() + 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_33(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 26, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_34(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 31))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_35(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = None

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_36(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            None,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_37(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            None,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_38(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            None,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_39(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            None,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_40(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_41(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_42(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_43(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_44(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(None)
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_45(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(None, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_46(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, None))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_47(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_48(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, ))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_49(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(21, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_50(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 51))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_51(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = None
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_52(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) / 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_53(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() + self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_54(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 3
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_55(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = None

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_56(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) / 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_57(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() + self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_58(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 3

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_59(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(None)
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_60(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(None, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_61(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, None))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_62(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_63(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, ))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_64(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = None
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_65(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) / 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_66(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() + self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_67(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 3
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_68(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(None)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_69(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = None
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_70(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(None, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_71(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, None, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_72(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, None)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_73(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_74(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_75(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, )
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_76(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(165, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_77(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 165, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_78(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 165)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_79(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(None, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_80(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, None, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_81(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, None)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_82(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_83(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_84(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, )
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_85(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(224, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_86(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 224, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_87(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 224)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_88(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = None
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_89(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(None, 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_90(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), None, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_91(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, None)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_92(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_93(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_94(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, )
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_95(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 6, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_96(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 6)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_97(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = None
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_98(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(None, 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_99(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), None, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_100(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, None)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_101(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_102(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_103(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, )

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_104(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 16, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_105(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 16)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_106(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = None
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_107(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) / 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_108(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() + _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_109(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 3
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_110(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(None, _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_111(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), None)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_112(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(_handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_113(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), )

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_114(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(None), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_115(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(None)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_116(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = None
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_117(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            None,
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_118(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            None,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_119(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            None,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_120(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            None,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_121(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_122(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_123(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_124(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_125(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            16,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_126(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            16,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_127(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = None
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_128(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(None)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_129(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(None, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_130(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, None)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_131(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_132(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, )
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_133(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, False)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_134(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(None, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_135(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, None)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_136(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_137(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, )
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_138(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, False)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_139(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(None, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_140(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, None)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_141(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_142(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, )
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_143(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, False)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_144(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(None, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_145(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, None)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_146(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_147(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, )
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_148(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, False)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_149(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = None
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_150(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(None, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_151(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, None, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_152(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, None)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_153(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_154(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_155(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, )
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_156(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(165, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_157(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 165, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_158(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 165)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_159(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(None)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_160(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(1.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_161(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(None, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_162(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, None)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_163(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_164(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, )  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_165(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = None
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_166(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(None)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_167(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = None
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_168(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(None)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_169(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = None
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_170(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(None)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_171(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(None)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_172(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(111)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_173(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(None)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_174(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(51)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_175(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(None)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_176(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(11)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_177(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = None
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_178(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            None,
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_179(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            None,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_180(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            None,
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_181(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_182(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_183(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_184(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            201.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_185(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(None, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_186(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, None)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_187(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(_color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_188(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, )
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_189(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(1, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_190(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(None, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_191(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, None)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_192(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(_color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_193(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, )
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_194(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(1.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_195(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(None, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_196(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, None)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_197(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(_color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_198(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, )

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_199(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(2, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_200(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = None
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_201(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            None,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_202(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            None,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_203(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            None,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_204(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            None,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_205(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_206(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_207(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_208(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_209(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = None
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_210(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(None)
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_211(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor(None))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_212(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("XX#888888XX"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_213(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = None
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_214(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(None)
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_215(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = None

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_216(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 5

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_217(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            None, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_218(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, None, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_219(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, None, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_220(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, None
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_221(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_222(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_223(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_224(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_225(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(None)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_226(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = None
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_227(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                ) - self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_228(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    None, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_229(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, None, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_230(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, None, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_231(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, None
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_232(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_233(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_234(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_235(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_236(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = None
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_237(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = None  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_238(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 - 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_239(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 16  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_240(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = None
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_241(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(None)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_242(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = None
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_243(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(None)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_244(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = None
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_245(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = None
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_246(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x + text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_247(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w / 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_248(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 3
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_249(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = None
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_250(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h - label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_251(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 - text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_252(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(None)
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_253(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(None, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_254(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, None, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_255(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, None))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_256(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_257(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_258(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, ))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_259(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(256, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_260(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 256, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_261(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 256))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_262(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(None, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_263(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, None, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_264(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, None, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_265(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, None)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_266(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_267(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_268(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_269(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, )
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_270(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(None, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_271(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, None, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_272(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, None)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_273(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_274(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_275(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, )

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_276(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(None)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_277(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(None, painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_278(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, None)
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_279(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(painter.brush())
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_280(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, )
        painter.fillPath(_handle_path, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_281(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(None, _handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_282(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, None)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_283(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_color)
        painter.end()

    def xǁBlocksSliderǁpaintEvent__mutmut_284(self, ev: QtGui.QPaintEvent) -> None:
        """Re-implemented method, paint widget"""
        opt = QtWidgets.QStyleOptionSlider()
        self.initStyleOption(opt)
        _style = self.style()

        # Clip the opt rect inside, so the handle and
        # groove doesn't exceed the limits
        opt.rect = opt.rect.adjusted(12, 10, -18, 20)  # This is a bit hardcoded

        self._groove_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderGroove,
            self,
        )

        self._groove_rect.setSize(QtCore.QSize(self.width() - 25, 30))

        self._handle_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderHandle,
            self,
        )

        self._handle_rect.setSize(QtCore.QSize(20, 50))
        # self.style().subControlRect(
        #     QtWidgets.QStyle.ComplexControl.CC_Slider, opt, QtWidgets.QStyle.SubControl.SC_SliderGroove or QtWidgets.QStyle.SubControl.SC_SliderHandle
        # )

        # if opt.state & QtWidgets.QStyle.StateFlag.State_Sunken:
        #     # give the track a color
        #     ...
        # elif opt.state & QtWidgets.QStyle.StateFlag.State_MouseOver:
        #     # Give another color when the mouse is over the track
        #     ...
        # else:
        #     # give a default color for the track
        #     ...
        _groove_x = (self.width() - self._groove_rect.width()) // 2
        _groove_y = (self.height() - self._groove_rect.height()) // 2

        self._groove_rect.moveTo(QtCore.QPoint(_groove_x, _groove_y))
        _handle_y = (self.height() - self._handle_rect.height()) // 2
        self._handle_rect.moveTop(_handle_y)

        _handle_color = (
            QtGui.QColor(164, 164, 164)
            if self.isSliderDown()
            else QtGui.QColor(223, 223, 223)
        )
        _handle_path = QtGui.QPainterPath()
        _handle_path.addRoundedRect(self._handle_rect.toRectF(), 5, 5)
        _groove_path = QtGui.QPainterPath()
        _groove_path.addRoundedRect(self._groove_rect.toRectF(), 15, 15)

        if self.isSliderDown():
            _handle_x = (
                self.sliderPosition() - _handle_path.currentPosition().x()
            ) // 2
            _handle_path.moveTo(int(round(_handle_x)), _handle_y)

        gradient_path = QtGui.QPainterPath()
        gradient_path.addRoundedRect(
            self._groove_rect.toRectF(),
            15,
            15,
            QtCore.Qt.SizeMode.AbsoluteSize,
        )
        painter = QtGui.QPainter(self)
        painter.setRenderHint(painter.RenderHint.Antialiasing, True)
        painter.setRenderHint(painter.RenderHint.LosslessImageRendering, True)
        painter.setRenderHint(painter.RenderHint.SmoothPixmapTransform, True)
        painter.setRenderHint(painter.RenderHint.TextAntialiasing, True)
        _color = QtGui.QColor(164, 164, 164)
        _color.setAlphaF(0.5)
        painter.fillPath(_groove_path, _color)  # Primary groove background color

        _color = QtGui.QColor(self.highlight_color)
        _color_1 = QtGui.QColor(self.highlight_color)
        _color_2 = QtGui.QColor(self.highlight_color)
        _color.setAlpha(110)
        _color_1.setAlpha(50)
        _color_2.setAlpha(10)
        _gradient = QtGui.QRadialGradient(
            self._handle_rect.center().toPointF(),
            200.0,
            self._handle_rect.center().toPointF(),
        )
        _gradient.setColorAt(0, _color)
        _gradient.setColorAt(0.5, _color_1)
        _gradient.setColorAt(1, _color_2)

        self.text_box_rect = _style.subControlRect(
            QtWidgets.QStyle.ComplexControl.CC_Slider,
            opt,
            QtWidgets.QStyle.SubControl.SC_SliderTickmarks,
            self,
        )
        min_v, max_v = self.minimum(), self.maximum()
        painter.setPen(QtGui.QColor("#888888"))
        fm = QtGui.QFontMetrics(painter.font())
        label_offset = 4

        _style.drawComplexControl(
            QtWidgets.QStyle.ComplexControl.CC_Slider, opt, painter, self
        )
        self.setStyle(_style)

        for v in [min_v, max_v]:
            x = (
                QtWidgets.QStyle.sliderPositionFromValue(
                    min_v, max_v, v, self._groove_rect.width()
                )
                + self._groove_rect.x()
            )
            y1 = self._groove_rect.bottom()
            y2 = y1 + 15  # tick length
            label = str(v)
            text_w = fm.horizontalAdvance(label)
            text_h = fm.ascent()
            text_x = x - text_w // 2
            text_y = y2 + text_h + label_offset
            painter.setPen(QtGui.QColor(255, 255, 255))
            painter.drawLine(x, y1, x, y2)
            painter.drawText(text_x, text_y, label)

        # Paint the elements with colors
        painter.setBrush(_gradient)
        painter.fillPath(gradient_path, painter.brush())
        painter.fillPath(_handle_path, )
        painter.end()
    
    xǁBlocksSliderǁpaintEvent__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁBlocksSliderǁpaintEvent__mutmut_1': xǁBlocksSliderǁpaintEvent__mutmut_1, 
        'xǁBlocksSliderǁpaintEvent__mutmut_2': xǁBlocksSliderǁpaintEvent__mutmut_2, 
        'xǁBlocksSliderǁpaintEvent__mutmut_3': xǁBlocksSliderǁpaintEvent__mutmut_3, 
        'xǁBlocksSliderǁpaintEvent__mutmut_4': xǁBlocksSliderǁpaintEvent__mutmut_4, 
        'xǁBlocksSliderǁpaintEvent__mutmut_5': xǁBlocksSliderǁpaintEvent__mutmut_5, 
        'xǁBlocksSliderǁpaintEvent__mutmut_6': xǁBlocksSliderǁpaintEvent__mutmut_6, 
        'xǁBlocksSliderǁpaintEvent__mutmut_7': xǁBlocksSliderǁpaintEvent__mutmut_7, 
        'xǁBlocksSliderǁpaintEvent__mutmut_8': xǁBlocksSliderǁpaintEvent__mutmut_8, 
        'xǁBlocksSliderǁpaintEvent__mutmut_9': xǁBlocksSliderǁpaintEvent__mutmut_9, 
        'xǁBlocksSliderǁpaintEvent__mutmut_10': xǁBlocksSliderǁpaintEvent__mutmut_10, 
        'xǁBlocksSliderǁpaintEvent__mutmut_11': xǁBlocksSliderǁpaintEvent__mutmut_11, 
        'xǁBlocksSliderǁpaintEvent__mutmut_12': xǁBlocksSliderǁpaintEvent__mutmut_12, 
        'xǁBlocksSliderǁpaintEvent__mutmut_13': xǁBlocksSliderǁpaintEvent__mutmut_13, 
        'xǁBlocksSliderǁpaintEvent__mutmut_14': xǁBlocksSliderǁpaintEvent__mutmut_14, 
        'xǁBlocksSliderǁpaintEvent__mutmut_15': xǁBlocksSliderǁpaintEvent__mutmut_15, 
        'xǁBlocksSliderǁpaintEvent__mutmut_16': xǁBlocksSliderǁpaintEvent__mutmut_16, 
        'xǁBlocksSliderǁpaintEvent__mutmut_17': xǁBlocksSliderǁpaintEvent__mutmut_17, 
        'xǁBlocksSliderǁpaintEvent__mutmut_18': xǁBlocksSliderǁpaintEvent__mutmut_18, 
        'xǁBlocksSliderǁpaintEvent__mutmut_19': xǁBlocksSliderǁpaintEvent__mutmut_19, 
        'xǁBlocksSliderǁpaintEvent__mutmut_20': xǁBlocksSliderǁpaintEvent__mutmut_20, 
        'xǁBlocksSliderǁpaintEvent__mutmut_21': xǁBlocksSliderǁpaintEvent__mutmut_21, 
        'xǁBlocksSliderǁpaintEvent__mutmut_22': xǁBlocksSliderǁpaintEvent__mutmut_22, 
        'xǁBlocksSliderǁpaintEvent__mutmut_23': xǁBlocksSliderǁpaintEvent__mutmut_23, 
        'xǁBlocksSliderǁpaintEvent__mutmut_24': xǁBlocksSliderǁpaintEvent__mutmut_24, 
        'xǁBlocksSliderǁpaintEvent__mutmut_25': xǁBlocksSliderǁpaintEvent__mutmut_25, 
        'xǁBlocksSliderǁpaintEvent__mutmut_26': xǁBlocksSliderǁpaintEvent__mutmut_26, 
        'xǁBlocksSliderǁpaintEvent__mutmut_27': xǁBlocksSliderǁpaintEvent__mutmut_27, 
        'xǁBlocksSliderǁpaintEvent__mutmut_28': xǁBlocksSliderǁpaintEvent__mutmut_28, 
        'xǁBlocksSliderǁpaintEvent__mutmut_29': xǁBlocksSliderǁpaintEvent__mutmut_29, 
        'xǁBlocksSliderǁpaintEvent__mutmut_30': xǁBlocksSliderǁpaintEvent__mutmut_30, 
        'xǁBlocksSliderǁpaintEvent__mutmut_31': xǁBlocksSliderǁpaintEvent__mutmut_31, 
        'xǁBlocksSliderǁpaintEvent__mutmut_32': xǁBlocksSliderǁpaintEvent__mutmut_32, 
        'xǁBlocksSliderǁpaintEvent__mutmut_33': xǁBlocksSliderǁpaintEvent__mutmut_33, 
        'xǁBlocksSliderǁpaintEvent__mutmut_34': xǁBlocksSliderǁpaintEvent__mutmut_34, 
        'xǁBlocksSliderǁpaintEvent__mutmut_35': xǁBlocksSliderǁpaintEvent__mutmut_35, 
        'xǁBlocksSliderǁpaintEvent__mutmut_36': xǁBlocksSliderǁpaintEvent__mutmut_36, 
        'xǁBlocksSliderǁpaintEvent__mutmut_37': xǁBlocksSliderǁpaintEvent__mutmut_37, 
        'xǁBlocksSliderǁpaintEvent__mutmut_38': xǁBlocksSliderǁpaintEvent__mutmut_38, 
        'xǁBlocksSliderǁpaintEvent__mutmut_39': xǁBlocksSliderǁpaintEvent__mutmut_39, 
        'xǁBlocksSliderǁpaintEvent__mutmut_40': xǁBlocksSliderǁpaintEvent__mutmut_40, 
        'xǁBlocksSliderǁpaintEvent__mutmut_41': xǁBlocksSliderǁpaintEvent__mutmut_41, 
        'xǁBlocksSliderǁpaintEvent__mutmut_42': xǁBlocksSliderǁpaintEvent__mutmut_42, 
        'xǁBlocksSliderǁpaintEvent__mutmut_43': xǁBlocksSliderǁpaintEvent__mutmut_43, 
        'xǁBlocksSliderǁpaintEvent__mutmut_44': xǁBlocksSliderǁpaintEvent__mutmut_44, 
        'xǁBlocksSliderǁpaintEvent__mutmut_45': xǁBlocksSliderǁpaintEvent__mutmut_45, 
        'xǁBlocksSliderǁpaintEvent__mutmut_46': xǁBlocksSliderǁpaintEvent__mutmut_46, 
        'xǁBlocksSliderǁpaintEvent__mutmut_47': xǁBlocksSliderǁpaintEvent__mutmut_47, 
        'xǁBlocksSliderǁpaintEvent__mutmut_48': xǁBlocksSliderǁpaintEvent__mutmut_48, 
        'xǁBlocksSliderǁpaintEvent__mutmut_49': xǁBlocksSliderǁpaintEvent__mutmut_49, 
        'xǁBlocksSliderǁpaintEvent__mutmut_50': xǁBlocksSliderǁpaintEvent__mutmut_50, 
        'xǁBlocksSliderǁpaintEvent__mutmut_51': xǁBlocksSliderǁpaintEvent__mutmut_51, 
        'xǁBlocksSliderǁpaintEvent__mutmut_52': xǁBlocksSliderǁpaintEvent__mutmut_52, 
        'xǁBlocksSliderǁpaintEvent__mutmut_53': xǁBlocksSliderǁpaintEvent__mutmut_53, 
        'xǁBlocksSliderǁpaintEvent__mutmut_54': xǁBlocksSliderǁpaintEvent__mutmut_54, 
        'xǁBlocksSliderǁpaintEvent__mutmut_55': xǁBlocksSliderǁpaintEvent__mutmut_55, 
        'xǁBlocksSliderǁpaintEvent__mutmut_56': xǁBlocksSliderǁpaintEvent__mutmut_56, 
        'xǁBlocksSliderǁpaintEvent__mutmut_57': xǁBlocksSliderǁpaintEvent__mutmut_57, 
        'xǁBlocksSliderǁpaintEvent__mutmut_58': xǁBlocksSliderǁpaintEvent__mutmut_58, 
        'xǁBlocksSliderǁpaintEvent__mutmut_59': xǁBlocksSliderǁpaintEvent__mutmut_59, 
        'xǁBlocksSliderǁpaintEvent__mutmut_60': xǁBlocksSliderǁpaintEvent__mutmut_60, 
        'xǁBlocksSliderǁpaintEvent__mutmut_61': xǁBlocksSliderǁpaintEvent__mutmut_61, 
        'xǁBlocksSliderǁpaintEvent__mutmut_62': xǁBlocksSliderǁpaintEvent__mutmut_62, 
        'xǁBlocksSliderǁpaintEvent__mutmut_63': xǁBlocksSliderǁpaintEvent__mutmut_63, 
        'xǁBlocksSliderǁpaintEvent__mutmut_64': xǁBlocksSliderǁpaintEvent__mutmut_64, 
        'xǁBlocksSliderǁpaintEvent__mutmut_65': xǁBlocksSliderǁpaintEvent__mutmut_65, 
        'xǁBlocksSliderǁpaintEvent__mutmut_66': xǁBlocksSliderǁpaintEvent__mutmut_66, 
        'xǁBlocksSliderǁpaintEvent__mutmut_67': xǁBlocksSliderǁpaintEvent__mutmut_67, 
        'xǁBlocksSliderǁpaintEvent__mutmut_68': xǁBlocksSliderǁpaintEvent__mutmut_68, 
        'xǁBlocksSliderǁpaintEvent__mutmut_69': xǁBlocksSliderǁpaintEvent__mutmut_69, 
        'xǁBlocksSliderǁpaintEvent__mutmut_70': xǁBlocksSliderǁpaintEvent__mutmut_70, 
        'xǁBlocksSliderǁpaintEvent__mutmut_71': xǁBlocksSliderǁpaintEvent__mutmut_71, 
        'xǁBlocksSliderǁpaintEvent__mutmut_72': xǁBlocksSliderǁpaintEvent__mutmut_72, 
        'xǁBlocksSliderǁpaintEvent__mutmut_73': xǁBlocksSliderǁpaintEvent__mutmut_73, 
        'xǁBlocksSliderǁpaintEvent__mutmut_74': xǁBlocksSliderǁpaintEvent__mutmut_74, 
        'xǁBlocksSliderǁpaintEvent__mutmut_75': xǁBlocksSliderǁpaintEvent__mutmut_75, 
        'xǁBlocksSliderǁpaintEvent__mutmut_76': xǁBlocksSliderǁpaintEvent__mutmut_76, 
        'xǁBlocksSliderǁpaintEvent__mutmut_77': xǁBlocksSliderǁpaintEvent__mutmut_77, 
        'xǁBlocksSliderǁpaintEvent__mutmut_78': xǁBlocksSliderǁpaintEvent__mutmut_78, 
        'xǁBlocksSliderǁpaintEvent__mutmut_79': xǁBlocksSliderǁpaintEvent__mutmut_79, 
        'xǁBlocksSliderǁpaintEvent__mutmut_80': xǁBlocksSliderǁpaintEvent__mutmut_80, 
        'xǁBlocksSliderǁpaintEvent__mutmut_81': xǁBlocksSliderǁpaintEvent__mutmut_81, 
        'xǁBlocksSliderǁpaintEvent__mutmut_82': xǁBlocksSliderǁpaintEvent__mutmut_82, 
        'xǁBlocksSliderǁpaintEvent__mutmut_83': xǁBlocksSliderǁpaintEvent__mutmut_83, 
        'xǁBlocksSliderǁpaintEvent__mutmut_84': xǁBlocksSliderǁpaintEvent__mutmut_84, 
        'xǁBlocksSliderǁpaintEvent__mutmut_85': xǁBlocksSliderǁpaintEvent__mutmut_85, 
        'xǁBlocksSliderǁpaintEvent__mutmut_86': xǁBlocksSliderǁpaintEvent__mutmut_86, 
        'xǁBlocksSliderǁpaintEvent__mutmut_87': xǁBlocksSliderǁpaintEvent__mutmut_87, 
        'xǁBlocksSliderǁpaintEvent__mutmut_88': xǁBlocksSliderǁpaintEvent__mutmut_88, 
        'xǁBlocksSliderǁpaintEvent__mutmut_89': xǁBlocksSliderǁpaintEvent__mutmut_89, 
        'xǁBlocksSliderǁpaintEvent__mutmut_90': xǁBlocksSliderǁpaintEvent__mutmut_90, 
        'xǁBlocksSliderǁpaintEvent__mutmut_91': xǁBlocksSliderǁpaintEvent__mutmut_91, 
        'xǁBlocksSliderǁpaintEvent__mutmut_92': xǁBlocksSliderǁpaintEvent__mutmut_92, 
        'xǁBlocksSliderǁpaintEvent__mutmut_93': xǁBlocksSliderǁpaintEvent__mutmut_93, 
        'xǁBlocksSliderǁpaintEvent__mutmut_94': xǁBlocksSliderǁpaintEvent__mutmut_94, 
        'xǁBlocksSliderǁpaintEvent__mutmut_95': xǁBlocksSliderǁpaintEvent__mutmut_95, 
        'xǁBlocksSliderǁpaintEvent__mutmut_96': xǁBlocksSliderǁpaintEvent__mutmut_96, 
        'xǁBlocksSliderǁpaintEvent__mutmut_97': xǁBlocksSliderǁpaintEvent__mutmut_97, 
        'xǁBlocksSliderǁpaintEvent__mutmut_98': xǁBlocksSliderǁpaintEvent__mutmut_98, 
        'xǁBlocksSliderǁpaintEvent__mutmut_99': xǁBlocksSliderǁpaintEvent__mutmut_99, 
        'xǁBlocksSliderǁpaintEvent__mutmut_100': xǁBlocksSliderǁpaintEvent__mutmut_100, 
        'xǁBlocksSliderǁpaintEvent__mutmut_101': xǁBlocksSliderǁpaintEvent__mutmut_101, 
        'xǁBlocksSliderǁpaintEvent__mutmut_102': xǁBlocksSliderǁpaintEvent__mutmut_102, 
        'xǁBlocksSliderǁpaintEvent__mutmut_103': xǁBlocksSliderǁpaintEvent__mutmut_103, 
        'xǁBlocksSliderǁpaintEvent__mutmut_104': xǁBlocksSliderǁpaintEvent__mutmut_104, 
        'xǁBlocksSliderǁpaintEvent__mutmut_105': xǁBlocksSliderǁpaintEvent__mutmut_105, 
        'xǁBlocksSliderǁpaintEvent__mutmut_106': xǁBlocksSliderǁpaintEvent__mutmut_106, 
        'xǁBlocksSliderǁpaintEvent__mutmut_107': xǁBlocksSliderǁpaintEvent__mutmut_107, 
        'xǁBlocksSliderǁpaintEvent__mutmut_108': xǁBlocksSliderǁpaintEvent__mutmut_108, 
        'xǁBlocksSliderǁpaintEvent__mutmut_109': xǁBlocksSliderǁpaintEvent__mutmut_109, 
        'xǁBlocksSliderǁpaintEvent__mutmut_110': xǁBlocksSliderǁpaintEvent__mutmut_110, 
        'xǁBlocksSliderǁpaintEvent__mutmut_111': xǁBlocksSliderǁpaintEvent__mutmut_111, 
        'xǁBlocksSliderǁpaintEvent__mutmut_112': xǁBlocksSliderǁpaintEvent__mutmut_112, 
        'xǁBlocksSliderǁpaintEvent__mutmut_113': xǁBlocksSliderǁpaintEvent__mutmut_113, 
        'xǁBlocksSliderǁpaintEvent__mutmut_114': xǁBlocksSliderǁpaintEvent__mutmut_114, 
        'xǁBlocksSliderǁpaintEvent__mutmut_115': xǁBlocksSliderǁpaintEvent__mutmut_115, 
        'xǁBlocksSliderǁpaintEvent__mutmut_116': xǁBlocksSliderǁpaintEvent__mutmut_116, 
        'xǁBlocksSliderǁpaintEvent__mutmut_117': xǁBlocksSliderǁpaintEvent__mutmut_117, 
        'xǁBlocksSliderǁpaintEvent__mutmut_118': xǁBlocksSliderǁpaintEvent__mutmut_118, 
        'xǁBlocksSliderǁpaintEvent__mutmut_119': xǁBlocksSliderǁpaintEvent__mutmut_119, 
        'xǁBlocksSliderǁpaintEvent__mutmut_120': xǁBlocksSliderǁpaintEvent__mutmut_120, 
        'xǁBlocksSliderǁpaintEvent__mutmut_121': xǁBlocksSliderǁpaintEvent__mutmut_121, 
        'xǁBlocksSliderǁpaintEvent__mutmut_122': xǁBlocksSliderǁpaintEvent__mutmut_122, 
        'xǁBlocksSliderǁpaintEvent__mutmut_123': xǁBlocksSliderǁpaintEvent__mutmut_123, 
        'xǁBlocksSliderǁpaintEvent__mutmut_124': xǁBlocksSliderǁpaintEvent__mutmut_124, 
        'xǁBlocksSliderǁpaintEvent__mutmut_125': xǁBlocksSliderǁpaintEvent__mutmut_125, 
        'xǁBlocksSliderǁpaintEvent__mutmut_126': xǁBlocksSliderǁpaintEvent__mutmut_126, 
        'xǁBlocksSliderǁpaintEvent__mutmut_127': xǁBlocksSliderǁpaintEvent__mutmut_127, 
        'xǁBlocksSliderǁpaintEvent__mutmut_128': xǁBlocksSliderǁpaintEvent__mutmut_128, 
        'xǁBlocksSliderǁpaintEvent__mutmut_129': xǁBlocksSliderǁpaintEvent__mutmut_129, 
        'xǁBlocksSliderǁpaintEvent__mutmut_130': xǁBlocksSliderǁpaintEvent__mutmut_130, 
        'xǁBlocksSliderǁpaintEvent__mutmut_131': xǁBlocksSliderǁpaintEvent__mutmut_131, 
        'xǁBlocksSliderǁpaintEvent__mutmut_132': xǁBlocksSliderǁpaintEvent__mutmut_132, 
        'xǁBlocksSliderǁpaintEvent__mutmut_133': xǁBlocksSliderǁpaintEvent__mutmut_133, 
        'xǁBlocksSliderǁpaintEvent__mutmut_134': xǁBlocksSliderǁpaintEvent__mutmut_134, 
        'xǁBlocksSliderǁpaintEvent__mutmut_135': xǁBlocksSliderǁpaintEvent__mutmut_135, 
        'xǁBlocksSliderǁpaintEvent__mutmut_136': xǁBlocksSliderǁpaintEvent__mutmut_136, 
        'xǁBlocksSliderǁpaintEvent__mutmut_137': xǁBlocksSliderǁpaintEvent__mutmut_137, 
        'xǁBlocksSliderǁpaintEvent__mutmut_138': xǁBlocksSliderǁpaintEvent__mutmut_138, 
        'xǁBlocksSliderǁpaintEvent__mutmut_139': xǁBlocksSliderǁpaintEvent__mutmut_139, 
        'xǁBlocksSliderǁpaintEvent__mutmut_140': xǁBlocksSliderǁpaintEvent__mutmut_140, 
        'xǁBlocksSliderǁpaintEvent__mutmut_141': xǁBlocksSliderǁpaintEvent__mutmut_141, 
        'xǁBlocksSliderǁpaintEvent__mutmut_142': xǁBlocksSliderǁpaintEvent__mutmut_142, 
        'xǁBlocksSliderǁpaintEvent__mutmut_143': xǁBlocksSliderǁpaintEvent__mutmut_143, 
        'xǁBlocksSliderǁpaintEvent__mutmut_144': xǁBlocksSliderǁpaintEvent__mutmut_144, 
        'xǁBlocksSliderǁpaintEvent__mutmut_145': xǁBlocksSliderǁpaintEvent__mutmut_145, 
        'xǁBlocksSliderǁpaintEvent__mutmut_146': xǁBlocksSliderǁpaintEvent__mutmut_146, 
        'xǁBlocksSliderǁpaintEvent__mutmut_147': xǁBlocksSliderǁpaintEvent__mutmut_147, 
        'xǁBlocksSliderǁpaintEvent__mutmut_148': xǁBlocksSliderǁpaintEvent__mutmut_148, 
        'xǁBlocksSliderǁpaintEvent__mutmut_149': xǁBlocksSliderǁpaintEvent__mutmut_149, 
        'xǁBlocksSliderǁpaintEvent__mutmut_150': xǁBlocksSliderǁpaintEvent__mutmut_150, 
        'xǁBlocksSliderǁpaintEvent__mutmut_151': xǁBlocksSliderǁpaintEvent__mutmut_151, 
        'xǁBlocksSliderǁpaintEvent__mutmut_152': xǁBlocksSliderǁpaintEvent__mutmut_152, 
        'xǁBlocksSliderǁpaintEvent__mutmut_153': xǁBlocksSliderǁpaintEvent__mutmut_153, 
        'xǁBlocksSliderǁpaintEvent__mutmut_154': xǁBlocksSliderǁpaintEvent__mutmut_154, 
        'xǁBlocksSliderǁpaintEvent__mutmut_155': xǁBlocksSliderǁpaintEvent__mutmut_155, 
        'xǁBlocksSliderǁpaintEvent__mutmut_156': xǁBlocksSliderǁpaintEvent__mutmut_156, 
        'xǁBlocksSliderǁpaintEvent__mutmut_157': xǁBlocksSliderǁpaintEvent__mutmut_157, 
        'xǁBlocksSliderǁpaintEvent__mutmut_158': xǁBlocksSliderǁpaintEvent__mutmut_158, 
        'xǁBlocksSliderǁpaintEvent__mutmut_159': xǁBlocksSliderǁpaintEvent__mutmut_159, 
        'xǁBlocksSliderǁpaintEvent__mutmut_160': xǁBlocksSliderǁpaintEvent__mutmut_160, 
        'xǁBlocksSliderǁpaintEvent__mutmut_161': xǁBlocksSliderǁpaintEvent__mutmut_161, 
        'xǁBlocksSliderǁpaintEvent__mutmut_162': xǁBlocksSliderǁpaintEvent__mutmut_162, 
        'xǁBlocksSliderǁpaintEvent__mutmut_163': xǁBlocksSliderǁpaintEvent__mutmut_163, 
        'xǁBlocksSliderǁpaintEvent__mutmut_164': xǁBlocksSliderǁpaintEvent__mutmut_164, 
        'xǁBlocksSliderǁpaintEvent__mutmut_165': xǁBlocksSliderǁpaintEvent__mutmut_165, 
        'xǁBlocksSliderǁpaintEvent__mutmut_166': xǁBlocksSliderǁpaintEvent__mutmut_166, 
        'xǁBlocksSliderǁpaintEvent__mutmut_167': xǁBlocksSliderǁpaintEvent__mutmut_167, 
        'xǁBlocksSliderǁpaintEvent__mutmut_168': xǁBlocksSliderǁpaintEvent__mutmut_168, 
        'xǁBlocksSliderǁpaintEvent__mutmut_169': xǁBlocksSliderǁpaintEvent__mutmut_169, 
        'xǁBlocksSliderǁpaintEvent__mutmut_170': xǁBlocksSliderǁpaintEvent__mutmut_170, 
        'xǁBlocksSliderǁpaintEvent__mutmut_171': xǁBlocksSliderǁpaintEvent__mutmut_171, 
        'xǁBlocksSliderǁpaintEvent__mutmut_172': xǁBlocksSliderǁpaintEvent__mutmut_172, 
        'xǁBlocksSliderǁpaintEvent__mutmut_173': xǁBlocksSliderǁpaintEvent__mutmut_173, 
        'xǁBlocksSliderǁpaintEvent__mutmut_174': xǁBlocksSliderǁpaintEvent__mutmut_174, 
        'xǁBlocksSliderǁpaintEvent__mutmut_175': xǁBlocksSliderǁpaintEvent__mutmut_175, 
        'xǁBlocksSliderǁpaintEvent__mutmut_176': xǁBlocksSliderǁpaintEvent__mutmut_176, 
        'xǁBlocksSliderǁpaintEvent__mutmut_177': xǁBlocksSliderǁpaintEvent__mutmut_177, 
        'xǁBlocksSliderǁpaintEvent__mutmut_178': xǁBlocksSliderǁpaintEvent__mutmut_178, 
        'xǁBlocksSliderǁpaintEvent__mutmut_179': xǁBlocksSliderǁpaintEvent__mutmut_179, 
        'xǁBlocksSliderǁpaintEvent__mutmut_180': xǁBlocksSliderǁpaintEvent__mutmut_180, 
        'xǁBlocksSliderǁpaintEvent__mutmut_181': xǁBlocksSliderǁpaintEvent__mutmut_181, 
        'xǁBlocksSliderǁpaintEvent__mutmut_182': xǁBlocksSliderǁpaintEvent__mutmut_182, 
        'xǁBlocksSliderǁpaintEvent__mutmut_183': xǁBlocksSliderǁpaintEvent__mutmut_183, 
        'xǁBlocksSliderǁpaintEvent__mutmut_184': xǁBlocksSliderǁpaintEvent__mutmut_184, 
        'xǁBlocksSliderǁpaintEvent__mutmut_185': xǁBlocksSliderǁpaintEvent__mutmut_185, 
        'xǁBlocksSliderǁpaintEvent__mutmut_186': xǁBlocksSliderǁpaintEvent__mutmut_186, 
        'xǁBlocksSliderǁpaintEvent__mutmut_187': xǁBlocksSliderǁpaintEvent__mutmut_187, 
        'xǁBlocksSliderǁpaintEvent__mutmut_188': xǁBlocksSliderǁpaintEvent__mutmut_188, 
        'xǁBlocksSliderǁpaintEvent__mutmut_189': xǁBlocksSliderǁpaintEvent__mutmut_189, 
        'xǁBlocksSliderǁpaintEvent__mutmut_190': xǁBlocksSliderǁpaintEvent__mutmut_190, 
        'xǁBlocksSliderǁpaintEvent__mutmut_191': xǁBlocksSliderǁpaintEvent__mutmut_191, 
        'xǁBlocksSliderǁpaintEvent__mutmut_192': xǁBlocksSliderǁpaintEvent__mutmut_192, 
        'xǁBlocksSliderǁpaintEvent__mutmut_193': xǁBlocksSliderǁpaintEvent__mutmut_193, 
        'xǁBlocksSliderǁpaintEvent__mutmut_194': xǁBlocksSliderǁpaintEvent__mutmut_194, 
        'xǁBlocksSliderǁpaintEvent__mutmut_195': xǁBlocksSliderǁpaintEvent__mutmut_195, 
        'xǁBlocksSliderǁpaintEvent__mutmut_196': xǁBlocksSliderǁpaintEvent__mutmut_196, 
        'xǁBlocksSliderǁpaintEvent__mutmut_197': xǁBlocksSliderǁpaintEvent__mutmut_197, 
        'xǁBlocksSliderǁpaintEvent__mutmut_198': xǁBlocksSliderǁpaintEvent__mutmut_198, 
        'xǁBlocksSliderǁpaintEvent__mutmut_199': xǁBlocksSliderǁpaintEvent__mutmut_199, 
        'xǁBlocksSliderǁpaintEvent__mutmut_200': xǁBlocksSliderǁpaintEvent__mutmut_200, 
        'xǁBlocksSliderǁpaintEvent__mutmut_201': xǁBlocksSliderǁpaintEvent__mutmut_201, 
        'xǁBlocksSliderǁpaintEvent__mutmut_202': xǁBlocksSliderǁpaintEvent__mutmut_202, 
        'xǁBlocksSliderǁpaintEvent__mutmut_203': xǁBlocksSliderǁpaintEvent__mutmut_203, 
        'xǁBlocksSliderǁpaintEvent__mutmut_204': xǁBlocksSliderǁpaintEvent__mutmut_204, 
        'xǁBlocksSliderǁpaintEvent__mutmut_205': xǁBlocksSliderǁpaintEvent__mutmut_205, 
        'xǁBlocksSliderǁpaintEvent__mutmut_206': xǁBlocksSliderǁpaintEvent__mutmut_206, 
        'xǁBlocksSliderǁpaintEvent__mutmut_207': xǁBlocksSliderǁpaintEvent__mutmut_207, 
        'xǁBlocksSliderǁpaintEvent__mutmut_208': xǁBlocksSliderǁpaintEvent__mutmut_208, 
        'xǁBlocksSliderǁpaintEvent__mutmut_209': xǁBlocksSliderǁpaintEvent__mutmut_209, 
        'xǁBlocksSliderǁpaintEvent__mutmut_210': xǁBlocksSliderǁpaintEvent__mutmut_210, 
        'xǁBlocksSliderǁpaintEvent__mutmut_211': xǁBlocksSliderǁpaintEvent__mutmut_211, 
        'xǁBlocksSliderǁpaintEvent__mutmut_212': xǁBlocksSliderǁpaintEvent__mutmut_212, 
        'xǁBlocksSliderǁpaintEvent__mutmut_213': xǁBlocksSliderǁpaintEvent__mutmut_213, 
        'xǁBlocksSliderǁpaintEvent__mutmut_214': xǁBlocksSliderǁpaintEvent__mutmut_214, 
        'xǁBlocksSliderǁpaintEvent__mutmut_215': xǁBlocksSliderǁpaintEvent__mutmut_215, 
        'xǁBlocksSliderǁpaintEvent__mutmut_216': xǁBlocksSliderǁpaintEvent__mutmut_216, 
        'xǁBlocksSliderǁpaintEvent__mutmut_217': xǁBlocksSliderǁpaintEvent__mutmut_217, 
        'xǁBlocksSliderǁpaintEvent__mutmut_218': xǁBlocksSliderǁpaintEvent__mutmut_218, 
        'xǁBlocksSliderǁpaintEvent__mutmut_219': xǁBlocksSliderǁpaintEvent__mutmut_219, 
        'xǁBlocksSliderǁpaintEvent__mutmut_220': xǁBlocksSliderǁpaintEvent__mutmut_220, 
        'xǁBlocksSliderǁpaintEvent__mutmut_221': xǁBlocksSliderǁpaintEvent__mutmut_221, 
        'xǁBlocksSliderǁpaintEvent__mutmut_222': xǁBlocksSliderǁpaintEvent__mutmut_222, 
        'xǁBlocksSliderǁpaintEvent__mutmut_223': xǁBlocksSliderǁpaintEvent__mutmut_223, 
        'xǁBlocksSliderǁpaintEvent__mutmut_224': xǁBlocksSliderǁpaintEvent__mutmut_224, 
        'xǁBlocksSliderǁpaintEvent__mutmut_225': xǁBlocksSliderǁpaintEvent__mutmut_225, 
        'xǁBlocksSliderǁpaintEvent__mutmut_226': xǁBlocksSliderǁpaintEvent__mutmut_226, 
        'xǁBlocksSliderǁpaintEvent__mutmut_227': xǁBlocksSliderǁpaintEvent__mutmut_227, 
        'xǁBlocksSliderǁpaintEvent__mutmut_228': xǁBlocksSliderǁpaintEvent__mutmut_228, 
        'xǁBlocksSliderǁpaintEvent__mutmut_229': xǁBlocksSliderǁpaintEvent__mutmut_229, 
        'xǁBlocksSliderǁpaintEvent__mutmut_230': xǁBlocksSliderǁpaintEvent__mutmut_230, 
        'xǁBlocksSliderǁpaintEvent__mutmut_231': xǁBlocksSliderǁpaintEvent__mutmut_231, 
        'xǁBlocksSliderǁpaintEvent__mutmut_232': xǁBlocksSliderǁpaintEvent__mutmut_232, 
        'xǁBlocksSliderǁpaintEvent__mutmut_233': xǁBlocksSliderǁpaintEvent__mutmut_233, 
        'xǁBlocksSliderǁpaintEvent__mutmut_234': xǁBlocksSliderǁpaintEvent__mutmut_234, 
        'xǁBlocksSliderǁpaintEvent__mutmut_235': xǁBlocksSliderǁpaintEvent__mutmut_235, 
        'xǁBlocksSliderǁpaintEvent__mutmut_236': xǁBlocksSliderǁpaintEvent__mutmut_236, 
        'xǁBlocksSliderǁpaintEvent__mutmut_237': xǁBlocksSliderǁpaintEvent__mutmut_237, 
        'xǁBlocksSliderǁpaintEvent__mutmut_238': xǁBlocksSliderǁpaintEvent__mutmut_238, 
        'xǁBlocksSliderǁpaintEvent__mutmut_239': xǁBlocksSliderǁpaintEvent__mutmut_239, 
        'xǁBlocksSliderǁpaintEvent__mutmut_240': xǁBlocksSliderǁpaintEvent__mutmut_240, 
        'xǁBlocksSliderǁpaintEvent__mutmut_241': xǁBlocksSliderǁpaintEvent__mutmut_241, 
        'xǁBlocksSliderǁpaintEvent__mutmut_242': xǁBlocksSliderǁpaintEvent__mutmut_242, 
        'xǁBlocksSliderǁpaintEvent__mutmut_243': xǁBlocksSliderǁpaintEvent__mutmut_243, 
        'xǁBlocksSliderǁpaintEvent__mutmut_244': xǁBlocksSliderǁpaintEvent__mutmut_244, 
        'xǁBlocksSliderǁpaintEvent__mutmut_245': xǁBlocksSliderǁpaintEvent__mutmut_245, 
        'xǁBlocksSliderǁpaintEvent__mutmut_246': xǁBlocksSliderǁpaintEvent__mutmut_246, 
        'xǁBlocksSliderǁpaintEvent__mutmut_247': xǁBlocksSliderǁpaintEvent__mutmut_247, 
        'xǁBlocksSliderǁpaintEvent__mutmut_248': xǁBlocksSliderǁpaintEvent__mutmut_248, 
        'xǁBlocksSliderǁpaintEvent__mutmut_249': xǁBlocksSliderǁpaintEvent__mutmut_249, 
        'xǁBlocksSliderǁpaintEvent__mutmut_250': xǁBlocksSliderǁpaintEvent__mutmut_250, 
        'xǁBlocksSliderǁpaintEvent__mutmut_251': xǁBlocksSliderǁpaintEvent__mutmut_251, 
        'xǁBlocksSliderǁpaintEvent__mutmut_252': xǁBlocksSliderǁpaintEvent__mutmut_252, 
        'xǁBlocksSliderǁpaintEvent__mutmut_253': xǁBlocksSliderǁpaintEvent__mutmut_253, 
        'xǁBlocksSliderǁpaintEvent__mutmut_254': xǁBlocksSliderǁpaintEvent__mutmut_254, 
        'xǁBlocksSliderǁpaintEvent__mutmut_255': xǁBlocksSliderǁpaintEvent__mutmut_255, 
        'xǁBlocksSliderǁpaintEvent__mutmut_256': xǁBlocksSliderǁpaintEvent__mutmut_256, 
        'xǁBlocksSliderǁpaintEvent__mutmut_257': xǁBlocksSliderǁpaintEvent__mutmut_257, 
        'xǁBlocksSliderǁpaintEvent__mutmut_258': xǁBlocksSliderǁpaintEvent__mutmut_258, 
        'xǁBlocksSliderǁpaintEvent__mutmut_259': xǁBlocksSliderǁpaintEvent__mutmut_259, 
        'xǁBlocksSliderǁpaintEvent__mutmut_260': xǁBlocksSliderǁpaintEvent__mutmut_260, 
        'xǁBlocksSliderǁpaintEvent__mutmut_261': xǁBlocksSliderǁpaintEvent__mutmut_261, 
        'xǁBlocksSliderǁpaintEvent__mutmut_262': xǁBlocksSliderǁpaintEvent__mutmut_262, 
        'xǁBlocksSliderǁpaintEvent__mutmut_263': xǁBlocksSliderǁpaintEvent__mutmut_263, 
        'xǁBlocksSliderǁpaintEvent__mutmut_264': xǁBlocksSliderǁpaintEvent__mutmut_264, 
        'xǁBlocksSliderǁpaintEvent__mutmut_265': xǁBlocksSliderǁpaintEvent__mutmut_265, 
        'xǁBlocksSliderǁpaintEvent__mutmut_266': xǁBlocksSliderǁpaintEvent__mutmut_266, 
        'xǁBlocksSliderǁpaintEvent__mutmut_267': xǁBlocksSliderǁpaintEvent__mutmut_267, 
        'xǁBlocksSliderǁpaintEvent__mutmut_268': xǁBlocksSliderǁpaintEvent__mutmut_268, 
        'xǁBlocksSliderǁpaintEvent__mutmut_269': xǁBlocksSliderǁpaintEvent__mutmut_269, 
        'xǁBlocksSliderǁpaintEvent__mutmut_270': xǁBlocksSliderǁpaintEvent__mutmut_270, 
        'xǁBlocksSliderǁpaintEvent__mutmut_271': xǁBlocksSliderǁpaintEvent__mutmut_271, 
        'xǁBlocksSliderǁpaintEvent__mutmut_272': xǁBlocksSliderǁpaintEvent__mutmut_272, 
        'xǁBlocksSliderǁpaintEvent__mutmut_273': xǁBlocksSliderǁpaintEvent__mutmut_273, 
        'xǁBlocksSliderǁpaintEvent__mutmut_274': xǁBlocksSliderǁpaintEvent__mutmut_274, 
        'xǁBlocksSliderǁpaintEvent__mutmut_275': xǁBlocksSliderǁpaintEvent__mutmut_275, 
        'xǁBlocksSliderǁpaintEvent__mutmut_276': xǁBlocksSliderǁpaintEvent__mutmut_276, 
        'xǁBlocksSliderǁpaintEvent__mutmut_277': xǁBlocksSliderǁpaintEvent__mutmut_277, 
        'xǁBlocksSliderǁpaintEvent__mutmut_278': xǁBlocksSliderǁpaintEvent__mutmut_278, 
        'xǁBlocksSliderǁpaintEvent__mutmut_279': xǁBlocksSliderǁpaintEvent__mutmut_279, 
        'xǁBlocksSliderǁpaintEvent__mutmut_280': xǁBlocksSliderǁpaintEvent__mutmut_280, 
        'xǁBlocksSliderǁpaintEvent__mutmut_281': xǁBlocksSliderǁpaintEvent__mutmut_281, 
        'xǁBlocksSliderǁpaintEvent__mutmut_282': xǁBlocksSliderǁpaintEvent__mutmut_282, 
        'xǁBlocksSliderǁpaintEvent__mutmut_283': xǁBlocksSliderǁpaintEvent__mutmut_283, 
        'xǁBlocksSliderǁpaintEvent__mutmut_284': xǁBlocksSliderǁpaintEvent__mutmut_284
    }
    xǁBlocksSliderǁpaintEvent__mutmut_orig.__name__ = 'xǁBlocksSliderǁpaintEvent'
